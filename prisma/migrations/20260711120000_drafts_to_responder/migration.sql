-- plan4 Фаза D (флип на self-events): черновые (HELPER) больше не используются как парсеры —
-- детект теперь идёт через СВОИ уведомления основного аккаунта (news/inbox). Единственный/все
-- черновые аккаунты становятся ОСНОВНЫМИ (RESPONDER), чтобы работать как отправители.
-- Безопасно сейчас: детект self-events не зависит от роли, поэтому парсер-роль больше не нужна.
-- Аддитивно по данным: меняем только роль; сессии/прокси/настройки аккаунта не трогаем.
UPDATE "InstagramAccount" SET role = 'RESPONDER' WHERE role = 'HELPER';
