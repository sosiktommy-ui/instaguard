-- §4.6 Гарантия доставки директов: дневная статистика доставки на аккаунт
-- {date:'YYYY-MM-DD', tried, ok, lastAlert} — по ней считаем долю недоставленных,
-- шлём троттлированный алерт владельцу и автоматически снижаем темп DM при массовой
-- недоставке (лички закрыты / ограничение). Аддитивно, nullable — существующие записи не трогает.

ALTER TABLE "InstagramAccount" ADD COLUMN IF NOT EXISTS "deliveryStats" JSONB;
