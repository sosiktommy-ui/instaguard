# PLAN-MASTER.md — Глобальный план доведения InstaGuard/ReactiveGram до профессионала

> **Создан:** 2026-07-19. **Статус:** живой рабочий документ. Ведётся до полного закрытия.
> **Назначение:** единый источник правды для ВСЕХ следующих агентов. Здесь — что делать, в каком
> порядке, в каких файлах, как проверять, что уже готово. Это НЕ замена `CLAUDE.md` (журнал сделанного)
> и `PLAN-LOGIN.md` (детальный план капчи) — это НАДПЛАН, связывающий всё и задающий приоритеты.
>
> **ДВА ГЛАВНЫХ ПРИОРИТЕТА (в этом порядке):**
> 1. 🔴 **ВХОД** — все пути входа работают надёжно, капча берётся, аккаунты НЕ горят.
> 2. 🔴 **ФУНКЦИОНАЛ** — каждый триггер срабатывает правильно, каждая связка «триггер→действие»
>    работает, действия идут в правильном порядке с паузами, всё синхронно, на любых оборотах.
>
> Всё остальное (анти-детект, анти-бан, UI, масштаб, безопасность, тесты) — обслуживает эти два.

---

## 0. КАК ПОЛЬЗОВАТЬСЯ ЭТИМ ПЛАНОМ (правила для агентов — ОБЯЗАТЕЛЬНО)

### 0.1 Железные правила
1. **НЕ ЛОМАТЬ РАБОЧЕЕ.** Только улучшать. Перед изменением рабочего кода — понять, ПОЧЕМУ он такой
   (часто за странным кодом стоит уже отловленный живой баг — см. комментарии и `CLAUDE.md`).
2. **Каждый выполненный пункт помечать `[x]`** прямо здесь, с датой, build-тегом и одной строкой «что
   сделано». Частично — `[~]` с пометкой, что осталось. Не трогать — оставлять `[ ]`.
3. **После КАЖДОГО значимого изменения:** `node --check` (воркер) + `npm test` (воркер и Next.js) +
   `tsc --noEmit` (Next.js) → всё зелёное → деплой (push master, Railway авто-деплой) → запись в
   `CLAUDE.md` → «что затестить живьём». Правило [[report-after-each-stage]].
4. **Воркер меняли → поднять `BUILD` в `workers/browser/server.js`** и проверить на `/health`.
5. **НИКАКИХ прямых API-вызовов к Instagram** (ADR-002). Только видимый DOM/навигация реальным
   браузером. `frame.evaluate` внутри СВОЕГО origin капча-фрейма (fbsbx/google) — легально (это не
   Instagram API). Приватный API — причина банов, ради ухода от которого весь проект переписан.
6. **Пароли тестовых аккаунтов ВСЕГДА верны** ([[login-passwords-always-correct]]): пользователь сам
   входит руками до бота. `bad_password` = НИКОГДА не «неверный пароль», а анти-бот/лок/гео/капча.
7. **БЕРЕЧЬ АККАУНТЫ.** Живой тест входа лочит аккаунт после нескольких попыток. НЕ гонять один аккаунт
   подряд. Между попытками — часы. На проде это критично (см. §1.5 account-safety).
8. **Локально Chromium не запускается** ([[local-playwright-blocked-by-av]]) — верификация браузерных
   правок только через редеплой воркера + прогон пользователем. Поэтому каждая правка ДЕТЕРМИНИРОВАНА
   и несёт ДИАГНОСТИКУ в трассу (чтобы по одному прогону понять, что не так, а не гадать).
9. **Код уровня SENIOR:** чисто, без мусора, без мёртвого кода (или помечать `_KeepX`), понятные имена,
   комментарий = ПОЧЕМУ (не что). Малые чистые функции. Тесты на чистую логику.
10. **Изоляция мультитенанта соблюдается ВЕЗДЕ** (`userId`). Любой новый роут/запрос — скоуп по владельцу.

### 0.2 Порядок исполнения (фазы верхнего уровня → секции)
- **Фаза A (СЕЙЧАС):** §1 Вход+капча до идеала (grid-solving, proxy-стратегия, account-safety).
- **Фаза B:** §2–§5 Функционал — детект, действия, все связки триггер→действие, LIKE из беты, много кампаний.
- **Фаза C:** §6 Поведение бота, §7 Анти-детект, §8 Анти-бан — до «неотличим от человека».
- **Фаза D:** §9 Масштаб (мульти-воркер, 20+ юзеров), §10 Безопасность.
- **Фаза E:** §11 UI/описания, §12 Чистка кода.
- **Фаза F:** §13 Живые масштабные тесты (100+/час, матрица прокси, неделя без бана) = go-live gate (§15).

### 0.3 Карта файлов (где что чинить)
| Область | Файлы |
|---|---|
| Капча | `workers/browser/lib/captcha.js` |
| Все пути входа | `workers/browser/lib/login.js`, `selectors.js`, `human.js`, `fingerprint.js`, `browser.js`, `proxy.js` |
| HTTP воркера | `workers/browser/server.js` |
| Обёртка Next→воркер | `lib/browser/client.ts` |
| Оркестрация входа | `app/api/accounts/auth/route.ts`, `.../challenge/route.ts`, `.../resend/route.ts`, `.../import/route.ts` |
| Гео-локаль | `lib/browser/geo.ts` |
| Поллинг/детект/действия | `app/api/poll/route.ts` |
| DM-очередь/авто-поллинг | `instrumentation.ts` |
| Действия в браузере | `workers/browser/lib/actions.js`, `session.js`, `lib/browser/actions.ts` |
| Self-events (детект) | `workers/browser/lib/selfevents.js`, `newsparse.js` |
| Лимиты/прогрев/ритм | `lib/limits.ts`, `lib/activity.ts`, `lib/delivery.ts` |
| Выбор целей/матчинг | `lib/targets.ts`, `lib/match.ts` |
| Безопасность-индекс | `lib/safety.ts` |
| UI вкладок | `app/(dashboard)/{triggers,accounts,stats,proxy,settings,drafts}/page.tsx` |
| Общие UI | `components/common/*`, `components/accounts/*`, `components/stats/*` |
| Схема БД | `prisma/schema.prisma` + `prisma/migrations/*` |
| Связанные планы | `PLAN-LOGIN.md`(капча), `PLAN-PRODUCTION.md`, `PLAN-IDEAL.md`, `PLAN-BEHAVIOR.md`, `AUDIT.md` |

### 0.4 Как отмечать прогресс (шаблон)
```
- [x] 2026-07-XX (build browser-NN) — <что сделано>. Проверено: node --check/tsc/тесты. ⚠️ живьём: <статус>.
- [~] <частично: что сделано / что осталось>.
- [ ] <не начато>.
```

---

# ЧАСТЬ I — 🔴 ПРИОРИТЕТ 1: ВХОД

> Цель: аккаунт заходит НАДЁЖНО по всем путям, капча берётся, аккаунты НЕ горят. Пока вход не идеален —
> всё остальное вторично (без входа нет ни детекта, ни действий).

## 1. ВХОД + КАПЧА ДО ИДЕАЛА

### 1.0 Текущее состояние (снято с живых трасс 2026-07-18/19, честно)
- Экран капчи: **reCAPTCHA Enterprise v2-чекбокс** «I'm not a robot» на `instagram.com/auth_platform/recaptcha/`,
  виджет в iframe `fbsbx.com/captcha/recaptcha/iframe/`, `data-action="ig_login_recaptcha"`, `data-s` НЕТ,
  `data-size="normal"`. Отправка результата — через `successCallback(token)` → `parent.postMessage({CAPTCHA_SOLVED, token})`.
  Кнопки Continue НЕТ.
- **Что уже РАБОТАЕТ (не переоткрывать):** детект sitekey (DOM+URL), enterprise-флаг, вписывание токена во
  все textarea, вызов `data-callback`/`cfg-callback`/`getResponse`-override/form-submit/postMessage, ретраи
  2captcha, счётчик попыток+дедлайны, клик чекбокса (frameLocator+human-мышь+aria-checked+челлендж-детект+повтор),
  PROXY-задача 2captcha (решение через наш IP+UA), сетевой лог verify-запросов, богатая трасса.
- **ГЛАВНАЯ СТЕНА (подтверждена трассами):** даже токен createTask с правильным `action`, решённый (в т.ч.
  через наш прокси), Meta **ОТВЕРГАЕТ** (`verify: экран НЕ сменился ✗`). Клиентская отправка ИДЕНТИЧНА штатной
  (дёргаем родную `successCallback`). Вывод: **Meta режет ПЕРЕСАЖЕННЫЙ Enterprise-токен по risk-score.**
  Пересадка токена (`userrecaptcha`/`createTask` proxyless И proxy) — тупик для этого экрана.

### 1.1 🔴 РЕШЕНИЕ КАПЧИ — НЕСТАНДАРТНО: не пересаживать токен, а РЕШАТЬ В БРАУЗЕРЕ (native token)
> Принцип (подтверждён логикой reCAPTCHA + подсказкой пользователя «решать как человек»): токен, полученный
> В НАШЕЙ сессии (наш браузер, наш IP, наша история мыши), Meta принимает — потому что его risk-score
> высокий/консистентный. Токен с фермы 2captcha — низкий скор → reject. Значит надо, чтобы reCAPTCHA
> выдала токен ИМЕННО в нашей странице.

Порядок реализации (каждый шаг — с диагностикой, деплой, тест на ОТДОХНУВШЕМ аккаунте):

- [ ] **Шаг 1 — довести клик чекбокса (почти готов, build-94).** Использовать боевой `humanClick`
  (`human.js:87`, `curveMoveTo` кривая траектория) вместо кастомного `humanClickAt` — переиспользовать
  проверенный код. Клик по `#recaptcha-anchor` в anchor-фрейме (frameLocator top→fbsbx→anchor). После клика
  читать `aria-checked` и детектить bframe. Трасса: `клик (метод, aria-checked, картинка-челлендж)`.
  **Файлы:** `captcha.js clickRecaptchaCheckbox`. Импортировать `humanClick,curveMoveTo` из `human.js`.
  **Тест:** трасса покажет `aria-checked=true` (прошло) / `картинка-челлендж=ДА` (нужен шаг 2) / клик не прошёл.
- [ ] **Шаг 2 — 🔑 IN-BROWSER GRID-SOLVING (ядро решения).** Когда после клика выпадает картинка-челлендж
  (bframe «выберите все …»): (а) скриншот bframe (challenge-области); (б) распознать инструкцию (текст задания)
  и сетку (3×3 / 4×4 / динамическая); (в) отправить в 2captcha **координатным/грид-методом**
  (`method=base64` + `coordinatescaptcha=1` в in.php, ЛИБО createTask `CoordinatesTask` — уточнить по докам
  2captcha.com/api-docs); (г) получить номера/координаты плиток; (д) **кликнуть плитки МЫШЬЮ в нашем bframe**
  (`humanClick` по каждой); (е) нажать «Verify»/«Подтвердить»; (ж) обработать МНОГОРАУНДОВЫЙ челлендж (reCAPTCHA
  часто даёт 2–4 раунда, иногда «динамические» плитки с fade-in — ждать и перескринивать); (з) когда решено —
  reCAPTCHA САМА дёрнет `successCallback` с РОДНЫМ токеном → Meta примет.
  **Почему это должно взять стену:** токен рождается в нашей сессии (наш браузер+IP+поведение) → скор нормальный.
  **Файлы:** `captcha.js` — новый `solveGridInBrowser(page)`. **Риски:** хрупкий DOM bframe, динамические
  плитки, многораундовость — делать С ДИАГНОСТИКОЙ (скрин каждого раунда в трассу) и лимитом раундов (≤4).
  **Тест:** трасса `грид: раунд 1/… плитки [3,5,7] → verify → раунд 2 … → капча УШЛА ✓`.
- [ ] **Шаг 3 — фолбэк-цепочка.** Порядок попыток: (1) клик чекбокса → вдруг тихо прошло (native, лучший
  случай); (2) если картинка → grid-solving (шаг 2, native token); (3) крайний фолбэк — старая пересадка
  токена (proxy-задача) — оставить, но знать, что Meta её чаще режет. Каждый исход — в трассу.
- [ ] **Шаг 4 — избегать капчу вообще (стратегически, §1.4).** Капча выпадает на НЕдоверенный вход. Лучший
  «фикс» — чистый мобильный/резидентный прокси в стране аккаунта + прогретый отпечаток → IG капчу не
  показывает. Это НЕ отменяет grid-solving (капча всё равно иногда будет), но резко снижает частоту.

### 1.2 Полировка отправки токена (если grid-solving даёт native token — часть уже не нужна, но оставить как фолбэк)
- [x] `injectToken`: textarea + data-callback + рекурсия cfg (глуб.6) + getResponse-override + form-submit +
  postMessage, по всем фреймам (builds 83–89). Не переоткрывать.
- [x] Верификация `waitCaptchaCleared` + сетевой `net:`-лог verify-запросов (build-93). Не переоткрывать.

### 1.3 2captcha — правильное использование (изучено по докам)
- [x] Enterprise → `createTask` (`RecaptchaV2EnterpriseTaskProxyless`/`RecaptchaV2EnterpriseTask`) с
  `enterprisePayload`(action/s), `userAgent`, при наличии прокси — proxy-версия (build-88/92).
- [ ] Для grid-solving — координатный метод 2captcha (уточнить точный контракт на 2captcha.com/api-docs:
  `CoordinatesTask` / `GridTask` / `in.php coordinatescaptcha`). Передавать инструкцию (`textinstructions`)
  и изображение bframe.
- [ ] Баланс 2captcha — следить; в UI показать расход/остаток (см. §7). Фатальные коды (`ZERO_BALANCE`/
  `WRONG_GOOGLEKEY`) — сразу наверх без ретрая (уже есть, `FATAL_2CAPTCHA`).

### 1.4 🔴 ПРОКСИ-СТРАТЕГИЯ (ключ к тому, чтобы капча/бан не выпадали) — см. также §9.4
- [ ] **Гео-матчинг аккаунт↔прокси ОБЯЗАТЕЛЕН.** Индонезийский аккаунт (`*heidy*` = id_ID) + польский
  датацентр-прокси (`proxxxymiron`) = максимум капчи/лока. Нужен прокси В СТРАНЕ аккаунта.
- [ ] **Тип прокси:** мобильные (лучший траст у IG) > резидентные > датацентр (худший, часто бан). Детект
  типа уже есть (`ipapi.is is_datacenter/is_mobile`, `proxy.js checkProxyBrowser`). Показать вердикт в UI.
- [ ] **Гео из строки прокси:** многие провайдеры зашивают гео в строку (`_country-PL_city-warsaw`). Сейчас
  фингерпринт-гео берётся из `Proxy.country` (заполняется только после «Проверить IP»). Доработать: если
  country пуст — вытащить из строки прокси (`lib/browser/geo.ts` + парсер). Иначе непроверенный прокси даёт
  фингерпринт US поверх не-US IP (лишний сигнал).
- [ ] **1 IP = 1 аккаунт одновременно** (не логинить параллельно два аккаунта с одного IP).
- [ ] **Матрица прокси-провайдеров** (§9.4): протестировать 3–5 провайдеров (мобильные id_ID/резидентные),
  замерить: доля капчи, доля бана, скорость, цена → выбрать оптимум цена/качество, задокументировать.

### 1.5 🔴 ACCOUNT-SAFETY при входе (чтобы не жечь аккаунты — БОЛЬНАЯ ТЕМА)
- [ ] **Не долбить вход.** После `bad_password`/лока — НЕ повторять сразу. Сейчас пользователь вручную
  жмёт вход по многу раз → аккаунт лочится. Ввести на уровне UI+бэкенда **кулдаун на повторный вход**
  одного аккаунта (напр. ≥30–60 мин между попытками; после 2–3 неудач — предупреждение «дай отдохнуть N часов»).
- [ ] **Авто-смена прокси при `bad_password`** (опция): следующая попытка — ДРУГОЙ IP из пула (не тот же
  сожжённый), с ограничением 1–2 смены (не бесконечно жечь пул). Пометка выжженных уже есть (`markProxyBlocked`
  на blacklist; расширить эвристикой «этот IP дал bad_password N раз подряд разным аккаунтам → флаг»).
- [ ] **Предупреждение в UI** перед повторным входом: «частые попытки лочат аккаунт; рекомендуется куки/пауза».
- [ ] **Метрика «здоровье попыток входа»** на аккаунт: сколько неудач подряд, когда последняя — видно в UI.

### 1.6 Все ОСТАЛЬНЫЕ пути входа (проверить/довести — из PLAN-LOGIN.md §2)
- [x] P1 обычный логин/пароль (форма `name=email/pass`, устойчивый поиск, human-ввод).
- [x] P2 2FA с ключом (авто-TOTP, окна 0/-1/+1, кнопка Continue во всех фреймах `findButtonAnyFrame`).
- [~] P3 2FA без ключа → «Try another way» → почта (`tryAnotherWayToEmail`) — проверить на живом.
- [x] P4 challenge email/SMS + явный выбор ПОЧТЫ (`chooseEmailChannel`) — код `name=email` учтён.
- [x] P5 device-approval (afad) — ждём ~2.25 мин.
- [ ] P6 капча — §1.1 (в работе).
- [x] P7 вход по кукам (`loginByState`) + фолбэк куки→пароль. **Куки — самый надёжный обход капчи/лока.**
- [x] P8 массовый импорт (паузы, ретрай на другом IP, дожатие challenge/2FA в UI).
- [x] P9 suspended-чекпоинт + image-капча (координатный фолбэк).
- [ ] **Довести вход по кукам до «одной кнопки»:** упростить выгрузку `sessionid` (инструкция/расширение),
  т.к. это рабочий обход капчи. Локальный `tools/cookie-gen/` уже есть — довести UX.

### 1.7 Диагностика входа (уже сильная — поддерживать)
- [x] Скрин экрана при любой неудаче (`captureDiag`), DOM-дамп формы, дамп фреймов капчи, исходники
  хендлеров, `net:`-лог. Трасса компактна в UI, полна в логах воркера. Не ломать.

### 1.8 Критерии «ВХОД ИДЕАЛЕН» (§1 done)
- [ ] На чистом мобильном прокси в гео аккаунта капча НЕ выпадает, вход проходит с первого раза.
- [ ] Если капча выпала — grid-solving берёт её (native token), вход завершается сессией.
- [ ] `bad_password`/лок не наступает от нормального использования (кулдаун+гео+один-IP-один-аккаунт).
- [ ] Все P1–P9 дают ЧЕСТНЫЙ исход (не `network`-заглушка), с диагностикой.
- [ ] Куки-вход = один простой шаг для пользователя.
- [ ] Аккаунты НЕ горят при аккуратном использовании (подтверждено на живом за неделю).

---

# ЧАСТЬ II — 🔴 ПРИОРИТЕТ 2: ФУНКЦИОНАЛ (триггер → действие)

> Цель: КАЖДЫЙ триггер срабатывает правильно на своё событие; КАЖДАЯ связка «триггер→действие»
> выполняется; действия идут в правильном порядке с человеческими паузами; всё синхронно; корректно
> на любых оборотах и при многих кампаниях на одном аккаунте.

## 2. ДЕТЕКТ СОБЫТИЙ (что бот замечает)

### 2.0 Текущее состояние
- Детект = **self-events**: основной аккаунт своей сессией читает СВОИ уведомления (`news/inbox`) — гибрид
  «открыть колокольчик + перехватить JSON ответа приложения» + DOM-резерв под гейтом `panelOpened`
  (`workers/browser/lib/selfevents.js`, разбор в `newsparse.js`). Черновые/HikerAPI из детекта убраны.
- Типы: NEW_FOLLOWER (подписка), NEW_COMMENT (коммент), NEW_LIKE (лайк — В БЕТЕ), STORY_MENTION (ответ/упоминание в сторис).
- Стори-события — отдельно через `/story-inbox` (`readStoryEvents`).

### 2.1 Надёжность детекта
- [ ] **Полнота на всплеске:** `SELF_EVENTS_AMOUNT` (окно чтения) — при 100+ событий/час старые уходят
  за окно. Проверить, что окна достаточно (env, сейчас 60→150) ИЛИ участить чтение при высоком потоке.
  Не терять события молча — логировать «в ленте было > окна».
- [ ] **Дедуп снапшотами** (`FOLLOWERS`/`LIKES`/`COMMENTS`/`STORY`) — проверить, что первый проход = базлайн
  (0 действий, не «поприветствовать всех старых»), а дальше ловятся только новые. Уже реализовано —
  подтвердить на живом.
- [ ] **media_id у комментов** есть (для reply/лайка коммента) — проверить на живом (иногда shortcode).
- [ ] **follow-request у приватного аккаунта** — авто-приём (`acceptFollowRequests`, `autoAcceptFollowers`),
  затем событие «новый подписчик». Проверить на живом приватном аккаунте.
- [ ] При сбое чтения — не терять события (остаются «новыми»), не маскировать причину (уже сделано).

### 2.2 🔴 NEW_LIKE — ВЫВЕСТИ ИЗ БЕТЫ («СКОРО»)
> Сейчас LIKE помечен `soon:true` и на паузе, потому что self-уведомления о лайках АГРЕГИРУЮТСЯ Instagram
> («X и ещё N лайкнули» → топ-актор + счётчик, не полный список). Триггер неполон.
- [ ] **Решение полноты лайков:** когда нужен ВЕСЬ список лайкнувших конкретный пост — добрать likers
  этого поста ЧЕРЕЗ ОСНОВНОЙ аккаунт браузером (открыть пост → список лайков в DOM), НЕ через API/черновых.
  `media_id` у лайк-событий уже есть (`newsparse.js` берёт `a.media[0].id`).
- [ ] **Снять бету:** `triggers/page.tsx` `TRIG_META` LIKE `soon:false`; `api/triggers` `SUPPORTED += NEW_LIKE`;
  `poll/route.ts` `LIKE_TRIGGER_BETA=false`. Проверить, что поток лайков реально выполняется и не двоит.
- [ ] **Тест:** пост с N лайками → обработаны все N (или задокументированный лимит), без задвоений, в пределах
  дневных лимитов.
- [ ] Обновить тексты/бейджи в UI (убрать «СКОРО» у лайка).

## 3. ИСПОЛНЕНИЕ ДЕЙСТВИЙ (что бот делает)

### 3.0 Действия и их состояние
- Директ (DM, +фото отдельным сообщением), Подписка (follow), Лайк (N постов), Просмотр/лайк сторис (N кадров),
  Ответ в комментах (@упоминание; настоящий тред-reply — TODO §3.4), Приглашение при гейте.
- Исполнители: основной браузером; часть (лайк/сторис) можно поручить черновому (`likeByDraft`/`storyByDraft` —
  но черновые из детекта убраны; переоценить нужность).

### 3.1 🔴 Порядок действий на цель + паузы
- [x] Фикс. порядок **follow → like → comment → story → dm** во всех путях (§13.9). Директ последним.
- [ ] **Паузы между действиями** (jitter) и **между целями** — подтвердить на живом, что серия не идёт
  «пулемётной очередью» (ban-сигнал). Коммент-поток — пауза между комментаторами (§[A2], уже добавлена).
- [ ] **Микро-браузинг** между действиями (`preActionBrowse`) — подтвердить, что вызывается.
- [ ] Проверить порядок вживую по журналу/трассе на канарейке.

### 3.2 🔴 Подтверждение КАЖДОГО действия (не «нажал = сделано»)
- [x] Подписка (кнопка сменилась на «Вы подписаны»/«Запрос отправлен»), директ (доставка `confirmDelivered`),
  коммент (поле очистилось), лайк (сердце стало «Unlike»/красное). Не переоткрывать — проверить на живом.
- [ ] Директ недоставлен → НЕ ставить основной на паузу (транзиент, не бан); авто-снижение темпа при массовой
  недоставке (`lib/delivery.ts`) — проверить, что срабатывает и объяснимо.

### 3.3 Краевые аккаунты (бот НЕ должен ломаться)
- [x] Закрытая личка → fallback follow+like; 0 постов → лайк «невозможно» (WARN, не ERROR); 0 сторис →
  сторис «невозможно»; удалённый/переименованный ник → сверка профиля `profileMatches` → `wrong_profile` (пропуск+ретрай).
- [ ] Подтвердить каждый краевой кейс на живом (канарейка на разных целях).

### 3.4 Довести хрупкие действия
- [ ] **Настоящий тред-reply** под конкретным комментом (клик «Ответить» под ним), фолбэк — @упоминание-коммент
  с пометкой degraded. Нужен `media_id` через всю цепочку + хрупкий DOM треда — делать с живой канарейкой.
- [ ] **Живой переход к человеку через ПОИСК** (§4.1 PLAN-BEHAVIOR) — варьированно (иногда поиск, иногда URL),
  со сверкой профиля. Селекторы поиска хрупкие — отлаживать на канарейке.
- [ ] **Фото-в-директ** — уходит отдельным сообщением после текста (build-28). Проверить на живом.

## 4. 🔴 СВЯЗКИ «ТРИГГЕР → ДЕЙСТВИЕ» — КАЖДАЯ И ВСЕ ВМЕСТЕ

> Пользователь особо просил: проверить КАЖДУЮ связку отдельно И все вместе (синхронность).

### 4.1 Матрица связок (проверить КАЖДУЮ на живой канарейке §9.3)
- [ ] NEW_FOLLOWER → директ / подписка-в-ответ / лайк N постов / сторис N кадров — каждое по отдельности.
- [ ] NEW_COMMENT (по фразе-сигналу) → ответ-в-комментах / директ автору / лайк постов автора / подписка / сторис.
- [ ] NEW_LIKE (после снятия беты) → директ / подписка / лайк / сторис лайкнувшему.
- [ ] STORY_MENTION → директ.
- [ ] Гейт подписки (`followed_by`/`mutual`): не подписан → приглашение-коммент, директ не уходит; подписан → всё идёт.
- [ ] Один человек в НЕСКОЛЬКИХ потоках (подписался+лайкнул+прокомментировал) → **1 директ за цикл** (дедуп
  `dmedThisCycle`), но остальные действия (лайк/подписка/сторис/ответ) идемпотентны и идут. Проверить.

### 4.2 🔴 МНОГО КАМПАНИЙ НА ОДНОМ АККАУНТЕ (важный вопрос пользователя: «10 разных кампаний — что будет?»)
> Надо продумать ГЛУБОКО. Сейчас `poll/route.ts` берёт ВСЕ активные триггеры аккаунта и на каждое новое
> событие прогоняет подходящие. Риски при 10 кампаниях:
- [ ] **Конфликт действий:** 3 кампании «Новая подписка» с разными директами → один новый подписчик подходит
  под все три → бот пошлёт 3 директа? **Дедуп `dmedThisCycle` режет до 1 директа/цикл** — но какой из трёх?
  Продумать ПРИОРИТЕТ/выбор кампании (первая активная? по порядку создания?) и задокументировать. Возможно,
  нужно правило «одно событие → одна кампания-победитель» (не все сразу).
- [ ] **Суммирование действий:** если кампания A = директ, кампания B = лайк, кампания C = подписка — на одного
  человека суммируется набор (директ+лайк+подписка). Это ок, но проверить, что не двоятся однотипные (2 лайка).
- [ ] **Дневные лимиты — общие на аккаунт**, не на кампанию: 10 кампаний не должны пробить лимит (follow 15/сут
  и т.д.). Проверить, что `use()`/бюджет считается на АККАУНТ, а не на кампанию (сейчас так — подтвердить).
- [ ] **Дрип при многих кампаниях:** 2–4 цели/цикл — на аккаунт, не на кампанию (иначе 10 кампаний × 4 = залп).
  Проверить `selectTargets` limit.
- [ ] **Приоритет типов:** если у человека событие подходит под NEW_FOLLOWER и NEW_COMMENT (подписался И
  прокомментировал) — оба потока сработают. Порядок follow→like→comment→story→dm сохраняется? Проверить.
- [ ] **Матрица выполнений (Статистика)** должна корректно показывать, какая кампания что сделала, при пересечениях.
- [ ] **Тест:** аккаунт с 10 кампаниями (разные типы+действия) + поток событий → в журнале видно, что каждая
  сработала правильно, дедуп директа работает, лимиты не пробиты, действия не задвоены. **Составить сценарий.**

### 4.3 Синхронность/изоляция
- [x] Сбой одной цели/коммента не роняет весь цикл (per-item try/catch); провалившаяся цель → ретрай;
  бан/челлендж → статус аккаунта + стоп цикла.
- [x] STOP-ON-BLOCK: на первом ограничении IG прерываем батч (не углубляем бан), `sessionDead` останавливает
  прочие потоки цикла.
- [ ] Подтвердить всё на живом при высоком потоке (§9).

### 4.4 «Все действия в итоге выполняются» (не теряются)
- [x] §4.3 (не терять capped-цели при исчерпании лимита — добираются в след. дни).
- [~] §4.3.2 (частичный набор действий при смешанном бюджете — цель получает часть, помечается известной →
  недостающее не добёрётся). Доводка «всё-или-ничего на цель» — с живой канарейкой (нужен peek-бюджет +
  персистентный per-действие дедуп директа). БЭКЛОГ.

## 5. КРИТЕРИИ «ФУНКЦИОНАЛ ИДЕАЛЕН» (§2–4 done)
- [ ] Все триггеры×действия (§4.1) подтверждены на живом ≥1 раз каждая.
- [ ] LIKE из беты, работает, полнота лайкнувших решена.
- [ ] 10 кампаний на аккаунте ведут себя предсказуемо (дедуп, приоритет, лимиты) — задокументировано+протестировано.
- [ ] Порядок действий + паузы подтверждены; ничего не двоится, никто не теряется.
- [ ] На 100+ событий/час бот приветствует в пределах лимитов, растягивая на дни, НЕ теряя людей, НЕ баня аккаунт.

---

# ЧАСТЬ III — ПОВЕДЕНИЕ, АНТИ-ДЕТЕКТ, АНТИ-БАН

## 6. ПОВЕДЕНИЕ БОТА (максимально «живое») — база в PLAN-BEHAVIOR.md
### 6.0 Готово (не переделывать)
- [x] Единая сессия на цикл (один контекст, прогрев один раз), органический прогрев (репертуар активностей),
  humanClick (кривой курсор `curveMoveTo`), humanType (по буквам, редкая опечатка+backspace), микро-браузинг
  между действиями, разнообразие «отвлечений», выход по простою (idle 3 мин).
### 6.1 Доработки
- [ ] **Переход к человеку живым способом** (§4.1 PLAN-BEHAVIOR): клик по актору в панели уведомлений →
  поиск по нику → URL-резерв, со сверкой профиля. Живой ПОИСК — с канарейкой (селекторы хрупкие).
- [ ] **Разнообразие таймингов:** все паузы из диапазонов (jitter), распределение не подозрительно ровное.
- [ ] **Суточный ритм/сон** (`lib/activity.ts activityWindow`): ночью тишина по tz аккаунта, «выходные».
  Проверить, что «Последняя проверка» честная и первое действие после тихих часов не «залпом».
- [ ] **Пики утро/вечер** (§1.6 PLAN-IDEAL) — вероятностное распределение активности по времени суток.
- [ ] **«Отложить действие на следующий визит»** (§1.5 PLAN-IDEAL) — иногда не делать всё разом.
### 6.2 Критерий
- [ ] Два аккаунта в системе выглядят как два РАЗНЫХ человека с разными устройствами/IP/привычками, а не
  как две копии одного бота (поведенчески, по времени, по отпечатку).

## 7. АНТИ-ДЕТЕКТ (отпечаток) — база в PLAN-IDEAL.md §2/§10, AUDIT.md §7.2
### 7.0 Готово (подтверждено живым self-test, redCount=0 — не переоткрывать)
- [x] `navigator.webdriver=false`, headful под Xvfb, WebGL ANGLE (не SwiftShader), UA-CH platform под ОС
  (Windows, не Linux), WebRTC не течёт (JS-редакция ICE), canvas/audio-шум per-account, стабильный отпечаток
  на аккаунт, гео-локаль под страну прокси.
### 7.1 Остаточные сигналы (из глубокого аудита — доделать, проверять self-test через прокси)
- [ ] **UA-CH `platformVersion` протекает как Linux-версия** при `platform="Windows"` (`browser.js:112-113`
  оверрайдит только `platform`) — закрепить `platformVersion` (Win «15.0.0») + `architecture`/`bitness`.
- [ ] **`sec-ch-ua` brand `"Chromium"` вместо `"Google Chrome"`** — по возможности подделать (LOW).
- [ ] **[D3] UA прибит к Chrome/130** — вычислять мажор из реального `chromium.version()`, не хардкодить.
- [ ] **[D5] tz по стране, а не по IP** — брать tz из `ipapi.is location.timezone` конкретного IP.
- [ ] **[D8] пул профилей 5 → 10–15** + варьировать шрифты/цветовую глубину (⚠️ каждый профиль КОНСИСТЕНТЕН
  ОС/GPU — иначе НОВЫЙ сигнал; проверять self-test, [[local-playwright-blocked-by-av]]).
- [ ] **[D6] Mac-профили DPR=2** (Retina), не 1.
- [ ] **Верификация:** после ЛЮБОЙ правки отпечатка — `POST /selftest/fingerprint` через прокси, `redCount=0`.

## 8. АНТИ-БАН (лимиты / прогрев / ритм / доставка) — база в AUDIT.md §5 (подтверждено рабочим)
### 8.0 Готово (НЕ ТРОГАТЬ без причины — проверено)
- [x] Дневные лимиты + прогрев по возрасту (день0=15%→день14=100%), единый бюджет по всем потокам, глобальный
  лок `poll:all`, резерв бюджета fallback, предохранитель бан/challenge→пауза, «первый проход только базлайн»,
  прокси обязателен, дрип 2–4/цикл, пейсинг директов, авто-приём заявок с лимитом, межпроцессный лок сессии,
  single-flight браузера, подтверждение доставки + автоснижение темпа.
### 8.1 Доработки/проверки
- [ ] **Настраиваемые/отключаемые лимиты** (`dailyCaps`, тумблер off) — проверить кламп и предупреждение.
- [ ] **Ban-safety Score наружу** (`lib/safety.ts` + `FleetSafety`) — светофор + авто-регулировка темпа
  (частично `deliveryStats`). Проверить факторы риска.
- [ ] **Гейт подписки** (followed_by/mutual) на живых — оба режима.
- [ ] **Cost-guard 2captcha** — потолок расходов + метрика в UI (см. §11).
- [ ] **Тест выживаемости:** канарейка неделю на нормальных оборотах — 0 банов (go-live gate §13).

---

# ЧАСТЬ IV — МАСШТАБ, БЕЗОПАСНОСТЬ, ИНФРА (основа под 20+ активных юзеров)

## 9. МАСШТАБ / МУЛЬТИ-ВОРКЕР (заложить основу под кучу эмуляторов) — [[must-build-multiworker-6-2]]
### 9.0 Текущее состояние
- Один браузерный воркер (`amiable-laughter`), `BROWSER_CONCURRENCY` ограничивает параллельные контексты,
  single-flight `getBrowser`, lifecycle контекстов закрывается в `finally`. Мультитенант по `userId` есть.
### 9.1 🔴 Мульти-воркер шардинг (ОБЯЗАТЕЛЬНО до реальных юзеров на масштабе — сейчас 1 воркер)
- [ ] **Реестр воркеров + шардинг по `hash(accountId)`** (`lib/browser/client.ts`): несколько воркер-URL,
  аккаунт всегда идёт на СВОЙ воркер (стабильный шард → тёплый контекст, не гоняем сессию между машинами).
  Env `BROWSER_WORKER_URLS` (список) или реестр в БД. Health-чек каждого, failover при падении шарда.
- [ ] **Оценка ёмкости:** 1 воркер держит ~N контекстов (замерить RAM/CPU на Railway при `CONCURRENCY`).
  20 юзеров × M аккаунтов = сколько воркеров? Заложить авто-масштаб/ручное добавление воркеров.
- [ ] **Очереди:** BullMQ dm-очередь + авто-поллинг heartbeat — при многих юзерах сериализация per-account
  локом (§4.8) не должна создавать «пробки». Рассмотреть per-worker очереди.
- [ ] **Изоляция ресурсов:** один тяжёлый юзер не должен голодать остальных (fair scheduling по userId).
### 9.2 Производительность БД/дашборда
- [ ] На 50–80+ аккаунтах: нет N+1 (напр. `mergeStats` делает SELECT на каждое действие — батчить),
  пагинация тяжёлых списков/логов, индексы на горячих запросах (`Log.accountId`, снапшоты, `poll` выборки).
- [ ] Кеши (uid/media/gate-множества) — вынести в Redis/БД с TTL (сейчас per-process, теряются на редеплое).
### 9.3 Канареечный инструмент (для живых тестов §13)
- [x] Кнопка 🧪 (`/api/canary-test`) — один аккаунт РЕАЛЬНО follow+comment+like другого. За debug-флаг (§7).
- [ ] Расширить: прогон КАЖДОЙ связки триггер→действие по кнопке, отчёт по каждой.
### 9.4 Матрица прокси (найти оптимум цена/качество) — связь с §1.4
- [ ] Протестировать 3–5 провайдеров (мобильные id_ID / резидентные разных стран): доля капчи, доля бана,
  скорость входа, стабильность, цена/GB или /IP. Свести в таблицу, выбрать рекомендованный, задокументировать
  в этом файле (§9.4-результаты) и в UI-подсказках (какие прокси брать).

## 10. БЕЗОПАСНОСТЬ (защита от взлома) — база в PLAN-PRODUCTION §14.5
### 10.0 Готово
- [x] Изоляция тенантов по `userId` во всех 29 роутах (аудит 2026-07-14, IDOR нет), `emailPassword` шифруется
  at-rest (AES-256-GCM), JWT-сессии, internal-secret для крона.
### 10.1 Доработки
- [ ] **Шифровать `browserState`/`sessionData` at-rest** (сейчас plaintext JSONB) — это ЖИВЫЕ сессии IG,
  утечка БД = угон аккаунтов. Требует смены типа колонки + правки всех точек чтения — аккуратно, не сломать вход.
- [ ] **Rate-limit на публичные роуты** (`/api/auth/login`, `/register`) — от брутфорса/перебора.
- [ ] **Валидация входных данных** (zod) на всех POST-роутах — уже частично; пройтись по всем.
- [ ] **Секреты не в бандле/гите** — проверить (`BROWSER_WORKER_SECRET`, `TWOCAPTCHA_API_KEY`, `JWT_SECRET`,
  `ENCRYPTION_KEY`, `HIKER_API_KEY`, internal-secret) — только env, не дефолтные в проде.
- [ ] **CSRF/headers** — Next.js middleware; проверить, что мутации защищены сессией.
- [ ] **Логи не текут секретами** (пароли/токены/куки не пишутся в `Log`/UI/URL) — аудит.
- [ ] **Права воркера** — эндпоинты воркера только по `BROWSER_WORKER_SECRET`; проверить каждый.
- [ ] **Зависимости** — `npm audit`, обновить уязвимые (уже был апдейт next; поддерживать).

---

# ЧАСТЬ V — UI / ОПИСАНИЯ / КАЧЕСТВО КОДА

## 11. UI / ОПИСАНИЯ (все странички до идеала) — база в PLAN-PRODUCTION §3
### 11.0 Правило
- Для КАЖДОЙ вкладки: (A) технически всё работает и согласовано; (B) единый дизайн, состояния
  загрузка/пусто/ошибка, адаптив, тёмная тема; (C) тексты по-человечески, без техножаргона.
### 11.1 По вкладкам (пройти каждую — детали в PLAN-PRODUCTION.md §3)
- [ ] **Кампании/главная** (`triggers/page.tsx`): конструктор — round-trip КАЖДОГО поля (draft↔trigger),
  шаблоны, иконки типов, счётчики «обнаружено/выполнено». Задержки показывать только где сохраняются (у DM).
- [ ] **Аккаунты** (`accounts/page.tsx`): все исходы входа с человеческой причиной+скрином, «Войти заново»,
  статус «Требует входа», авто-приём, метрика подписчиков со временем обновления, кулдаун входа (§1.5).
- [ ] **Статистика** (`stats/page.tsx`): 3D-диаграмма, матрица, FleetSafety; humanize журнала (сделано);
  «Здоровье автоматизации» честные названия; расход 2captcha/прокси.
- [ ] **Прокси** (`proxy/page.tsx`): проверка/подбор/ротация/вердикты, воркер-сбой не красит живой прокси;
  подсказки «какие прокси брать» (§9.4).
- [ ] **Настройки** (`settings/page.tsx`): интервал, лимиты, «Проверить сейчас», справка без легаси.
- [ ] **Общие** (`TopNav`/`LogModal`/`ActivityBell`): статус воркера, Live-журнал, колокольчик, фильтры.
### 11.2 Тексты/описания
- [ ] Везде человеческий язык (`lib/logText.ts humanizeLog` — расширять карту жаргон→текст).
- [ ] Онбординг актуален (без черновых/API), ведёт до первого срабатывания.
- [ ] Убрать debug-инструменты за флаг `NEXT_PUBLIC_DEBUG_TOOLS` (🔔-проба сделано; 🧪/🔤 — проверить/скрыть).
- [ ] Убрать временную telegram-ссылку из `layout.tsx` перед релизом (если ещё стоит).

## 12. КАЧЕСТВО КОДА (senior-уровень, чисто)
- [ ] **Мёртвый код** — вычистить или явно пометить (`TriggerCard` не рендерится; legacy no-op поля;
  `_KeepDraftsInBundle`; черновые/HikerAPI-задел). Решить: удалять или оставлять как фолбэк — задокументировать.
- [ ] **Дубли** — вынести общие функции (уже: `lib/match.ts`, `lib/targets.ts` вынесены из poll). Пройтись.
- [ ] **Типы** — `tsc --noEmit` чист (кроме паузной auth — её либо доделать, либо удалить, чтобы tsc был
  чист без исключений; сейчас требует изолированный worktree для проверки).
- [ ] **Тесты чистой логики** — расширять (`lib/*` + воркер `newsparse`/`proxy`): добавить на новые функции
  (grid-parse, geo-from-proxy, multi-campaign dedup). Держать зелёными.
- [ ] **Комментарии = ПОЧЕМУ.** Убрать устаревшие/противоречивые.
- [ ] **Единый стиль** (форматирование, именование) — при правках приводить к окружающему коду.
- [ ] **CRLF/LF** — репо на Windows даёт CRLF-варнинги; не критично, но настроить `.gitattributes` при желании.

---

# ЧАСТЬ VI — ТЕСТИРОВАНИЕ (живое, масштабное) = GO-LIVE GATE

## 13. ЖИВЫЕ МАСШТАБНЫЕ ТЕСТЫ
> ⚠️ ДИСЦИПЛИНА: беречь аккаунты (§1.5). Использовать ОТДОХНУВШИЕ аккаунты + чистые мобильные прокси в гео.
> НЕ гонять один аккаунт подряд. Каждый T-кейс — с трассой+скрином в результат.
### 13.1 Вход (матрица PLAN-LOGIN §15, T1–T14)
- [ ] T1 чистый вход без капчи (мобильный прокси в гео) → success.
- [ ] T2 вход с reCAPTCHA → grid-solving берёт → success.
- [ ] T3 reCAPTCHA + повторные раунды → берём.
- [ ] T4 2FA с ключом; T5 2FA без ключа→почта; T6 challenge email; T7 challenge (IG шлёт SMS)→форсим email;
  T8 device-approval; T9 куки; T10 куки→пароль фолбэк; T11 гео-несовпадение→понятный `session_rejected`;
  T12 массовый импорт; T13 suspended; T14 image-капча.
### 13.2 Функционал (связки триггер→действие) — §4.1
- [ ] Каждая связка на канарейке доведена до конца (действие ПОДТВЕРЖДЕНО, `fireCount` на success, stats N/N).
- [ ] Порядок follow→like→comment→story→dm подтверждён по журналу.
- [ ] 10 кампаний на аккаунте (§4.2) — сценарий прогнан, поведение предсказуемо.
### 13.3 🔴 ВЫСОКИЕ ОБОРОТЫ (100+ подписчиков/час)
- [ ] Симуляция уже есть (`tests/highvolume.sim.test.ts`) — все обрабатываются за дни, ≤лимит/сут, никто не
  потерян, без дублей. **Живой прогон:** реально нагнать поток на канарейку → бот приветствует в пределах
  лимитов, растягивает, НЕ теряет, НЕ банится. Проверить окно детекта (§2.1).
### 13.4 Не баняться (выживаемость)
- [ ] Канарейка неделя на нормальных оборотах → 0 банов. Разные прокси (§9.4).
### 13.5 Матрица прокси (§9.4)
- [ ] 3–5 провайдеров прогнать: капча/бан/скорость/цена → выбрать оптимум, записать сюда.

## 14. ДОПОЛНИТЕЛЬНЫЕ ПУНКТЫ (добавлено агентом — что ещё доработать/перепроверить)
> Пользователь просил добавить от себя. Ниже — то, что реально всплывёт при доведении до профессионала.
- [ ] **Реальный мониторинг/алерты:** нет воркера / массовая недоставка / бан аккаунта / пустой баланс
  2captcha/HikerAPI → алерт владельцу (есть `ALERT_WEBHOOK_URL`-задел; довести: email/telegram/in-app).
- [ ] **Наблюдаемость системы:** дашборд здоровья (воркеры, очереди, RAM, длина очереди DM, кол-во активных
  контекстов) — для оператора при 20+ юзерах.
- [ ] **Идемпотентность действий:** гарантировать, что редеплой/краш посреди цикла не задваивает директы
  (снапшоты + пометка «после подтверждения»). Сейчас частично; довести персистентный per-действие дедуп.
- [ ] **Восстановление после краха воркера:** контексты/сессии не теряются, автоперезапуск, докачивание
  незавершённых джобов.
- [ ] **Логика «протухшей сессии» единая** на ВСЕХ путях (детект/приём/прогрев/визит/стори) → один статус
  «Требует входа» + кулдаун (не спамить WARN каждый цикл).
- [ ] **Биллинг/планы** (`User.plan` есть задел): лимиты по тарифу (кол-во аккаунтов/действий), если продукт
  платный для 20+ юзеров.
- [ ] **Резервное копирование БД** (Railway) + миграции только аддитивные/безопасные (правило соблюдается).
- [ ] **Грациозная деградация без Redis** (авто-поллинг на setInterval уже есть; DM-очередь при отсутствии
  Redis — понятное поведение, не молчаливая потеря).
- [ ] **i18n-готовность** (если целевая аудитория — не только рус): вынести строки. НЕ приоритет, но заложить.
- [ ] **Юнит-тесты на новые чистые функции** (grid-parse инструкции/сетки, geo-from-proxy-string,
  multi-campaign выбор кампании, кулдаун входа) — без сети, сразу.
- [ ] **Документация для оператора** (как добавить воркер, как читать трассу капчи, что делать при банах) —
  короткий `OPERATIONS.md`.
- [ ] **Чистка untracked-мусора** в репо (`public/test.html`, `structure.txt`, `public/3D stats/` дубль,
  `.mcp.json` если не нужен) — решить, что коммитить, что в `.gitignore`.
- [ ] **Проверить `parseSellerLine` edge-case** (пароль из 16+ base32-символов ошибочно принимается за 2FA-ключ)
  — редко, но реальный латентный баг импорта/вставки.

## 15. КРИТЕРИИ «ГОТОВО К РЫНКУ» (go-live gate — финальная подпись)
- [ ] **§1 Вход:** капча берётся (grid-solving) ИЛИ не выпадает (мобильный гео-прокси); аккаунты не горят;
  все P1–P9 честны; куки — просто.
- [ ] **§2–5 Функционал:** все триггеры×действия подтверждены на живом; LIKE из беты; 10 кампаний
  предсказуемы; порядок+паузы; ничего не двоится/не теряется; 100+/час — в пределах лимитов без бана.
- [ ] **§6–8 Поведение/анти-детект/анти-бан:** неотличим от человека; self-test redCount=0; неделя без бана.
- [ ] **§9 Масштаб:** мульти-воркер шардинг работает; дашборд/списки быстрые; матрица прокси выбрана.
- [ ] **§10 Безопасность:** сессии шифруются at-rest; изоляция тенантов; секреты в env; rate-limit; нет утечек.
- [ ] **§11 UI:** каждая вкладка технически+дизайн; тексты человеческие; debug скрыт; онбординг актуален.
- [ ] **§12 Код:** senior-чистота; tsc/тесты зелёные; мёртвый код убран/помечен.
- [ ] **§13 Тесты:** матрица входа + связок + высокие обороты + выживаемость пройдены на ЖИВОМ.

---

## 16. ЖУРНАЛ ПРОГРЕССА ПО ЭТОМУ ПЛАНУ (агенты дописывают сверху)
- 2026-07-19 — план создан. Приоритеты: §1 вход/капча (grid-solving — ядро), §2–5 функционал. Капча:
  builds 83–94 (отправка токена, ретраи, счётчик, enterprise-детект, createTask+action, PROXY-задача,
  клик чекбокса до идеала, net-лог). ГЛАВНАЯ СТЕНА: Meta режет пересаженный токен по скору → следующий
  шаг §1.1 шаг 2 (in-browser grid-solving для native-токена). ⚠️ аккаунты пользователя залочены от
  перебора — перед тестами дать отдохнуть, использовать мобильный прокси в гео.

---

# ЧАСТЬ VII — ПРИЛОЖЕНИЯ (детальные спеки для реализации)

## A. ДЕТАЛЬНАЯ СПЕКА: IN-BROWSER GRID-SOLVING reCAPTCHA (§1.1 шаг 2 — ядро решения входа)

> Это КОНКРЕТНЫЙ план реализации самого важного пункта. Цель — получить РОДНОЙ токен (решён в нашей
> сессии) вместо пересаженного, чтобы Meta приняла по скору. Всё через видимый DOM/мышь (ADR-002 ок:
> bframe — свой google-origin, это не Instagram API).

### A.1 Анатомия reCAPTCHA v2 checkbox (что где)
- top: `instagram.com/auth_platform/recaptcha/` → iframe `fbsbx.com/captcha/recaptcha/iframe/`.
- внутри fbsbx: `.g-recaptcha[data-sitekey][data-callback=successCallback][data-action=ig_login_recaptcha]`.
- google-фреймы: `…/recaptcha/(enterprise|api2)/anchor` (чекбокс `#recaptcha-anchor`) и `…/bframe` (челлендж).
- решение: `successCallback(token)` → `parent.postMessage({CAPTCHA_SOLVED, token})`. Верхний фрейм отправляет
  токен на бэкенд Meta (assessment). Наш `net:`-лог ловит этот запрос.

### A.2 Алгоритм (псевдокод) — `solveGridInBrowser(page)`
```
1. clickRecaptchaCheckbox(page)            // build-94: human-мышь, aria-checked, детект bframe
2. await settle(1.5s)
3. if aria-checked == 'true' or captchaCleared(): return {ok:true, native:true}   // тихо прошло
4. bframe = frame matching /recaptcha.*bframe/
   if !bframe: return {ok:false, reason:'no-challenge-no-pass'}                    // клик не открыл челлендж
5. for round in 1..MAX_ROUNDS(=4):
     a. instruction = text of bframe .rc-imageselect-desc(-no-canonical)          // «Select all images with …»
     b. gridType   = 3x3 (dynamic) | 4x4 (one-time) — по числу .rc-imageselect-tile
     c. shot = screenshot of bframe challenge area (.rc-imageselect-challenge)
     d. tiles = await twocaptchaGrid({ image: shot(base64), textinstructions: instruction,
                                        recaptcha: true, rows, cols })            // координатный/грид метод
     e. for each tile index in tiles: humanClick(page, bframe tile[index])        // клик по плиткам мышью
     f. if dynamic(3x3): wait for new tiles fade-in, re-screenshot changed tiles, repeat within round
     g. humanClick(page, bframe .rc-button-default[Verify/Подтвердить])
     h. await settle
     i. if captchaCleared() or aria-checked=='true': return {ok:true, native:true}   // reCAPTCHA сама дёрнет successCallback
     j. if bframe shows «try again»/«select all»: continue (следующий раунд)
6. return {ok:false, reason:'grid-rounds-exhausted', diag: screenshots}
```

### A.3 2captcha координатный контракт (уточнить на 2captcha.com/api-docs — на момент плана)
- Legacy `in.php`: `method=base64` + `coordinatescaptcha=1` + `textinstructions=<инструкция>` + `img=base64`.
  Ответ `res.php` → координаты кликов `coordinates:x=..,y=..`. ЛИБО grid — вернёт номера плиток.
- New `createTask`: тип `CoordinatesTask`/`GridTask` (проверить точное имя) с `body`(base64), `comment`(инструкция),
  `rows`/`columns`. Решение — массив кликов/индексов.
- ⚠️ Инструкция важна (на языке страницы — тут английский `Select all images with …`). Динамические 3×3
  требуют дослать обновлённые плитки — либо решать каждый обновлённый тайл, либо перескринивать всю сетку.

### A.4 Диагностика (обязательна — вслепую не отладить)
- В трассу на каждый раунд: `грид: раунд N, инструкция «…», сетка 3x3/4x4, плитки [..], verify → результат`.
- Скрин bframe каждого раунда в `diag` (как уже делаем для формы/капчи).
- netLog verify-запроса покажет, приняла ли Meta финальный токен.

### A.5 Риски и митигации
- Хрупкий DOM bframe → селекторы `.rc-imageselect-*` могут меняться; держать фолбэк по aria/роли + скрин.
- Многораундовость/динамика → лимит раундов (4), таймаут на раунд, при исчерпании — честный `✗ + diag`.
- reCAPTCHA может НЕ дать решаемый челлендж недоверенному браузеру (зациклить) → тогда стена принципиальна,
  вывод в трассу «reCAPTCHA зациклила челлендж — нужен доверенный IP/куки».
- Стоимость: грид-солв дороже токена; следить за балансом 2captcha.

### A.6 Порядок фолбэков финально (в `trySolveCaptcha`)
```
1. клик чекбокса → тихо прошло? → ГОТОВО (native, лучший)
2. выпал челлендж → solveGridInBrowser → native token → ГОТОВО (целевой путь)
3. grid не смог → пересадка токена (proxy-задача createTask) → чаще режется, но пробуем
4. всё ✗ → честная ошибка + полная трасса (клик/грид/net/frames) → вывод: доверенный IP/куки
```

## B. ДЕТАЛЬНАЯ СПЕКА: МНОГО КАМПАНИЙ НА ОДНОМ АККАУНТЕ (§4.2 — «10 кампаний, что будет?»)

> Пользователь просил продумать ГЛУБОКО. Ниже — целевая модель разрешения конфликтов.

### B.1 Классы конфликтов
1. **Несколько кампаний ОДНОГО типа события** (3× «Новая подписка») с разными действиями/текстами:
   один новый подписчик подходит под все три.
2. **Кампании РАЗНЫХ типов** на одного человека (он и подписался, и прокомментировал): NEW_FOLLOWER-кампания
   и NEW_COMMENT-кампания сработают обе.
3. **Однотипные действия из разных кампаний** (две кампании обе хотят «лайк») → риск двойного лайка.

### B.2 Целевые правила (реализовать + задокументировать)
- **Директ:** максимум 1 за цикл на человека (есть `dmedThisCycle`). ПРИ КОНФЛИКТЕ выбираем кампанию-победителя
  ДЕТЕРМИНИРОВАННО: приоритет = (a) явный `priority` у кампании, если введём поле; иначе (b) самая ранняя по
  `createdAt` активная кампания подходящего типа. Задокументировать выбранное правило в UI («при пересечении
  кампаний директ шлёт первая созданная»).
- **Подписка/сторис:** идемпотентны (уже подписан / посмотрели) → выполнять один раз, дубли не слать.
- **Лайк:** дедуп по посту — не лайкать один пост дважды из разных кампаний за цикл.
- **Ответ в комментах:** на КАЖДЫЙ новый коммент отвечаем один раз (ключ по комменту), приветствие (директ/
  подписка) — один раз на человека (`alreadyGreeted`) — уже так; проверить при многих кампаниях.
- **Бюджет/лимиты:** считаются на АККАУНТ (не на кампанию) — 10 кампаний НЕ умножают дневной лимит. Подтвердить.
- **Дрип 2–4 цели/цикл** — на аккаунт, не на кампанию.
- **fireCount/stats** — инкремент у ТОЙ кампании, что реально выполнила действие (при выборе победителя —
  у победителя). Матрица выполнений (Статистика) должна это отражать без задвоений.

### B.3 Возможная доработка модели (если правил недостаточно)
- [ ] Ввести поле `TriggerRule.priority` (int) — пользователь задаёт порядок при пересечении. Миграция аддитивна.
- [ ] В `poll` при сборе действий на цель — группировать по человеку, применять правила B.2 ДО исполнения
  (сейчас потоки идут по типам; нужна агрегация «на человека» через потоки, как `dmedThisCycle`, но для всех
  действий). Аккуратно, горячий путь — с тестом.

### B.4 Тест-сценарий (для §13.2)
- Аккаунт, 10 кампаний: 3×NEW_FOLLOWER (разные директы), 2×NEW_COMMENT (разные ответы), 1×NEW_LIKE,
  1×STORY_MENTION, +3 с лайком/подпиской. Один тест-человек подписывается И комментирует.
- Ожидаем: 1 директ (от кампании-победителя по правилу), 1 подписка-в-ответ (не 3), лайки без дублей,
  ответ на его коммент, лимиты не пробиты, в журнале видно какая кампания что сделала. Задокументировать факт.

## C. АРХИТЕКТУРА И ГЛОССАРИЙ (для будущих агентов — быстро войти в контекст)

### C.1 Поток «событие → действие» end-to-end
```
Крон/heartbeat (instrumentation.ts, setInterval ~30мин) ИЛИ «Проверить сейчас» (UI)
  → POST /api/poll (глобальный лок poll:all)
    → для каждого активного аккаунта (status ACTIVE, есть browserState):
       [per-account browserLock]
       → browserSelfEvents (воркер /self-events) — читаем СВОИ уведомления (детект)
       → раскладываем на selfFollows/selfLikes/selfComments (+ story-inbox отдельно)
       → selectTargets (дрип 2–4, дедуп снапшотами, базлайн)
       → handleTargets: на каждую цель — набор действий из активных кампаний
          → директ → BullMQ dm-очередь (пейсинг 45–115с) ИЛИ inline (ручной/без Redis)
          → follow/like/story/comment → визит воркера (/session/run) или пооперационно
       → сохранить снапшоты ПОСЛЕ обработки (провалившиеся → ретрай), обновить browserState (liveState)
  → dm-воркер (instrumentation.ts, BullMQ) шлёт директы под browserLock, свежий browserState из БД
```
### C.2 Ключевые модели БД (prisma/schema.prisma)
- `User`(plan) · `InstagramAccount`(username, browserState, status, proxy, role, locale/timezoneId, followers,
  limits, deliveryStats, autoAcceptFollowers, createdAt) · `TriggerRule`(triggerType, isActive, conditions,
  actions[], fireCount, stats) · `Snapshot`(type, data) · `Proxy`(url, status, country, датацентр-флаги,
  igBlocked) · `Section`(папки) · `UserSettings`(pollIntervalHours, dailyCaps, allowNoProxy, accountsPerProxy)
  · `Log`(level, message, accountId) · `Template` · `AppLock`(poll:all, browser:<id>).
### C.3 Термины
- **self-events** — чтение аккаунтом СВОИХ уведомлений (детект без черновых/API).
- **browserState** — storageState сессии (куки+хранилище) для входа без пароля.
- **дрип** — брать 2–4 новых цели за цикл, остальные добираются позже (анти-спам).
- **прогрев (warmup)** — рампа лимитов по возрасту + keep-alive заходы (сессия не стынет).
- **гейт** — проверка «подписан ли на нас» перед действием (followed_by/mutual).
- **капча-трасса** — строка `🔐 Капча:` с полным ходом решения (детект/клик/solve/inject/verify/net/frames).

### C.4 Как читать капча-трассу (для отладки входа)
- `распознана recaptcha ENTERPRISE … data-s … data-attrs …` — что за капча и параметры.
- `клик чекбокса (метод, aria-checked=…, картинка-челлендж=ДА/нет)` — попал ли клик, выпала ли картинка.
- `solve via createTask ENTERPRISE (PROXY-задача…)` — как решали токен.
- `вписан [textarea/getResponse/data-callback/cfg-callback/form-submit/postMessage]` — как отправляли.
- `verify: капча УШЛА ✓ / экран НЕ сменился ✗` — приняла ли Meta.
- `net: [400 …userverify…]` — серверный ответ verify (причина отказа).
- `frames: {…}` — структура/хендлеры fbsbx при неудаче.

## D. ПРАВИЛА КОММИТОВ/ДЕПЛОЯ (напоминание)
- Коммит точечный, сообщение по-русски + `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- Ветка master (Railway авто-деплой). Воркер и Next.js — отдельные сервисы, оба на master.
- Перед пушем: изолированный `git worktree` от HEAD для tsc (в рабочем дереве — паузная auth, даёт ложные
  ошибки). Воркер: `node --check` + `npm test` + `/health` build после деплоя.
- НЕ коммитить паузную auth-работу (`app/api/auth/{google,register}/*`) без полей схемы + миграции.

> Конец плана. Держать актуальным: помечать `[x]`, дописывать журнал §16, не ломать рабочее.

