# plan4.md — Источник событий «Лента уведомлений основного аккаунта» (self-events)

> Отдельный план по идее пользователя (2026-07-11): вместо того чтобы ЧЕРНОВОЙ парсил
> подписчиков/лайки/комменты ОСНОВНОГО, **основной сам читает СВОИ уведомления** (лента
> активности Instagram — «Сповіщення»: «X почав стежити за вами», «X вподобав ваш допис»,
> «X прокоментував…»). Это закрывает ВСЕ триггеры без черновых и без HikerAPI.
>
> Статус: **план/анализ.** Не реализовано. Реализовывать по разделу «Как строить» после
> живого подтверждения (у пользователя появился рабочий резидентный прокси — можно проверить).

---

## 0. TL;DR (кратко)

- **Что:** основной аккаунт своей же залогиненной сессией читает `GET /api/v1/news/inbox/`
  (тот самый приватный web-API, что уже используется в `parse.js`/`readStoryEvents`) — там
  Instagram сам отдаёт СОБЫТИЯ: новые подписчики, лайки твоих постов, комментарии, ответы/
  упоминания в сторис.
- **Зачем:** это ровно те 4 триггера, что у нас есть (NEW_FOLLOWER / NEW_LIKE / NEW_COMMENT /
  STORY_MENTION). Значит **черновые и HikerAPI для СВОИХ событий не нужны вообще.**
- **Плюс безопасности:** чтение аккаунтом СВОИХ уведомлений — самое естественное действие
  (так делает любой живой юзер). Это БЕЗОПАСНЕЕ, чем черновой, лазящий по чужим спискам
  подписчиков (скрейп-паттерн). Ban-риск на ЧТЕНИИ ≈ 0.
- **Главный подвох:** лента агрегирует и пагинируется — при всплеске между проверками часть
  событий уходит за окно; лайки могут склеиваться («X и ещё N»); для NEW_COMMENT нужен ТЕКСТ
  коммента (для матчинга фраз) — надо проверить, что он есть в payload инбокса, иначе добор.
- **Решение по умолчанию:** сделать `self` НОВЫМ основным источником, а `drafts`/`api` —
  фолбэком (цепочка `self → drafts → api`). Это и есть просьба пользователя: «всё через
  основные, а при неудаче/отсутствии черновых — добор».

---

## 1. Как это ложится на наши триггеры

| Триггер | Что нужно | Даёт ли лента уведомлений |
|---|---|---|
| **NEW_FOLLOWER** (новая подписка) | username нового подписчика | ✅ да, отдельным событием «начал(а) читать вас» |
| **NEW_LIKE** (лайк) | кто лайкнул твой пост | ✅ да, но лайки часто АГРЕГИРУЮТСЯ («X и ещё N») |
| **NEW_COMMENT** (комментарий) | username + **текст** коммента (для матчинга фраз) | ⚠️ username да; текст — надо проверить на живом (см. §5) |
| **STORY_MENTION** (ответ/упоминание в сторис) | ответы на сторис/упоминания | ✅ уже читаем отдельно (`/story-inbox`, `readStoryEvents`) — лента уведомлений это дублирует/дополняет |

Вывод: **все 4 триггера — это «свои события» основного аккаунта.** Черновые/HikerAPI
изначально парсили СВОИ же данные основного (его подписчиков/лайкеров/комментаторов) — просто
чужой сессией. Лента уведомлений даёт то же самое родной сессией и «из коробки».

---

## 2. Откуда берём (технически)

Тот же приём, что уже обкатан в `workers/browser/lib/parse.js` (`apiGet`) и
`readStoryEvents`: **fetch приватного web-API ИЗНУТРИ залогиненной страницы** (наследует
cookies + `x-ig-app-id`, same-origin):

```js
// Внутри page.evaluate на origin https://www.instagram.com/ (сессия основного):
GET /api/v1/news/inbox/        // лента активности: подписки/лайки/комменты
// (там структура { new_stories:[...], old_stories:[...], ... }, у каждой story — type + args)
```

- **Почему это надёжнее DOM-скрейпа** (скринов «Сповіщення»): панель уведомлений
  локализована (укр./id/pt — зависит от locale отпечатка!), виртуализована и часто меняет
  вёрстку. JSON-инбокс отдаёт **структурные коды типов и user-объекты**, не переведённый
  текст → парсер не зависит от языка аккаунта. Это критично: у нас отпечаток ставит locale
  по стране прокси (id_ID/pt_BR/…), т.е. DOM был бы на разных языках.
- **Где живёт:** новый эндпоинт воркера, напр. `POST /self-events` (по аналогии со
  `/story-inbox`), принимает `{storageState, proxy, username, locale, timezoneId}` +
  опц. `types` (какие события нужны) → возвращает нормализованный список
  `[{type:'follow'|'like'|'comment'|'story', pk, username, text?, media_id?, ts}]`.
- **Дедуп/база:** ровно как сейчас — снапшоты по типу (`FOLLOWERS`/`LIKES`/`COMMENTS`/`STORY`)
  + `selectTargets` (первый проход = базлайн, реагируем только на новые). Можно добавить
  курсор/`max_id` инбокса, чтобы добирать за пределами видимого окна.

---

## 3. Плюсы (почему это шаг вперёд)

1. **Черновые больше не нужны для своих событий.** Уходит целый класс сложности: покупка/
   вход/прогрев/прокси/ban-риск черновых аккаунтов, их health-мониторинг, ротация, `draftPool`.
2. **HikerAPI не нужен** для своих событий → $0 за парсинг (сейчас платно за запрос).
3. **Ниже общий ban-риск.** Чтение своих уведомлений = поведение живого владельца. В отличие
   от чернового, который ходит по ЧУЖИМ профилям и листает их списки подписчиков (это
   скрейп-сигнал). Меньше аккаунтов в системе = меньше поверхность бана.
4. **Решает `parseBlocked`** (запись 2026-07-11): verified/приватные аккаунты СКРЫВАЮТ список
   подписчиков от третьих сторон → черновой видел пусто. Но **свои-то уведомления аккаунт
   видит ВСЕГДА**, независимо от приватности. Т.е. self-events работает там, где черновой
   бессилен.
5. **Все типы в одном месте и «пуш».** Instagram сам складывает подписки+лайки+комменты в одну
   ленту по мере поступления — не надо отдельно тянуть followers-list, likers каждого поста,
   comments каждого поста (это N запросов). Один `news/inbox` = все свежие события.
6. **Реактивнее и дешевле по запросам.** Вместо диффа полного списка подписчиков (тяжело для
   крупного аккаунта) — читаем только дельту событий.
7. **Не зависит от языка** (JSON-типы, не переведённый текст).

---

## 4. Минусы / подводные камни (честно)

1. **Агрегация лайков.** Лента часто склеивает: «X и ещё 12 лайкнули ваш пост». Тогда получаем
   ТОЛЬКО верхнего актора (или вообще «N человек» без списка). Для NEW_LIKE это значит: часть
   лайкнувших не попадёт под триггер. Черновой/likers-парс достаёт ВСЕХ лайкеров поста.
   → *Митигация:* для NEW_LIKE, если нужна полнота, оставить фолбэк на likers-парс (drafts/API)
   именно для этого триггера; для follow/comment лента полна.
2. **Текст комментария для матчинга фраз.** NEW_COMMENT-триггер умеет фильтровать по фразам
   («Сигнал»). Нужно проверить на живом: несёт ли `news/inbox` полный ТЕКСТ коммента в `args`.
   Если несёт только «прокоментував ваш допис» без текста — придётся ДОБИРАТЬ текст отдельным
   запросом (`media/{id}/comments`) по media_id из события. Это +1 запрос на новый коммент
   (терпимо), но усложняет. *Пока не подтверждено — узнаём на живом.*
3. **Окно/пагинация.** Инбокс отдаёт последние N событий. Если между проверками (авто = 30 мин)
   пришёл всплеск > окна, «самые старые» из всплеска уйдут за границу до того, как мы их
   увидим. → *Митигация:* курсор/`max_id` для добора + разумная частота. При наших
   консервативных дневных лимитах (dm 10/сут) всё равно обрабатываем не больше `MAX_NEW_PER_POLL`,
   так что гнаться за 100% полнотой ленты смысла нет — но нельзя терять события молча
   (логировать «в ленте было > окна, добираю по курсору»).
4. **Чтение помечает уведомления просмотренными** (гасит красную точку). Косметика, но если
   владелец сам заходит в аккаунт — он потеряет «непрочитанность». Обычно не важно (это боты),
   но упомянуть в UI.
5. **Стабильность приватного эндпоинта.** `news/inbox` — приватный web-API, Instagram может
   менять формат/коды типов. Тот же риск, что уже принят для `parse.js`/`readStoryEvents`.
   → *Митигация:* дефенсивный парсинг (любой сбой → пустой список + фолбэк на drafts/api,
   НИКОГДА не роняем цикл), фиксация набора `type`-кодов с запасом и логированием незнакомых.
6. **Коды типов событий** различаются и не документированы официально. Нужно снять их на живом
   (какой `type`/`story_type` = follow / like / comment / mention) и захардкодить карту с
   дефенсивным «неизвестный тип → пропустить + debug-лог».
7. **Не даёт «полный список/счётчик подписчиков».** Лента — это события, а не список. Метрика
   «Подписчики» (число) по-прежнему берётся из `web_profile_info` (уже есть, отдельно). Это ок.
8. **«follow request» у приватных аккаунтов.** Если основной сам приватный — новый подписчик
   сперва «запросил подписку». Событие в ленте может отличаться (request vs follow) — учесть.

---

## 5. Ban-риск для основных аккаунтов (главный вопрос пользователя)

Разделяем ДВЕ вещи — их часто путают:

### 5.1 Риск ЧТЕНИЯ событий (детект)
- Чтение СВОЕЙ ленты уведомлений — **самое естественное действие**, нулевой инкрементальный
  риск. Живой юзер открывает «колокольчик» по много раз в день.
- Это **безопаснее текущего чернового**, который заходит на ЧУЖИЕ профили и листает их
  подписчиков/лайкеров (сторонний скрейп — потенциальный сигнал).
- Частота: даже каждые 30 мин (наш авто-цикл) — норма. Можно и чаще без риска. Один запрос
  `news/inbox` дешевле и незаметнее, чем серия `friendships/followers`.

### 5.2 Риск ДЕЙСТВИЙ (DM/подписка/лайк/сторис) — НЕ меняется
- **Источник детекта НЕ влияет на риск действий.** DM/follow/like шлёт основной своей сессией
  — точно так же, как сейчас. Все существующие защиты остаются и обязательны:
  - Дневные лимиты (`DAILY_CAPS`: dm 10 / follow 12 / like 30 / comment 8 / story 40) —
    консервативные, ужимаются прогревом по возрасту (`warmupFactor`).
  - Суточный ритм/сон (`activityWindow`) — ночью тишина.
  - Паузы между действиями и между целями (§ [A2]/визиты), человекоподобный ввод.
  - Подтверждение доставки + автоснижение темпа при массовой недоставке (§4.6).
  - Межпроцессный лок сессии (§4.8), single-flight браузера (§6.1).
- **Важный нюанс роста нагрузки:** если раньше черновой «сглаживал» и часть трафика шла через
  него, то теперь ВСЁ (и детект, и действия) на основном. Детект дешёвый — не проблема. Но
  надо следить, чтобы **число новых событий за цикл не провоцировало залп действий**:
  `MAX_NEW_PER_POLL` + дневные лимиты + пейсинг уже это держат. Правило неизменно:
  **выживание ≫ скорость.**

### 5.3 Итоговая оценка
Переход на self-events **снижает** совокупный ban-риск (меньше аккаунтов, безопасный детект,
уходит скрейп чужих профилей) при неизменном риске действий (он и так основной и уже защищён).
Единственное «но» — не потерять полноту (лайки/окно) — закрывается фолбэком.

---

## 6. Целевая архитектура: цепочка источников с фолбэком

Расширяем `UserSettings.parsingSource`. Сейчас: `api` | `drafts` | `drafts_then_api`.
Добавляем `self` и делаем возможными комбинации-цепочки:

- **`self`** (НОВЫЙ ДЕФОЛТ) — основной читает свою ленту уведомлений. Ничего лишнего не нужно.
- **`self_then_drafts`** — self основной; если лента вернула пусто/сбой ИЛИ нужен полный список
  лайкеров — добор черновым.
- **`self_then_api`** — self основной; фолбэк HikerAPI (без черновых).
- (оставляем старые `drafts` / `api` / `drafts_then_api` для обратной совместимости).

**Логика на поллинге (псевдокод), per тип потока:**
```
async function getEventsFor(type, account, source):
  order = resolveChain(source)      // напр. ['self','drafts','api']
  for src of order:
    try:
      ev = await fetchByType(type, account, src)   // self=news/inbox, drafts=parse.js, api=hiker
      if ev.ok and ev.list.length >= 0 and not ev.degraded: return ev.list
      // пусто на первом проходе — это валидный базлайн, НЕ повод для фолбэка
      if ev.ok and isBaseline: return ev.list
    catch: log(WARN, `${src} не смог, пробую следующий`)
  return []   // все источники не смогли → WARN владельцу, цикл не падает
```
Ключевое: **фолбэк срабатывает на ОШИБКУ/деградацию источника, а не на «0 новых»** (0 новых —
нормальный результат, не повод дёргать платный API).

**Что именно фолбэчить:**
- follow/comment/story → self покрывает; фолбэк только на реальный сбой.
- like → если нужна полнота (агрегация лайков), можно ВСЕГДА добирать likers через drafts/api
  для NEW_LIKE-триггеров (настройка «полный список лайкнувших: да/нет»).

---

## 7. Как строить (шаги реализации)

> Порядок — от воркера к Next.js к UI. Каждый шаг проверяем; на живом снимаем реальный формат
> `news/inbox` (коды типов, наличие текста коммента) — это единственное, что нельзя узнать без
> прогона.

1. **Воркер: снять формат.** Дифф-скрипт (dev-only): залогиненной сессией дёрнуть
   `/api/v1/news/inbox/` и вывести сырой JSON — зафиксировать: где follow/like/comment, есть ли
   `media_id`, есть ли текст коммента, как выглядит агрегированный лайк, поля `pk`/`username`/`ts`.
2. **Воркер: эндпоинт `POST /self-events`** (`workers/browser/lib/parse.js` или новый
   `selfevents.js`, по образцу `readStoryEvents`): `apiGet(page,'/api/v1/news/inbox/')` →
   нормализация в `[{type,pk,username,text?,media_id?,ts}]`. Дефенсивно (сбой → `{events:[],error}`).
   Опц. курсор `max_id` для добора. Вернуть `browserState` (сессия дозрела — как везде).
3. **Next.js клиент:** `browserSelfEvents(ctx, {types, cursor?})` в `lib/browser/client.ts`.
4. **Next.js poll:** новая функция-источник `parseSelfEventsFor(account, types)` + встроить в
   цепочку `resolveChain(parsingSource)`. Потоки follower/like/comment/story берут события из
   self, дедуп прежними снапшотами, дальше — ТА ЖЕ логика действий (ничего в действиях не
   меняем). Комментам, если нет текста в инбоксе, добрать текст по `media_id`.
5. **Схема:** значения `parsingSource` — строка, миграции НЕ нужны (поле уже строковое); просто
   расширяем допустимые значения + дефолт `self` для новых пользователей.
6. **UI (Настройки → «Источник парсинга»):** добавить «Свои уведомления (рекомендуется)» +
   «Свои + черновые (фолбэк)» + «Свои + API». Честный текст: что self не нужны черновые/ключ,
   что для полноты лайков можно включить добор.
7. **UI (Черновые):** когда `parsingSource=self`, показывать «черновые не требуются — основной
   читает свои уведомления»; черновые остаются опцией-фолбэком.
8. **Тесты (§10.1):** юнит на нормализатор `news/inbox` (по зафиксированным фикстурам JSON:
   follow/like-агрегат/comment с текстом и без) → корректные `{type,username,text}` + дефенс к
   мусору. Это делается БЕЗ сети (фикстуры), сразу.

---

## 8. Пограничные случаи (не забыть)

- **Базлайн:** первый `self`-проход при пустом снапшоте — только фиксируем известные, 0 действий
  (как сейчас `selectTargets`). Иначе бот разом ответит на всю ленту.
- **Приватный основной:** новый подписчик = сперва «запрос на подписку» (другой тип события) —
  различать follow vs follow-request.
- **Агрегированные лайки:** если событие = «N человек», а нужен список — либо добор likers
  (drafts/api), либо обрабатываем только именованного актора и логируем «остальные N не
  развёрнуты».
- **Гейт подписки (COMMENT/DM):** проверка «подписан ли на нас» (`passesGateFor`) — остаётся,
  выполняется основной сессией (как сейчас).
- **Комментарий без текста в инбоксе** → добор `media/{id}/comments`; если и там нет (удалён) —
  пропуск с WARN.
- **Дубликаты между `news/inbox` и `/story-inbox`** (упоминания в сторис могут прийти в оба) —
  дедуп по общему снапшоту STORY.
- **Ограничение Instagram на аккаунте** — если инбокс отдаёт 4xx/пусто стабильно, это может
  быть сам ban-сигнал: не молотить, логировать, фолбэк.

---

## 9. Что self-events НЕ заменяет (оставляем как есть)

- **Число подписчиков (метрика/спарклайн)** — `web_profile_info` (уже отдельно, раз в день).
- **Действия** — основной браузером (без изменений).
- **Полный список лайкеров конкретного поста** — только drafts/API (если включена полнота).
- **Стори-инбокс** — уже отдельным эндпоинтом; self-events его дополняет, не отменяет.

---

## 10. Рекомендация

**Строить `self` как основной источник + `self_then_drafts`/`self_then_api` как фолбэк-цепочки.**
Это ровно просьба пользователя («всё через основные, при неудаче/отсутствии черновых — добор») и
одновременно снижает ban-риск и стоимость. Черновые/HikerAPI НЕ удалять — оставить фолбэком
(для полноты лайкеров и на случай сбоя инбокса).

**Единственный блокер перед кодом:** снять на ЖИВОМ формат `news/inbox` (шаг 7.1) — коды типов и
есть ли текст коммента. У пользователя теперь есть рабочий прокси → это можно сделать. После
этого реализация прямолинейна и переиспользует всю существующую машинерию (снапшоты, selectTargets,
действия, лимиты, локи).

---

## 11. Связь с текущим багом «действия не работают» (важно)

Идея self-events касается ДЕТЕКТА. Наблюдаемые «0 срабатываний» при найденном подписчике — это
про ДЕТЕКТ→ДЕЙСТВИЕ на текущем (drafts/parse.js) пути и НЕ лечится сменой источника. Сначала
диагностируем по логам аккаунта (базлайн / тихие часы / ошибка действия), чиним, и лишь потом
(или параллельно) внедряем self-events. Иначе смена источника замаскирует настоящую причину.

> **[x] 2026-07-11:** первый шаг сделан — `fireCount` («срабатывание») теперь инкрементится
> ТОЛЬКО при реально выполненном действии (не по попытке), провал → ERROR-лог с причиной.
> Т.е. корневая причина провала действия больше не маскируется; чиним её в Фазе 4 ниже.

---

## 12. ГЛОБАЛЬНЫЙ ПЛАН РЕАЛИЗАЦИИ (по фазам, ТЕСТ после каждой)

> Цель: перейти на self-events как ЕДИНСТВЕННЫЙ рабочий механизм, скрыть черновые+API из
> интерфейса/обучения (код НЕ удаляем — оставляем как аварийный фолбэк), перенести единственный
> черновой аккаунт в основные, довести до идеала детект И выполнение действий.
>
> **Принцип очередности:** НЕЛЬЗЯ отключить черновые/API раньше, чем self-events подтверждён на
> живом — иначе детект остановится вовсе. Поэтому сначала строим self-events рабочим, потом
> прячем/выключаем старое. Черновые/API до финала живут как СКРЫТЫЙ автоматический фолбэк
> (не видны в UI, не выбираются, срабатывают только если self вернул ошибку) — либо жёсткий
> отруб сразу (см. развилку в конце). После КАЖДОЙ фазы — обязательный тест (зелёный → дальше).

### Фаза A — Скрытие черновых/API из интерфейса + миграция аккаунта (без риска для детекта)
Детект пока работает по-старому (скрыто), просто из UI всё про черновые/API исчезает.
- **UI-скрытие (не удаляя код):**
  - `TopNav` — убрать пункт «Черновые аккаунты» / «Парсинг (API)».
  - Настройки (`settings/page.tsx`) — убрать блок «Источник парсинга» и всё про API/черновые/ключ HikerAPI; справку «Что где находится» почистить от них.
  - `/drafts` страница — редирект на `/` (или заглушка «раздел скрыт»); компонент оставить в коде.
  - Бейджи/подсказки кампаний, упоминающие «через API»/«черновые» — убрать/переписать на «свои уведомления».
  - **Онбординг/тур** (`OnboardingTour`, справка) — убрать шаги/тексты про черновые и API.
  - Индикатор источника, `DraftsStatus`/`ParsingStatus` — скрыть.
- **Миграция аккаунта (HELPER→RESPONDER): ПЕРЕНЕСЕНА на ФЛИП (Фаза D), НЕ в Фазу A.** Причина: если у пользователя `parsingSource='drafts'`, черновой — его ЕДИНСТВЕННЫЙ парсер; убрать его роль ДО того как self-events заработает = детект встанет (0 событий). Поэтому черновой остаётся HELPER и продолжает парсить (скрыто) до флипа; на флипе — одновременно `UPDATE role='RESPONDER' WHERE role='HELPER'` + переключение детекта на self.
- **Источник:** дефолт `parsingSource='self'`; неизвестные/старые значения → трактуем как `self` (с фолбэком). Черновые/API-режимы остаются в enum, но в UI не выбираются.
- **ТЕСТ A:** `tsc` чисто; в UI (все вкладки + бургер + онбординг + настройки) НЕТ ни слова про черновые/API; `/drafts` пункта в меню нет, прямой заход редиректит на `/`; детект НЕ упал (parsingSource в БД не тронут — черновой парсит как прежде). **[x] СДЕЛАНО 2026-07-11** (кроме миграции аккаунта — перенесена на флип, см. выше).

### Фаза B — self-events: снять реальный формат `news/inbox` на живом — [x] СДЕЛАНО (JSON получен 2026-07-11)
- **[x] 2026-07-11:** инструмент (🔔 → `/self-events raw`) + получен реальный payload с @aheidy.hec.
  Формат: `old_stories[]`, у истории `story_type`(int) + `notif_name`(строка) + `args`.
  Подтверждено: **follow** = `story_type 101` / `notif_name "user_followed"`, актор в `args.profile_id`/
  `profile_name`/`inline_follow.user_info` (+`friendship_status.following` — follow-back знаем).
  **`comment_like` (story_type 13)** = «лайкнули МОЙ коммент» — НЕ наш триггер, отбрасывается.
  like/comment на МОЙ пост в сэмпле не встретились (у аккаунта нет своих постов) — их `notif_name`
  добавлены по известным вариантам, доуточним когда придут. `notif_name` — язык-независимый гл. сигнал.
- Dev-эндпоинт воркера `POST /debug/news-inbox` (под секретом) ИЛИ временный лог в `/self-events`: залогиненной сессией основного дёрнуть `GET /api/v1/news/inbox/`, вернуть СЫРОЙ JSON.
- Снять: коды типов (follow / like / comment / mention), где `username`/`pk`/`ts`, есть ли `media_id`, есть ли ТЕКСТ коммента, как выглядит агрегированный лайк («X и ещё N»), отличие follow vs follow-request.
- **ТЕСТ B:** получить реальный JSON с @aheidy.hec (или канареечного) через рабочий прокси; сохранить как фикстуры в `tests/fixtures/news-inbox/*.json`. Без этого Фазу C не начинаем.

### Фаза C — self-events: нормализатор + эндпоинт воркера — [x] СДЕЛАНО (коды типов доуточнятся с живого)
- `workers/browser/lib/selfevents.js`: `/api/v1/news/inbox/` изнутри залогиненной страницы → `normalizeNews` → `[{type,pk,username,text?,media_id?,ts,code}]`. Дефенсивно (сбой → `{events:[],error}`). Эндпоинт `POST /self-events`, клиент `browserSelfEvents` — сделаны в Фазе B.
- **[x] 2026-07-11:** чистый разбор вынесен в `workers/browser/lib/newsparse.js` (без Playwright — тестируемо): `classify`/`normalizeNews`/`pickUser` + `NEWS_TYPE_CODES` (карта story_type→тип, заполняется с живого без правки логики). Классификация по СТРУКТУРЕ (inline_follow→follow; media±коммент-признак→like/comment) + тексту (мультиязычно) + кодам (перекрывают эвристику).
- **[x] ТЕСТ C:** `workers/browser/test/selfevents.test.js` (`node --test`) — 9 тестов на РЕАЛЬНОМ формате: follow(user_followed/101), comment_like→отброшен, comment/like поста по notif_name, follow по структуре, код-подстраховка, normalizeNews, мусор, pickUser. Воркер зелёный, `tsc` чист.
- **[x] 2026-07-11 (финализация по живому JSON):** `newsparse.js` переведён на `notif_name` как главный сигнал (`NEWS_NAME_MAP`) + `NEWS_TYPE_CODES` (follow:101, ignore:13); `comment_like`/`like_on_comment` → `null` (не триггер); `classify` возвращает null для не-триггеров. Осталось для 100%: увидеть реальные `notif_name` для like/comment НА ПОСТ (когда у аккаунта появятся посты с активностью) — сейчас по известным вариантам + структурный резерв.

### Фаза D — интеграция self в poll (детект через свои уведомления) — БЕЗ ФОЛБЭКА — [x] СДЕЛАНО (2026-07-11)
- `parseSelfEventsFor(account, types)` в poll — ЕДИНСТВЕННЫЙ источник детекта. Ветки drafts/api из потоков детекта убираются (код в `lib/scraper/hiker.ts`/`parse.js`/`draftPool.ts` остаётся, но `poll` их для детекта не зовёт). На флипе — одновременно миграция `role HELPER→RESPONDER`.
- **[x] 2026-07-11:** `poll/route.ts` — в начале цикла аккаунта под per-account локом (§4.8) один вызов `browserSelfEvents` → события раскладываются на `selfFollows`/`selfLikes`/`selfComments`; потоки подписчиков/лайков/комментов теперь питаются ИМИ (не `parseFollowersFor`/`parseLikersFor`/`parseCommentsFor` — те стали мёртвым кодом). Стори-события по-прежнему через `/story-inbox` (директ-механизм, под тем же локом). Гейт `needsScraper`/`blockedMains` НЕЙТРАЛИЗОВАН (HikerAPI для детекта не нужен; остался лишь опциональной метрикой числа подписчиков, деградирует мягко). Детект теперь требует `canDetect` (лок+сессия): занято → откладываем цикл (события «новые» не теряются). **Миграция `20260711120000_drafts_to_responder`** (HELPER→RESPONDER) — черновой стал основным. Фолбэка НЕТ. `tsc` + `next build` чисто. ⚠️ Нужен передеплой Next.js (миграция применится на старте) + воркер уже `browser-36`. **Живой тест:** новый подписчик из уведомлений → триггер → действие (Фаза E-проверка).
- Потоки NEW_FOLLOWER/NEW_LIKE/NEW_COMMENT/STORY берут события из self; дедуп существующими снапшотами (`FOLLOWERS`/`LIKES`/`COMMENTS`/`STORY`); `selectTargets` (первый проход = базлайн). Комментам без текста — добор `media/{id}/comments`.
- **[x] 2026-07-14 — Метрика «Подписчики» переведена на self-events (HikerAPI УБРАН из счётчика).** По решению пользователя (апишки/черновые не используем): вместо запроса числа подписчиков у HikerAPI (баланс кончился → счётчик «замерзал» + спам-лог «💳 заморожен») счётчик теперь РАСТЁТ из самих уведомлений: каждый НОВЫЙ подписчик, впервые попавший в «известные» в потоке подписчиков (`followersGained`, дельта `knownPks` vs `prevKnownForCount` после снятия провалившихся — учитывается РОВНО раз, устойчиво к дрипу/ретраям; на базлайне не считаем), прибавляется к `account.followers` + точка истории. Оба HikerAPI-блока счётчика удалены из `poll/route.ts`, импорт `scrapeUserInfo`/`scraperConfigured` убран. Гейт «подписан на нас» (`scrapeFollowers`/`scrapeFollowing`) НЕ трогал — он уже деградирует при недоступности HikerAPI и нужен для режима mutual. `tsc` чист.
- **ТЕСТ D (живой):** на @aheidy.hec — реально подписаться тестовым аккаунтом → в течение цикла событие приходит из self (лог «источник: self»), триггер видит нового подписчика, дедуп не пропускает повторно. Проверить, что фолбэк НЕ дёргается при нормальной работе.
- **[x] 2026-07-12 (КРИТ-фикс детекта на живом):** живая проба вскрыла баг — при НЕоткрытии колокольчика DOM-резерв читал ЛЕНТУ+«Suggested for you» и слал ложные follow из рекомендаций (засоряло снапшот → вклад в «новый подписчик не обнаруживается»). Три слоя защиты (build `browser-41/42`): `classifyRow` отбрасывает реко/«Followed by» и требует объект «на вас/you»; гейт `panelOpened` (DOM только если ушёл запрос news/inbox); API-резерв ДО DOM + реплей graphql-запроса из матчнувшего ответа. Подтверждено живой пробой: `panelOpened:true`, `source:intercept`, `events` чистые (0 из ленты). Плюс прогрев ленты ПЕРЕД чтением уведомлений (`preActionBrowse`). Тесты воркера 26/26.

### Фаза E — действия ДО ИДЕАЛА (то, что сейчас «сработал, но 0 выполнено») — В РАБОТЕ
- По ERROR-логам (из фикса fireCount) найти корневую причину провала действий и починить: визит `/session/run` / `sendDM` (сессия `login_required` → пометить повторный вход; прокси; закрытая личка → fallback follow+like; таймаут воркера; селекторы). Подтверждение доставки §4.6 не должно давать ложных «не доставлено».
- **[x] 2026-07-12 — меж-потоковый дедуп директа:** один человек в нескольких потоках (подписка+лайк+коммент) слал до 3 директов за цикл (спам-сигнал). Введён `dmedThisCycle` (по username) в `poll/route.ts` — не более 1 директа на человека за цикл (проверка ДО `use('dm')`; первый поток выигрывает). Лайк/подписка/сторис/ответ не дедупятся (идемпотентны). `tsc` чист.
- **[x] 2026-07-12 — ручная проверка выполняет действие INLINE + диагностика:** пользователь: подписчик найден (3), но «0 срабатываний, 0 ошибок». Корень: при заданном `dmQueue` (Redis) ДАЖЕ ручная «Проверить подписчиков» ставила действие в ОТЛОЖЕННУЮ очередь (`delay` 8–22с) → выполнение зависело от фонового dm-воркера (`instrumentation.ts`); если он в проде не крутит очередь — джоб молча копится, `fireCount` не растёт. Фикс (`poll/route.ts`): `if (dmQueue && !isManual)` — ручной прогон идёт `runFollowerActionsInline` СРАЗУ (реальный визит `/session/run`, `fireCount`/SUCCESS/ERROR прямо в ответе), пауза между целями для manual 4–12с (была 40–90). Авто-поллинг — по-прежнему очередь. Плюс ДИАГНОСТИКА (только `isManual`, чтобы не спамить авто-логи): INFO «Уведомления прочитаны: всего N (подписки/лайки/комменты)» (видно, прочитал ли self-events сам follow) + «Первый проход (базлайн)/Новых подписок N» (видно базлайн vs новые). Теперь по логу аккаунта сразу ясно, ГДЕ обрыв: 0 в уведомлениях = проблема чтения self-events; базлайн = нажать «Сбросить»; ERROR = причина провала действия. `tsc` чист.
- **⬜ ОСТАЛОСЬ (нужен ЖИВОЙ прогон кампании с прокси):** по новым INFO/ERROR-логам ручного прогона добить конкретные провалы (селекторы/таймауты/закрытая личка/чтение уведомлений).
- **ТЕСТ E (канареечный аккаунт, осторожно, с прокси):** new follower → DM РЕАЛЬНО доставлен (подтверждение в треде), и/или follow/like/story реально выполнены; в UI `fireCount` растёт ТОЛЬКО на успехе, badge «выполнено» ≥1; прогон каждого триггер→действие end-to-end (§10.4).

### Фаза F — полнота лайков (опционально, по желанию)
- Настройка «полный список лайкнувших» (для NEW_LIKE): если включена — добор likers конкретного поста через скрытый фолбэк (self даёт агрегат/топ-актора).
- **ТЕСТ F:** пост с N лайками → обработаны все N (или задокументированный лимит), без задвоений.
- **[x] 2026-07-12 — NEW_LIKE ПОМЕЧЕН БЕТОЙ («СКОРО») + НА ПАУЗЕ** (по просьбе пользователя, до готовности Фазы F): (1) `triggers/page.tsx` `TRIG_META` LIKE `soon:true` → в конструкторе кампании тип «Лайк» показывается неактивным с бейджем «СКОРО», выбрать нельзя; (2) `api/triggers` `SUPPORTED` без `NEW_LIKE` → создать новую NEW_LIKE-кампанию API отклоняет; (3) `poll/route.ts` `LIKE_TRIGGER_BETA=true` → `likeTriggers=[]`, поток лайков НЕ выполняется даже для существующих активных NEW_LIKE-кампаний (пауза на уровне исполнения). Причина (уточнено): self-уведомления о лайках **агрегируются** Instagram («X и ещё N лайкнули» → топ-актор + счётчик, НЕ полный список лайкнувших) → триггер неполон до добора likers конкретного поста (Фаза F). `media_id` у лайк-событий как раз ЕСТЬ (`newsparse.js classify` берёт `a.media[0].id`); проблема именно в неполноте списка. **Комментарии (NEW_COMMENT) НЕ затронуты** — их события несут `media_id` (числовой pk из `a.media` или shortcode из DOM), `mediaPostUrl(media_id)` строит postUrl для ответа → триггер комментов рабочий, бета не нужна. Снять бету лайков = вернуть `soon:false` + `NEW_LIKE` в `SUPPORTED` + `LIKE_TRIGGER_BETA=false`. `tsc` чист.

### Фаза G — БЕЗ ФОЛБЭКА С САМОГО НАЧАЛА (решение 2026-07-11)
- Пользователь: «отказ пусть будет с самого начала, полностью переходим на новую систему, а старая пусть ожидает и не используется». → **Фолбэка на черновые/API НЕТ вообще.** На флипе (Фаза D) детект = ТОЛЬКО self-events; ветки drafts/api в детекте `poll` не вызываются (код черновых/HikerAPI остаётся в репо мёртвым грузом-заделом, нигде не исполняется).
- Если self-events вернул ошибку/пусто — НЕ идём в старое: логируем WARN владельцу «self-events недоступен» и ждём следующего цикла (события останутся «новыми», добьются). Это цена отказа от фолбэка — принята пользователем.
- **ТЕСТ G:** grep по коду — ни один путь детекта не зовёт drafts/hiker; весь детект на self; в UI/логах старое не фигурирует.
- **[x] 2026-07-12 — ТЕСТ G ВЫПОЛНЕН (статически):** grep `poll/route.ts` подтвердил: ДЕТЕКТ событий = только `browserSelfEvents` (self); `parseFollowersFor`/`parseCommentsFor`/`parseLikersFor` (черновые/hiker) — ОПРЕДЕЛЕНЫ, но НИГДЕ НЕ ВЫЗЫВАЮТСЯ (мёртвый задел, как и решено). HikerAPI остаётся ТОЛЬКО как мягкое, try/caught-дополнение вне детекта: (а) `scrapeUserInfo` — метрика числа подписчиков (деградирует к 0/прошлому); (б) `scrapeFollowers`/`scrapeFollowing` в ГЕЙТЕ подписки (`ensureGateFollowers`/`ensureGateFollowing`) — обёрнуты в try/catch, при отсутствии ключа падают на снапшот («используем что есть, гейт ошибётся в безопасную сторону»); гейт для потока ПОДПИСЧИКОВ вообще не используется (`withGate=false` — новый подписчик и так на нас подписан). Итог: инвариант «детект без фолбэка, self — единственный» соблюдён; отсутствие HikerAPI-ключа детект не ломает. `tsc`+`next build` чисто.

### РЕШЕНО (2026-07-11): вариант «жёсткий» — self единственный путь, БЕЗ фолбэка
Пользователь выбрал жёсткий отруб: в финале черновые/API НЕ используются нигде (код в репо
остаётся). Чтобы не было простоя детекта на время стройки, порядок исполнения:
**Фаза A (UI-скрытие + миграция аккаунта) — сейчас, детект НЕ трогаем (пока идёт по-старому,
невидимо) → Фазы B–D (строим self и проверяем на живом) → ФЛИП: детект переключается на self и
старый путь из детекта УДАЛЯЕТСЯ совсем.** «Старое под капотом» на Фазах A–C — это транзиторная
стройка, а НЕ постоянный фолбэк (в финале его нет).

### Фаза H — КОЛОКОЛЬЧИК уведомлений о срабатываниях (В САМУЮ ПОСЛЕДНЮЮ ОЧЕРЕДЬ)
Запрос пользователя (2026-07-11): «у людей нет чёткого понимания, когда сработал какой триггер —
добавь колокольчик-уведомления, туда спамятся уведомления о срабатываниях; реализовать в самую
последнюю очередь». То есть глобальный in-app фид: «Триггер X → @user: директ доставлен / подписка
/ лайк» в реальном времени, с непрочитанным счётчиком (badge на иконке).
- **Данные УЖЕ есть:** каждое срабатывание пишется в `Log` (SUCCESS/WARN/ERROR + текст «Сработал
  триггер … → @user»). Колокольчик = отдельный вид на эти логи (фильтр по «срабатываниям»), с
  непрочитанным счётчиком и «пометить прочитанным».
- **UI:** иконка 🔔 в `TopNav` с badge количества новых срабатываний; выпадающий/модальный фид
  (переиспользовать `LogModal`-стиль, но глобально по всем аккаунтам, только события действий).
  Опрос `/api/logs` (уже есть, принимает лимит) или новый лёгкий `/api/activity` (только SUCCESS
  действий) с курсором «последнее виденное» (localStorage `rg-seen-activity-ts`).
- **Приоритет:** ПОСЛЕ D/E/F/G (детект+действия должны сперва реально работать; иначе колокольчик
  будет пустой/вводящий в заблуждение). Делать в самом конце.
- **[x] 2026-07-12 — СДЕЛАНО:** `app/api/activity/route.ts` (фид логов-срабатываний: `level in SUCCESS/WARN/ERROR` + message содержит «риггер «», скоуп по владельцу) + `components/common/ActivityBell.tsx` (🔔 в `TopNav` с badge непрочитанного; непрочитанное по метке `rg-seen-activity-ts` в localStorage; открытие панели = «прочитано»; поллинг 60с; первая загрузка не заваливает badge историей). `tsc` чист.
- **ТЕСТ H:** сработал триггер → в 🔔 появилось уведомление с корректным типом действия и @user;
  счётчик непрочитанного растёт; «прочитано» сбрасывает badge. ⬜ Живой тест на реальном срабатывании — после прогона кампании.

### Развилка стратегии отключения старого (историческая — решение принято выше)
- **Вариант «мягкий» (рекомендую):** черновые/API скрыты из UI и работают ТОЛЬКО как автоматический невидимый фолбэк, пока self-events не подтверждён на живом (Фазы D–E). Детект не может сломаться. После стабильного live (Фаза G) — отключаем.
- **Вариант «жёсткий»:** сразу полностью отключить черновые/API (self — единственный путь). Точно по букве просьбы, но если у self-events на живом всплывёт проблема — детект встанет до фикса.

### Тесты — сводно
- Юнит (без сети): нормализатор `news/inbox` (Фаза C), уже есть тесты `cookies`/`limits`/`match`/`targets`/`splitProxy` (§10.1) — не сломать.
- Живые (с прокси): Фаза B (снять формат), D (детект self), E (действия end-to-end / канарейка §10.4).
- Регресс UI: после Фазы A пройти все вкладки + онбординг — нет упоминаний черновых/API.

---

## 13. ПРЕДРЫНОЧНЫЕ ФИЧИ ЦЕННОСТИ (бэклог — реализовать ПОСЛЕ рабочего ядра A–H)

> Идеи из внешнего разбора (пользователь: «добавь, потом перечитаю и удалю ненужное»). Это НЕ
> часть текущего цикла plan4 (сначала self-events + действия должны реально работать — Фазы A–H).
> Ранжировано по «вау × реализуемость». Честная пометка по анти-бану у каждой.
>
> ⚠️ Общий честный контекст (не терять при продаже): вход через неофициальный браузер + прокси —
> «серая» зона; фичи ниже СНИЖАЮТ риск/паттерн и повышают ценность, но НЕ обнуляют риск бана.
> Позиционирование: «низкий риск + человекоподобность + максимум пользы», НЕ «бана не будет».

### 13.1 🔥 AI-персонализация каждого директа (Claude) — ТОП, дёшево, двойная польза
- Перед отправкой Claude генерит УНИКАЛЬНЫЙ текст под человека (ник/био/последний пост/коммент).
- **Взрывает:** уникальный текст = нет спам-паттерна = ниже риск бана И выше % ответов. Фича = защита.
- **Куда:** в `poll → dm-job` перед `sendDM` — вызов Claude с контекстом цели → текст вместо шаблона.
  Лимит токенов на владельца, фолбэк на шаблон при сбое.
- **Эффорт:** низкий (уже Claude-стек). Анти-бан: снижает паттерн.

### 13.2 🔥 AI-диалог в директе (двусторонний) — «менеджер», не «один выстрел»
- Ответил на директ → AI ведёт разговор, квалифицирует лид, ведёт к цели. Нужен поллинг входящих
  директов + контекст диалога на контакт.
- **Взрывает:** «рассылка» → виртуальный продавец. В дешёвом сегменте нет (ManyChat — только кнопки).
- **Куда:** поток чтения входящих (частично есть inbox) + модель `Conversation`/`Message` + Claude с историей.
- **Эффорт:** средний-высокий. Анти-бан: чтение своих директов безопасно; частота под лимиты.

### 13.3 🔥 Встроенный CRM/воронка из директов
- Контакт: стадии (написали→ответил→тёплый→продажа), теги, заметки, канбан.
- **Взрывает:** продаём СИСТЕМУ заработка → клиент видит деньги → меньше churn.
- **Куда:** модель `Lead` + UI-канбан, питается из потоков директа/диалога (13.2).
- **Эффорт:** средний (продуктовый UI). Анти-бана не касается.

### 13.4 ⚡ Ban-safety Score наружу + авто-регулировка темпа
- Индекс безопасности (`lib/safety.ts`) вынести живым светофором + авто-снижение темпа (частично §4.6).
- **Сильно:** продаём спокойствие + прозрачную защиту; у конкурентов на виду нет.
- **Куда:** виджет на дашборде/карточке; связать авто-темп с `deliveryStats`/лимитами.
- **Эффорт:** низкий (переиспользовать индекс). Анти-бан: прямая польза.

### 13.5 ⚡ Явный «тёплый прогрев» перед директом
- Перед директом: сторис + лайк поста + пауза → потом пишем (как живой «заметил»). Куски есть
  (fallback follow+like, §1.2 preActionBrowse) — оформить явной выбираемой стратегией.
- **Эффорт:** низкий. Анти-бан: выше % ответов, ниже риск.

### 13.6 ⚡ A/B-тест текстов + аналитика конверсии
- Автотест вариантов директа → бот выбирает победителя. Дашборд: reply rate, клики, конверсия.
- **Куда:** метрики ответов/кликов в `stats` (reply rate нужен inbox-поллинг из 13.2); варианты уже есть.
- **Эффорт:** средний. Ценность для агентств/реселлеров.

### 13.7 💡 Оркестрация для реселлеров + white-label
- Роли команды, суб-аккаунты клиентов, клонирование кампании на N аккаунтов в 1 клик, перепродажа под их брендом.
- **Куда:** организации/роли поверх мультитенанта (userId есть), bulk-применение, тема/лого.
- **Эффорт:** высокий (продуктовый). Монетизация реселлеров.

### 13.8 Ядро позиционирования
Связка **13.1 + 13.2 + 13.3** → «Ваши аккаунты сами общаются с аудиторией персонально, как живой
менеджер, и превращают подписчиков в продажи. С защитой от бана.» Отстройка от масс-ботов (банят) и
от ManyChat (нет follow/like/story-триггеров и настоящего AI-диалога).

### 13.9 ⚙️ ПРИОРИТЕТНЫЙ порядок действий на цель (запрос пользователя 2026-07-12)
Когда на одну цель настроено НЕСКОЛЬКО действий (подписка + лайк + сторис + директ) — выполнять
их в ФИКСИРОВАННОМ порядке приоритета, а НЕ случайно:
1. **Подписка (follow)** — всегда первой (устанавливаем связь).
2. **Лайк (like)**.
3. **Просмотр сторис (story)**.
4. **Директ (DM)** — всегда ПОСЛЕДНИМ (после «прогрева» цели follow/like/story директ выглядит
   естественнее и чаще доходит).
- **⚠️ ОТМЕНЯЕТ рандомизацию порядка §1.5** (сейчас `runVisit` в `workers/browser/lib/session.js`
  и шаги в `lib/browser/actions.ts` ПЕРЕМЕШИВАЮТ действия Фишер–Йейтсом). Решение пользователя:
  детерминированный приоритет важнее анти-детект-рандома порядка. Реализация: заменить `shuffle(tasks)`
  на сортировку по приоритету `{follow:0, like:1, story:2, dm:3}` в `runVisit`; в `runViaIndividualCalls`
  (fallback) и inline-пути — тот же порядок. Fallback директа (follow+like при закрытой личке) остаётся.
  Мелкий джиттер пауз между действиями (человекоподобность) сохранить, но ПОРЯДОК фиксировать.
- **[x] 2026-07-13 — СДЕЛАНО.** Порядок **follow→like→comment→story→dm** зафиксирован во ВСЕХ трёх путях:
  воркер `session.js runVisit` (`shuffle`→`orderTasks` по приоритету `{follow:0,like:1,comment:2,story:3,dm:4}`);
  Next.js `lib/browser/actions.ts runViaIndividualCalls` (убран `shuffle`, шаги в фикс. порядке); коммент-поток
  `poll/route.ts` (был comment→like→follow→dm→story, стал follow→like→comment→story→dm). Директ последним,
  идёт независимо от исхода прошлых (кроме гейта/дедупа/лимита). Build browser-49. `tsc` чист.

### 13.10 ⚙️ Настраиваемый ЛАЙК: сколько постов + семантика «1 срабатывание на цель» (запрос 2026-07-12)
- **Настройка действия «Лайк»:** сколько последних постов цели лайкнуть (N, дефолт 1). UI-поле в
  конструкторе кампании у чипа «Лайк». Проброс `count` в job → `likeUser(context,{targetUsername,count})`
  (в воркере `count` уже поддержан, лайкает до N последних постов с паузами).
- **Семантика подсчёта (важно, как просил пользователь):** ЛАЙК как действие на цель = ОДНО
  «срабатывание»/одно выполненное действие, НЕЗАВИСИМО от числа лайкнутых постов:
  - настроено 5, у человека 3 поста → лайкнуто 3 → **1 действие** (не 3);
  - у человека 5 постов, лайкнуто 5 → **1 действие**;
  - у человека 0 постов / 0 успешных лайков → триггер СРАБОТАЛ (1 срабатывание), но **0 действий**
    (лайк = 0, `incDone.like` не растёт, но само срабатывание засчитано, если было ХОТЬ ОДНО другое
    выполненное действие; если лайк — единственное действие и постов 0 → срабатывание есть, done=0).
  - Реализация: `incFired.like`+1 за попытку (резерв `use('like')` — ОДИН, не N); `incDone.like`+1
    только если реально лайкнут ≥1 пост (`r.liked>0`), иначе 0. Дневной лимит `like` списывается
    ОДИН раз на цель (не по постам) — иначе N-постовый лайк выест лимит.
- **ТЕСТ:** цель с 3 постами при настройке «лайкнуть 5» → лайкнуты 3, в stats like=1 (done), не 3;
  цель с 0 постами → like done=0, но триггер отмечен сработавшим (если было др. действие).
- **[x] 2026-07-13 — СДЕЛАНО.** Воркер `likeUser(count)`/`viewStories(count)` — до N постов/кадров, ≥1 успех = 1
  «done» (не N); 0 постов/0 сторис → `impossible:true`. poll: лайк = ОДИН бюджет на цель (`use('like')`, убран
  `COMMENT_LIKE_POSTS`); «невозможно» логируется как WARN «действие невозможно», НЕ ERROR; `fireCount` растёт
  при success ИЛИ при impossible-без-ошибок. UI: `Draft.likeCount` (1–10) в `LikeBlock`, `storyFrames` (1–20) в
  `StoriesBlock`; `count` пробрасывается в `LIKE_MEDIA`/`VIEW_STORIES` и читается обратно при редактировании. Оба
  потока (подписчики + комменты) + dm-воркер. `tsc` чист.

### 13.11 🔒 АВТО-ПРИЁМ подписчиков для ЗАКРЫТЫХ (приватных) аккаунтов (запрос 2026-07-12)
- **Новая функция:** для приватного основного аккаунта — автоматически ПОДТВЕРЖДАТЬ входящие
  запросы на подписку (follow requests), чтобы новый подписчик принимался без ручного действия
  (и дальше по нему могли отработать триггеры «Новая подписка»).
- **Реализация:** новый воркер-эндпоинт (напр. `/follow-requests/accept`) — читает ожидающие
  запросы приватным web-API изнутри залогиненной сессии (`/api/v1/friendships/pending/`) и
  подтверждает каждого (`POST /api/v1/friendships/approve/{user_id}/`), как `readStoryEvents`.
  Отдельный проход в `poll` (или свой кулдаун) + переключатель на аккаунте «Авто-приём подписчиков»
  (только для приватных; у публичных запросов на подписку нет). Дневной лимит на approve
  (ban-safety) + человекоподобные паузы. Учитывать в §1.6 суточном ритме.
- **ТЕСТ:** приватный канареечный аккаунт, входящий запрос → авто-подтверждён; далее событие
  «новый подписчик» (self-events) видит его и триггер отрабатывает.
- **[x] 2026-07-13 — СДЕЛАНО (код).** Воркер `acceptFollowRequests` (friendships/pending → approve,
  пейсинг+лимит) + `POST /follow-requests/accept` (build browser-50). client `browserAcceptFollowRequests`.
  poll: под локом ДО чтения уведомлений подтверждает до 10 заявок/цикл, логирует принятых. Схема
  `InstagramAccount.autoAcceptFollowers` (миграция `20260713140000_account_auto_accept`, аддитивна) +
  тумблер в деталях аккаунта + API GET/PATCH. `tsc` чист. ⚠️ **Живьём НЕ подтверждено:** тест на
  @cheidy.ite упал `login_required` (сессия аккаунта мертва) — нужен приватный аккаунт с ЖИВОЙ сессией.
  См. `PLAN-PRODUCTION.md §2.4`.

> **⚠️ ЖИВОЙ ТЕСТ Фазы E 2026-07-14 — ЧАСТИЧНО, вскрыл P0-баги.** Прогон @aheidy.hec + канарейка
> @cheidy.ite: детект self-events работает (читал подписки/лайки/комменты), авто-приём вызывается,
> НО: (1) визит действий `/session/run` упал по таймауту 180с → директ не доставлен; (2) периодически
> `Body has already been read` в чтении уведомлений; (3) сессия @cheidy.ite мертва (login_required) и
> это НЕ отражается в UI + не удаётся перезайти; (4) техножаргон в логах; (5) кнопка Live не обновляет
> журнал; (6) несовпадения счётчиков. **Все зафиксированы в новом `PLAN-PRODUCTION.md` (P0-раздел) —
> финальном плане перед рынком.** Действия §13.9/§13.10 в коде верны, но требуют ЖИВОГО подтверждения
> после фикса таймаута визита и мёртвой сессии канарейки.

### 13.12 🎨 3D-tilt карточек аккаунтов — вернуть лёгкий параллакс за курсором (запрос 2026-07-12)
- **Что:** вернуть лёгкий 3D-наклон (параллакс) карточек аккаунтов вслед за курсором. Компонент
  `components/ui/Tilt.tsx` уже был, но в редизайне H1 (2026-07-04) его превратили в проходную
  обёртку (наклон убрали ради «успокоения эффектов»). Нужно аккуратно вернуть — но ДЕЛИКАТНО:
  малый угол (≤6–8°), плавный `transition`, `will-change: transform`, сброс при уходе курсора.
- **Где:** карточки аккаунтов на `triggers/page.tsx` (`AccountCard`) и `accounts/page.tsx`.
- **Ограничения (ban-safety не при чём, это чистый UI):** уважать `prefers-reduced-motion`
  (наклон off); не мешать кликам/тултипам/провалу внутрь аккаунта; отключать на тач-устройствах
  (нет курсора). Не возвращать убранные в H1 «кричащие» эффекты (pulse-glow/sheen/neon) — только
  мягкий tilt + существующие `card-3d`/`gloss`.
- **ТЕСТ:** наведение → плавный наклон; уход → возврат; reduced-motion → без наклона; клик и
  проваливание внутрь аккаунта работают; на мобиле наклона нет.
- **[x] 2026-07-12 — СДЕЛАНО.** `components/ui/Tilt.tsx` переписан из проходной обёртки в рабочий
  наклон: угол ≤6°, `perspective:900`, активный переход `0.08s` / плавный возврат `0.4s`,
  `will-change:transform`, обновление через `requestAnimationFrame`. Гейт `enabled` по
  `matchMedia('(hover:hover) and (pointer:fine)')` И `!prefers-reduced-motion` (тач/мобайл/SSR →
  проходная обёртка без наклона). Подключён в `triggers/page.tsx` `AccountCard` (обёртка `h-full`);
  `accounts/page.tsx` уже использовал `Tilt`. `tsc` чист. **Поведение проверено в браузере**
  (временная public-страница, удалена после): reduced-motion→passthrough; desktop→perspective+
  обработчики; move(низ-право)→`rotateX(-4.2°) rotateY(4.4°)` + `0.08s`; leave→сброс в `0` + `0.4s`.
  ⚠️ В самих боевых `/triggers`·`/accounts` вживую не открыть локально (нет `.env`/БД → логин 500) —
  но компонент/интеграция верифицированы изолированно; финальный взгляд на реальных карточках —
  на деплое.

### 13.13 📊 3D-вкладка «Статистика» — сравнение по типам кампаний (какой триггер срабатывает чаще)
- **Что:** переделать вкладку `stats/page.tsx` в визуально богатую, интерактивную 3D-статистику.
  Главная метрика — **сравнение по ТИПАМ кампаний** (какой триггер срабатывает больше всего):
  Новая подписка / Комментарий / Лайк / Ответ на сторис. Плюс переключаемые срезы (по аккаунту,
  по действию DM/лайк/подписка/сторис, по времени).
- **Данные уже есть:** `TriggerRule.fireCount` (срабатывания на success) + `TriggerRule.stats`
  (JSON: dm/like/follow/story/comment) + `EventType` кампании. Агрегировать по `EventType`
  (маппинг → «Новая подписка/Комментарий/Лайк/Ответ на сторис») для главного графика.
- **Дизайн:** интерактивный 3D-график (кликабельные секторы/бары, hover-подсветка, легенда,
  плавные переходы), в стиле, показанном пользователем (3D pie/donut с explode, drag-rotate,
  переключатели срезов внизу). Тема-aware, адаптив, `prefers-reduced-motion`.
- **Заготовка:** написан ГЛОБАЛЬНЫЙ супер-промпт для генерации такой вкладки внешней моделью —
  см. `PROMPT-3D-STATS.md` в корне (потом адаптируется под наши данные/палитру/API).
- **ТЕСТ:** реальные `fireCount`/`stats` из БД → корректные доли по типам; клик по сектору →
  дифференцирует; пусто/загрузка/ошибка; тёмная тема + мобайл; reduced-motion без вращения.
- **[x] 2026-07-12 — СДЕЛАНО (адаптирован готовый макет пользователя).** Пользователь скачал
  готовую 3D-диаграмму (Three.js/WebGPU, `public/3D stats/mb luczsze/Stats.html`) — она же на
  референс-скрине. Адаптирована, НЕ переписана: чистая копия → `public/stats3d/index.html`.
  Правки: (1) локализация (заголовок «Срабатывания по типу кампании», кнопки Сбросить/Остановить/
  Разложить/Кольцо, легенда «Разбивка»); (2) 3 среза-вкладки вместо демо — «По типу кампании» /
  «По действию» / «По аккаунту»; (3) DATASETS заменены на наши типы с брендовыми цветами
  (подписка=фиолет, коммент=зелёный, лайк=янтарь, сторис=голубой) + плейсхолдеры; (4) живой поток
  данных `postMessage` — страница шлёт `{type:'ig-stats', campaigns[], actions[], accounts[]}`,
  диаграмма перестраивается; гейт нулей (value=0 отфильтрованы, пусто → «Пока нет срабатываний»,
  без NaN); (5) скрыт авторский dev-панель «Scene Settings» (⚙). Встроена в `stats/page.tsx` как
  hero-карточка через iframe (`components/stats/Chart3D.tsx`), кормится реальными
  `fireCount`/`stats.done`; плоские бары ниже оставлены как «числа» + фолбэк, если WebGPU/WebGL
  недоступны. `/stats3d/index.html` — публичный статический шаблон БЕЗ данных (данные приходят
  postMessage от авторизованной страницы). `tsc` чист. **Проверено в браузере (standalone):**
  грузится без ошибок, Three init/canvas ОК, локализация ОК, postMessage `campaigns[5,9,3,7]` →
  легенда 20.8/37.5/12.5/29.2% (точно), нули не роняют, переключение всех 3 срезов без ошибок.
  ⚠️ Живой 3D-кадр в песочнице не снят (preview-таб фоновый → rAF на паузе, скриншот таймаутит) и
  сам `/stats` локально не открыть (нет БД → логин 500) — визуальный вид = ровно тот макет, что
  пользователь одобрил; финальный взгляд на реальных данных — на деплое.
  ⚠️ **Перф-заметка ([[mascot-prerendered-not-webgl]]):** WebGPU тяжелее; изолирован в iframe
  (не тормозит остальной дашборд), есть WebGL2-фолбэк и кнопка «Остановить вращение». Следить на
  слабых машинах; при жалобах — статичный рендер/пауза авто-вращения по умолчанию.
- **[x] 2026-07-12 (8) — фикс битых меток диаграммы:** метки долек рисовались `TextGeometry` со
  шрифтом helvetiker (латиница-only) → кириллица `????`. Переписал `createSliceLabel` на
  canvas-спрайт (`fillText`, системный шрифт). Локализованы англ. JS-кнопки. «По типу кампании»
  → «По типу триггера» везде. Проверено в браузере (canvas рендерит кириллицу).
- **[x] 2026-07-12 (9) — «Матрица выполнений» поднята до 3D-уровня диаграммы (единый язык):**
  `CampaignMatrix.tsx` — объёмные заголовки-плитки типов, глянцевые пилюли-ячейки, Σ/Итого-пилюли,
  count-up, каскад строк. Цвета типов СИНХРОНИЗИРОВАНЫ с диаграммой (было расхождение: лайк/сторис).
  Модели данных не тронуты. Все 3 вида проверены в браузере на моковых данных. `tsc` чист.

---

## 14. ПОЛНЫЙ АУДИТ ДО БОЕВОЙ ГОТОВНОСТИ (перед выходом на рынок)

> Сквозная проверка «до идеала» — и функционал, и дизайн/UX. Делать ПОСЛЕ ядра (A–H), с живым прокси.
> Каждый пункт — критерий «готово» + как проверить. Это финальный gate перед фичами §13.

### 14.1 Функциональный аудит (всё работает end-to-end)
- [ ] Матрица триггер×действие на живом (канарейка §10.4): NEW_FOLLOWER/NEW_COMMENT/NEW_LIKE/STORY_MENTION × директ/подписка/лайк/сторис/ответ-в-комментах/гейт. Для каждого: событие ЗАМЕЧЕНО (self-events) → действие ВЫПОЛНЕНО (подтверждено) → учтено (fireCount на success, stats N/N).
- [ ] Вход: логин/пароль, куки, 2FA, challenge, массовый импорт — все пути; «Ошибка авторизации» показывает ПРИЧИНУ.
- [ ] Прокси: проверка/подбор/ротация/«выжженные»/гео-локаль.
- [ ] Лимиты/ритм/прогрев соблюдаются; авто-снижение при недоставке.
- [ ] Доставка: подтверждение директа; недоставка → честный статус, без ложного «сработал».
- [ ] Крайние случаи: закрытая личка (fallback), нет постов/сторис, приватный аккаунт, дедуп, пустой базлайн.
- [ ] Гонки/стабильность: §4.8, crash-guard, single-flight, нет утечки контекстов.
- [ ] Регресс легаси: боевой путь НИГДЕ не зовёт Python/HikerAPI/черновые (grep + прогон).

### 14.2 Аудит дизайна / UI
- [ ] Консистентность компонентов/отступов/типографики/палитры на ВСЕХ вкладках.
- [ ] Тёмная тема (если есть) на каждом экране; контраст ≥ AA.
- [ ] Адаптив (мобайл/планшет/десктоп); модалки скроллятся, кнопки доступны (был баг «Авторизоваться» за экраном).
- [ ] Состояния: загрузка/пусто/ошибка/длинные списки.
- [ ] Иконки/лого/favicon/заголовки вкладок консистентны.
- [x] 2026-07-14 — Убрать debug-инструменты (🔔-проба) за флаг. 🔔-проба (кнопка + панель вывода в `AccountDetailModal`) скрыта за `NEXT_PUBLIC_DEBUG_TOOLS==='1'` (по умолчанию НЕ задан → скрыто, релиз-safe; при отладке — выставить флаг). Канарейка 🧪 оставлена (легитимный тест §10.4).

### 14.3 UX / удобство
- [ ] Онбординг актуален (без черновых/API), короткий путь, верный счётчик шагов.
- [ ] Копирайт понятен не-технарю; Hint-подсказки; ошибки объясняют ЧТО делать.
- [ ] Главный сценарий (аккаунт → кампания → срабатывание) — минимум кликов, без тупиков.
- [ ] Видимый отклик на каждое действие; прогресс на долгих операциях.
- [ ] Колокольчик срабатываний (Фаза H) понятен. Справка «Что где находится» актуальна.

### 14.4 Производительность / масштаб
- [ ] Дашборд/списки быстрые на 50–80 аккаунтах; нет N+1; пагинация тяжёлого.
- [ ] Воркер: RAM под concurrency, контексты закрываются, нет утечек (§6.1).
- [ ] Поллинг укладывается в интервал; self-events дешёвый.

### 14.5 Безопасность / приватность
- [x] 2026-07-14 — Изоляция тенантов (userId) — аудит ВСЕХ 29 роутов `app/api/**`. ЧИСТО: каждый data-роут требует auth (`getUserOrFirst`/`getCurrentUser`); все 6 `[id]`-мутаций (accounts/proxies/sections/templates/triggers + reset-snapshot) проверяют владение (`findFirst {id,userId}`→404 ИЛИ `deleteMany/updateMany {id,userId}`) ДО любой незаскоуплённой записи — IDOR нет. `poll` — internal-secret (крон) + user-scope (ручной). `auth/resend` без DB-скоупа осознанно (challenge-сессия в воркере по username, код уходит владельцу аккаунта — cross-tenant утечки нет). Health-роуты (browser/scraper) не отдают данные юзера. Правок не потребовалось.
- [ ] Секреты (пароли почты/сессии) шифруются at-rest; не текут в логи/UI/URL.
- [ ] Секрет воркера/INTERNAL_SECRET/JWT заданы, не дефолтные в проде; нет секретов в git/бандле.

### 14.6 Надёжность / мониторинг
- [ ] Логи информативны (причина провала), не спамят.
- [ ] Алерты при системных проблемах (нет воркера, массовая недоставка, бан аккаунта).
- [ ] Здоровье воркера/скрейпера в UI; авто-восстановление после краха.
- [ ] Миграции аддитивны/безопасны.

### 14.7 Критерии «ГОТОВО К РЫНКУ» (подпись приёмки)
- [ ] Все триггеры×действия подтверждены на живом ≥1 раз.
- [~] 0 упоминаний легаси/черновых/API в UI; self-events — единственный детект. **UI-часть закрыта (2026-07-12 (8)):** `/drafts` редиректит, `DraftsStatus` скрыт, метка «Парсинг (API)» на Статистике → честная «Число подписчиков (API)». Осталось: подтвердить «self-events — единственный детект» на ЖИВОМ (Фаза E, нужен прокси).
- [ ] Дизайн/UX-аудит пройден без крупных замечаний.
- [ ] Безопасность без дыр; изоляция тенантов подтверждена.
- [ ] Ban-safety честный; лимиты/прогрев/паузы активны; на канарейке неделя без бана.
- [ ] Нет debug-инструментов на виду; онбординг ведёт новичка до первого срабатывания.

> Порядок: ядро (A–H) → аудит (§14) как финальный gate → предрыночные фичи (§13) по одной,
> каждая со своим тестом и честной оценкой анти-бана.
