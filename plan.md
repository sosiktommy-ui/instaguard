# InstaGuard — ПЛАН перехода на браузерную автоматизацию (эмуль) + микс парсинга (HikerAPI + черновые)

> Это ГЛАВНЫЙ рабочий план. Любой агент, начиная задачу, читает его целиком и идёт по фазам сверху вниз.
> После завершения пункта — отмечает `[x]` и дописывает краткий итог в раздел «Журнал внедрения» внизу.
> Значимые изменения по-прежнему фиксируются в `CLAUDE.md` (не заменяет его — plan.md про ЦЕЛЬ, CLAUDE.md про историю).

---

## 0. Правила для агентов

- **Не ломать то, что работает.** Переход поэтапный (Фазы 1→5). Каждая фаза оставляет приложение рабочим.
- **Не коммитить паузную auth-работу** (регистрация с кодом/Google) — она в рабочем дереве, но не готова (ждёт ключей). Коммитить только файлы своей задачи (`git add <файлы>`, не `git add -A`).
- **Проверять перед пушем:** `npx tsc --noEmit` чисто + `npx next build` проходит. Python/браузерный воркер — `py_compile` / `tsc` соответственно.
- **Секреты — только в ENV**, никогда в код/гит (ключи прокси, 2captcha, API).
- **Railway деплой:** Next.js — Nixpacks; воркеры — Dockerfile. Миграции БД применяются `prisma migrate deploy` в `start`.
- Каждая фаза заканчивается разделом «что тестить» — прогнать перед следующей.

---

## 1. Контекст и диагноз (зачем переход)

**Проблема:** вход в Instagram по логину/паролю через `instagrapi` (приватный мобильный API) НЕ проходит. Пользователь сменил 20+ чистых резидентных прокси — ответ один: `UserInvalidCredentials` / «Forgotten password» / «change your IP … blacklist». Одинаковый отказ через много разных чистых IP ⇒ виноват **не прокси, а сам метод входа** (приватный API детектится Meta и отбивается в recovery-флоу для свежекупленных аккаунтов).

**Решение (подтверждено проектом autofacebook):** вход и действия — через **настоящий браузер (Playwright + Chromium)**, как делает autofacebook для Facebook и LeadFeed для Instagram. Живой браузер Meta пускает; приватный API — нет. Парсинг (чтение публичных данных) остаётся через **HikerAPI** (не требует аккаунтов) с опциональным fallback на черновые.

**Что берём из autofacebook:** реальный Chromium, `--disable-blink-features=AutomationControlled`, ввод в настоящую форму. **Что добавляем сверх него:** прокси на контекст, stealth-плагин (у них импортирован, но не подключён), человекоподобное поведение, сохранение сессии (cookies/storageState), обработка checkpoint/2FA.

---

## 2. Целевая архитектура

```
┌─────────────────────────┐     парсинг (чтение)      ┌──────────────────┐
│  Next.js (Railway)       │ ────────────────────────▶ │  HikerAPI (внеш.) │
│  UI + API + poll + очередь│                            └──────────────────┘
│                          │     логин + действия       ┌──────────────────────────┐
│                          │ ────────────────────────▶ │  БРАУЗЕРНЫЙ ВОРКЕР (новый) │
│                          │                            │  Node + Playwright/Chromium│
│                          │     (fallback-парсинг      │  workers/browser           │
│                          │      черновыми)            └──────────────────────────┘
└──────────┬───────────────┘
           │ Prisma
     ┌──────▼──────┐   ┌──────────┐
     │ PostgreSQL  │   │ Redis    │  (BullMQ: очередь действий, авто-поллинг)
     └─────────────┘   └──────────┘

Python instagrapi-воркер → LEGACY. Оставляем задеплоенным до Фазы 5 как аварийный
fallback, затем удаляем. Новый код его НЕ использует.
```

**Три роли, чётко разделены:**
1. **HikerAPI** — парсинг подписчиков/комментов/лайков БЕЗ аккаунтов (основной источник данных). Уже внедрён.
2. **Браузерный воркер** — ВСЁ, что требует аккаунта: вход, директ, лайк, подписка, сторис, ответ в комментах. Сессия = Playwright `storageState` (cookies+localStorage) на аккаунт.
3. **Черновые (HELPER)** — опциональный fallback-парсер: логинятся браузером, парсят через DOM. Включаются настройкой (см. §5). По умолчанию не нужны.

### 2.1 Где крутится браузер: облако (Railway) vs локально — РЕШЕНО, не переобсуждать

Это была главная развилка. Решение принято, оба пути дают ОДИН и тот же артефакт — Playwright `storageState` (cookies+localStorage), который кладётся в `InstagramAccount.browserState`. Поэтому они взаимозаменяемы на уровне данных и не конфликтуют.

- **Основной путь — браузерный воркер на Railway (§4).** Держит продукт «облачным»: пользователь ничего не ставит на свой ПК, вход и действия идут сами. Логин + действия = реальный Chromium + резидентный прокси аккаунта + человекоподобное поведение + стабильный отпечаток. Это и делает продукт «просто работающим». Минус — RAM (Chromium ~300–500 МБ на контекст) → жёстко ограничиваем конкуренцию (`BROWSER_CONCURRENCY=1–2`) и закрываем контексты.
- **Запасной путь / ускоритель — локальный генератор кук `tools/cookie-gen/` (Фаза 1, опционально).** Мини-скрипт Playwright headful на ПК пользователя: читает список `логин:пароль:почта:пароль-почты`, логинит по одному в видимом окне, пользователь глазами решает checkpoint, на выходе — `storageState` строкой → вставляется в «Импорт списком» (режим «Куки»/«Сессия»). Нужен для «упрямых» аккаунтов, которые в облаке циклят на checkpoint, и как аварийный разблокировщик. Тот же флоу логина, что в §4.5, только `headless:false` и без секрета.
- **Нюанс гео/IP (важно для будущих агентов):** `storageState` переносим между IP, но Instagram насторожен к резкому прыжку IP/страны между входом и действиями. Поэтому: (а) даже при локальном входе логиниться желательно ЧЕРЕЗ прокси аккаунта (или хотя бы из страны/региона аккаунта); (б) после импорта все действия этой сессии идут через закреплённый за аккаунтом прокси — не с «голого» IP сервера. Домашний резидентный IP пользователя чище shared-прокси, но это всё равно чужая страна для азиатских аккаунтов — идеал: прокси в стране аккаунта на всех этапах.

Вывод для агентов: **по умолчанию делаем облачный воркер (§4)**. Локальный `tools/cookie-gen/` — отдельная маленькая задача Фазы 1, не блокирует основной путь и не переписывает архитектуру.

---

## 3. Модель данных (изменения Prisma)

```prisma
model InstagramAccount {
  // ...существующее...
  browserState   Json?     // Playwright storageState (cookies+origins). Основная сессия эмуля.
  sessionData    Json?     // LEGACY instagrapi-сессия. Не трогаем, читаем только для миграции.
  loginMethod    String    @default("browser") // "browser" | "cookies" | "legacy"
  emailLogin     String?   // почта аккаунта (для авточтения кода checkpoint по IMAP)
  emailPassword  String?   // пароль почты (ШИФРОВАТЬ; только для IMAP). Не отдавать в UI.
}

model UserSettings {
  // ...существующее (allowNoProxy, allowNoDrafts, likeByDraft, storyByDraft — LEGACY)...
  parsingSource  String   @default("api") // "api" | "drafts" | "drafts_then_api"
  actionEngine   String   @default("browser") // "browser" | "legacy" (на время перехода)
  browserHeadful Boolean  @default(false) // отладка: показывать окно (только локально)
}
```

- Миграция: `20260709000000_browser_engine` (идемпотентно, `ADD COLUMN IF NOT EXISTS`).
- `emailPassword` шифровать тем же `ENCRYPTION_KEY`, что и сессии. В API-ответах НИКОГДА не отдавать.
- `browserState` наружу не отдавать (как `sessionData`).

---

## 4. Браузерный воркер `workers/browser/` (сердце перехода)

### 4.1 Стек и деплой
- **Node.js 20 + Playwright** (`playwright`), stealth: `playwright-extra` + `puppeteer-extra-plugin-stealth`.
- **Docker** на базе `mcr.microsoft.com/playwright:v1.4x-jammy` (браузеры и системные зависимости уже внутри).
- Отдельный Railway-сервис, Root Directory = `workers/browser`, `railway.json` builder = DOCKERFILE.
- Порт из `$PORT`. Health: `GET /health` → `{ build, playwright, chromium }`.
- Авторизация запросов: заголовок `X-Worker-Secret` = ENV `BROWSER_WORKER_SECRET` (в Next.js — `BROWSER_WORKER_URL` + тот же секрет).

### 4.2 Управление сессиями и браузером
- **Сессия аккаунта** = Playwright `storageState` (JSON). Хранится в `InstagramAccount.browserState`.
- Каждое действие: `browser.newContext({ storageState, proxy, userAgent, locale, timezoneId, viewport })` → выполнить → `context.storageState()` вернуть Next.js для сохранения (сессия «дозревает»).
- **Один браузер-процесс на воркер**, контекст на задачу (изоляция cookies/прокси). Контекст закрывать после задачи.
- **Конкуренция:** ENV `BROWSER_CONCURRENCY` (по умолч. 2). Очередь задач внутри воркера (p-queue). Chromium ест ~300–500 МБ на контекст — не превышать RAM Railway.
- **Прокси на контекст:** `proxy: { server, username, password }`. Схему (http/socks5) определять как в Python-воркере.
- **Гео-консистентность:** `locale`/`timezoneId` из страны прокси (как `_locale_for_proxy` в Python-воркере).

### 4.3 Анти-детект (обязательно)
- `playwright-extra` + stealth-плагин ПОДКЛЮЧИТЬ (autofacebook импортировал, но не применил — не повторять их ошибку).
- Флаги: `--disable-blink-features=AutomationControlled`, `--no-sandbox`, `--disable-dev-shm-usage`.
- Реалистичный desktop UA (Chrome на Win/Mac), под него — `viewport` из набора реальных разрешений (случайно на аккаунт, но СТАБИЛЬНО — детерминированно по username, как отпечаток устройства в Python-воркере).
- **Человекоподобность:** ввод по буквам с задержкой 80–220 мс/символ (`page.type` c `delay`), паузы между полями, случайные движения мыши/скролл перед действием, «прогрев» ленты (зайти на главную, проскроллить 1–2 экрана) перед первым действием сессии.
- `context.addInitScript` — маскировать `navigator.webdriver`, `languages`, `plugins` (частично покрыто stealth).
- Одна сессия = один стабильный отпечаток (UA/viewport/locale не меняются между входами этого аккаунта).

### 4.4 Эндпоинты (все POST, кроме health; тело JSON)
| Эндпоинт | Вход | Выход |
|---|---|---|
| `GET /health` | — | `{build, playwright, chromium}` |
| `/login` | `{username,password,proxy,totpSecret?,locale?}` | `{ok, storageState}` ИЛИ `{needsCheckpoint, channel:'email'\|'sms', sentTo}` ИЛИ `{needs2fa}` ИЛИ `{error:'bad_password'\|'suspended'\|...}` |
| `/login/checkpoint` | `{username,proxy,code, pendingRef}` | `{ok, storageState}` \| `{error}` |
| `/session/test` | `{storageState,proxy}` | `{alive:boolean, username?}` |
| `/dm` | `{storageState,proxy,toUsername,text,image?}` | `{ok, storageState}` \| `{error, closed?:bool}` |
| `/follow` | `{storageState,proxy,targetUsername}` | `{ok, storageState}` |
| `/like` | `{storageState,proxy,targetUsername,count?}` | `{ok, liked, storageState}` |
| `/stories` | `{storageState,proxy,targetUsername,like?}` | `{ok, viewed, liked, storageState}` |
| `/comment` | `{storageState,proxy,postUrl,text}` | `{ok, storageState}` |
| `/reply-comment` | `{storageState,proxy,postUrl,text,parentCommentId?}` | `{ok, storageState}` |
| `/parse/followers` | `{storageState,proxy,targetUsername,limit}` | `{followers:[{pk,username}]}` (fallback-парсинг черновым) |
| `/parse/comments` | `{storageState,proxy,targetUsername,mediaCount,perMedia}` | `{comments:[...]}` |
| `/parse/likers` | `{storageState,proxy,targetUsername,mediaCount,perMedia}` | `{likers:[...]}` |

- Все действия оперируют **username** (навигация `instagram.com/{username}/`), НЕ pk. Цели из HikerAPI несут и username, и pk — брать username.
- Каждый ответ действия возвращает обновлённый `storageState` → Next.js сохраняет в БД (сессия живёт дольше).
- Ошибки классифицировать: `bad_password` / `checkpoint` / `2fa` / `suspended` / `action_blocked` (feedback_required-аналог) / `dm_closed` (личка закрыта) / `network`. Next.js по ним ставит статус аккаунта (как сейчас `statusFromError`).

### 4.5 Флоу логина (детально)
1. Контекст с прокси → `goto('https://www.instagram.com/accounts/login/')`.
2. Принять cookie-баннер если есть. Ввести username (по буквам), пауза, password (по буквам), клик «Войти».
3. Ждать один из исходов (по URL/DOM/тексту):
   - **Успех** — редирект на ленту/`/` → сохранить `storageState`, вернуть `ok`.
   - **Неверный пароль** — текст «incorrect»/форма с ошибкой → `error:bad_password`.
   - **Checkpoint** — «We sent a code / Enter the code / подтвердите» → определить канал (email/SMS), вернуть `needsCheckpoint`. Контекст СОХРАНИТЬ в память воркера по `pendingRef` (или persist на диск, как challenge в Python-воркере) для шага `/login/checkpoint`.
   - **2FA** — поле кода 2FA → если `totpSecret` есть, сгенерировать TOTP и ввести; иначе `needs2fa`.
   - **Suspended/«мы приостановили аккаунт»** → `error:suspended` (войти нельзя).
4. `/login/checkpoint`: восстановить контекст, ввести код, дождаться успеха → `storageState`.
5. **Автополучение кода с почты (опционально, Фаза 2):** если у аккаунта есть `emailLogin/emailPassword` и почта поддерживает IMAP (firstmail — да) — читать код письмом (модуль `mail-reader`), не заставляя пользователя вводить руками. Иначе — ручной ввод через UI (флоу challenge уже есть).

### 4.6 Флоу действий (детально)
- **DM:** `goto('/{username}/')` → дождаться профиля → кнопка «Message»/«Написать» → в треде поле ввода → `type` текст по буквам → Enter/кнопка «Отправить». Если «Message» нет (закрытый профиль/ограничение) → `dm_closed`. Фото — вложение через input[type=file].
- **Follow:** профиль → кнопка «Follow»/«Подписаться» (если уже «Following» — noop).
- **Like:** профиль → открыть последний пост → кнопка «Нравится» (по aria-label). `count` — пролайкать N последних.
- **Stories:** профиль → клик по аватарке-сторис → просмотр (пауза на «кадр») → опц. лайк.
- **Comment/reply:** `goto(postUrl)` → поле комментария → type → отправить.
- Между действиями — паузы (как в текущем поллинге: 40–115 с через очередь). Перед первым действием сессии — «прогрев» ленты.
- Селекторы вынести в конфиг (`selectors.js`) с несколькими вариантами (aria-label RU/EN + роль) — IG часто меняет вёрстку. При «не нашёл селектор» — понятная ошибка, скрин в лог (Фаза 4).

---

## 5. Парсинг: микс HikerAPI + черновые (`parsingSource`)

Настройка владельца `parsingSource`:
| Режим | Источник данных | Черновые логинятся | Вкладка «Черновые» |
|---|---|---|---|
| `api` (по умолч.) | HikerAPI | нет | скрыта (показывается «Парсинг (API)») |
| `drafts` | черновые через `/parse/*` браузерного воркера | да (браузером) | видна |
| `drafts_then_api` | черновые; если у владельца нет живого чернового → HikerAPI | да | видна |

- `api` — уже реализовано (см. `lib/scraper/hiker.ts`, `poll/route.ts`).
- `drafts` / `drafts_then_api` — вернуть draft-контур из истории git (коммит ДО `da71ede`) и парсить через браузерный воркер (`/parse/followers|comments|likers`) сессией чернового `browserState`. Гейт-проверки подписки — тоже через `/parse/followers` основного (публично) или через API.
- Черновые в новом мире = обычные аккаунты с ролью HELPER, логин которых прошёл БРАУЗЕРОМ (тот же `/login`). Свой прокси, свой `browserState`.
- Стори-события (`STORY_MENTION`) — всегда сессией ОСНОВНОГО (его личка), через браузерный `/stories`-чтение или директ-инбокс (Фаза 3).

---

## 6. Изменения в Next.js

- **`lib/browser/client.ts` (новый)** — обёртка над браузерным воркером (`browserFetch`, функции `browserLogin`, `submitCheckpoint`, `browserDM`, `browserFollow`, `browserLike`, `browserStories`, `browserComment`, `browserReply`, `parseFollowers/Comments/Likers`, `testSession`). Формы ответов действий = как у Python-клиента, чтобы poll менялся минимально. Каждое действие после успеха → сохранить возвращённый `storageState` в БД.
- **`app/api/accounts/auth/route.ts`** — вход теперь через `browserLogin` (при `actionEngine='browser'`). 202 при `needsCheckpoint`/`needs2fa` (флоу уже есть). Сохранять `browserState` + `loginMethod='browser'` + (опц.) `emailLogin/emailPassword`.
- **`app/api/accounts/auth/challenge/route.ts`** — `submitCheckpoint` браузерного воркера.
- **`app/api/accounts/import/route.ts`** — массовый вход браузером; парсить `логин пароль почта пароль-почты` + сохранять почту для IMAP.
- **`app/api/poll/route.ts`** — действия слать в браузерный воркер вместо Python. Очередь `dm-send` (BullMQ) в `instrumentation.ts` — переключить вызовы `/send-dm` и т.д. на браузерный воркер. Парсинг — по `parsingSource` (§5). Сохранять обновлённый `storageState` после каждого действия.
- **`instrumentation.ts`** — dm-воркер зовёт браузерный воркер; сохраняет `browserState`. Авто-поллинг без изменений.
- **`app/api/settings/route.ts`** — принять/отдать `parsingSource`, `actionEngine`.
- **`app/api/browser-health/route.ts` (новый)** — прокси на `/health` браузерного воркера (как `worker-health`).
- **`lib/safety.ts`** — фактор «сессия жива» опирается на `browserState`; фактор парсинга зависит от `parsingSource`.

---

## 7. Вкладки — что на каждой и как работает (ДЕТАЛЬНО)

> Формат каждой вкладки ниже: **Маршрут/меню · Назначение · Структура сверху вниз · Данные (endpoints) · Состояния (загрузка/пусто/ошибка) · Что меняется при переходе на эмуль.** Агент, трогающий вкладку, сверяет реализацию с её пунктом.

### 7.1 Рекламные кампании (`/triggers`) — ГЛАВНАЯ
- **Маршрут/меню:** `/triggers`, пункт «Рекламные кампании» (иконка `List`). Корневой redirect `(dashboard)/page.tsx` → `/triggers`.
- **Назначение:** пирамида «аккаунт → его кампании». Точка входа: видеть аккаунты, их здоровье, создавать/смотреть кампании.

**Структура (сверху вниз):**
1. **`PageHeader`** — иконка + «Рекламные кампании» + подзаголовок-подсказка. Якорь тура `data-tour="page"`.
2. **`SectionBar`** — чипы разделов/подразделов (папки), фильтруют список аккаунтов ниже. «+ Раздел».
3. **Форма «Создать кампанию» (`CreateForm`, `startOpen`, раскрыта по умолчанию).** Три шага:
   - Шаг 1 «Аккаунты» — мультивыбор основных (скрыт/заперт, если провалились внутрь аккаунта — `lockedAccountId`).
   - Шаг 2 «Событие» — карточки-иконки: **Подписка** (`NEW_FOLLOWER`), **Комментарий** (`NEW_COMMENT`), **Лайк** (`NEW_LIKE`), **Сторис** (`STORY_MENTION`).
   - Шаг 3 «Настройка» — все блоки сворачиваемые (`Group defaultOpen={false}`): **Название кампании (первое поле)** → чипы **действий** (Директ/Лайк/Подписка/Сторис, мультивыбор) → **Сообщение** (порядок: картинка → текст → кастомный текст) → **Сигнал** (на что реагировать: все слова / конкретные фразы, «только точная фраза») → для Комментария — «Ответ в комментариях» (≥5 вариантов) → **гейт подписки** («Проверять подписку», режим followed_by/mutual + текст неподписанным) → **задержки** (min/max). Кнопки «Сохранить как шаблон» / «Шаблоны» (выезжающая панель применить/удалить).
4. **Сетка карточек аккаунтов (`AccountCard`), Уровень 1.** На карточке: аватар, @username, **статус-плашка** (🟢 работает / 🔵 готов / 🔴 бан-челлендж / 🟡 ошибки), **чип «Прокси»/«Без прокси»** (красный = риск), **`SecurityBadge`** (индекс безопасности, тултип-чеклист факторов), **4 иконки типов триггеров** (горят градиентом, если активен триггер этого типа), **3 цифры**: Кампаний · Подписчиков · Срабатываний, кнопка «Открыть лог» (`LogModal`). Клик по карточке → провал на Уровень 2.
5. **Кнопка «+ Аккаунт»** под сеткой → единый `AddAccountModal` (§7.2). Пустое состояние (аккаунтов нет) — та же кнопка крупно + пояснение.
6. **Сводка (`StatCard`)** — метрики внизу.

**Уровень 2 (внутри аккаунта, после провала):** хлебные крошки в `TopNav` («Рекламные кампании / @username», первый — назад) → шапка аккаунта → список компактных `CampaignCard` («сработал N раз» + действия со счётчиками `DM ×12 · Лайк ×5 · Подписка ×3`, детали текста/задержки под «details») → кнопка «+ Кампания» (открывает `CreateForm` с `lockedAccountId`).

- **Данные:** `GET /api/accounts` (список, `followers`, `hasSession`, `proxyId`, `sectionId`, `status`), `GET /api/triggers` (кампании + `fireCount` + `stats` + `responder.status`), `GET /api/sections`, `GET /api/settings` (для `draftCount`/контекста `SecurityBadge`). Создание — `POST /api/triggers` (по одному `TriggerRule` на выбранный аккаунт).
- **Состояния:** загрузка — скелет карточек; пусто — «+ Аккаунт» крупно; ошибка сети — баннер «не удалось загрузить, повторить».
- **Что меняется при переходе на эмуль:** UI не меняется. Под капотом действия кампаний исполняет **браузерный воркер** (§4), а не Python. `SecurityBadge`/статус «жива ли сессия» опирается на `browserState` вместо `sessionData` (правка `lib/safety.ts` + `/api/accounts` поле `hasSession = Boolean(browserState || sessionData)`).

### 7.2 Аккаунты (`/accounts`)
- **Маршрут/меню:** `/accounts`, пункт «Аккаунты» (иконка `Users`).
- **Назначение:** добавление, вход и управление ОСНОВНЫМИ (RESPONDER) аккаунтами — теми, что шлют директ/лайк/подписку.

**Структура (сверху вниз):**
1. **`PageHeader`** «Аккаунты».
2. **`SectionBar`** — фильтр по разделам/подразделам.
3. **Список карточек** (строится ПРЯМО из `/api/accounts`, НЕ из Zustand — это был баг «аккаунты не показывались», см. CLAUDE.md 2026-07-06). Карточка: @username, статус, чип прокси, `SecurityBadge`, «Открыть лог» (`LogModal`, фильтр по accountId). Клик → `AccountDetailModal`.
4. **Кнопки** «+ Аккаунт» (открывает `AddAccountModal`) и «Импорт списком» (`ImportCookiesModal`).

**`AccountDetailModal` (детали аккаунта):** спарклайн прироста подписчиков (`followersHistory`), разбивка действий (`stats` кампаний), «кампаний активно X/Y», «срабатываний», список кампаний, смена прокси/раздела (`select`), пауза/возобновление, «Сбросить снапшот» (заново набрать базлайн, чтобы не разослать всем старым), удаление. Шапка модалки — тоже «Открыть лог».

**`AddAccountModal` (единый попап, §single-add-account-modal — НЕ форкать):**
- Режимы входа: **«Логин/Пароль»** (теперь = браузерный вход §4.5) и **«Куки»** (импорт готовой сессии/`storageState`).
- Поля: username, password, 2FA-ключ TOTP (опц.), **почта + пароль почты (опц.) — для авточтения кода checkpoint по IMAP** (§4.5 п.5), прокси (переключатель **Авто из пула** / **Уникальный**; пусто в авто = подберётся из пула).
- При checkpoint/2FA сервер отдаёт **202** → модалка показывает шаг ввода кода (numeric, куда отправлен код — почта/SMS + маскированный контакт), «Подтвердить»/«Назад», «Отправить ещё раз / сменить канал». Это отлаженный 202-флоу — не сломать (см. `single-add-account-modal`).
- Переиспользуется на 4 точках: Аккаунты · Триггеры «+ Аккаунт» · Прокси «Новый аккаунт» · Черновые (с `role="HELPER"`).
- **Изменение при переходе:** подсказка «вход идёт как настоящий браузер (медленно, по-человечески)»; почта нужна для авто-подтверждения кода. Умная вставка «логин пароль 2FA» и подбор прокси остаются.

**`ImportCookiesModal` («Импорт списком»):** массовый вход. Два режима: **Куки/Сессия** (одна строка = одна `storageState`/куки-строка — сюда же вставляется вывод локального `tools/cookie-gen/`) и **Логин/Пароль** (`логин пароль почта пароль-почты [2FA-ключ]`). Паузы между строками (20–45с password / 5–15с куки — не «ферма»). Строки «ждут код» дожимаются полем ввода прямо под результатами (challenge/2FA не теряется). `lockedRole` фиксирует роль пачки.

- **Данные:** `GET /api/accounts`, `POST /api/accounts/auth` (вход → 202 при коде), `POST /api/accounts/auth/challenge` (код), `POST /api/accounts/auth/resend` (повтор/канал), `POST /api/accounts/import` (массово), `PATCH/DELETE /api/accounts/[id]`, `GET /api/sections`, `GET /api/proxies`.
- **Состояния:** загрузка — скелет; пусто — «+ Аккаунт» + пояснение (если все аккаунты черновые — не предлагать «перезагрузить»); вход — прогресс без «бесконечной загрузки» (клиентский таймаут); ошибка входа — понятный текст (bad_password / checkpoint / suspended / прокси).
- **Что меняется при переходе на эмуль:** «Логин/Пароль» и «Куки» идут через **браузерный воркер** (`browserLogin`/`submitCheckpoint`), сохраняется `browserState` + `loginMethod='browser'` + (опц.) `emailLogin/emailPassword`. Формы 202/ошибок сохраняются, чтобы UI не переписывать.

### 7.3 Парсинг / Черновые (`/drafts`) — содержимое и название зависят от `parsingSource`
- **Маршрут/меню:** `/drafts`. **Название пункта динамическое** (сейчас в `TopNav.tsx` захардкожено «Парсинг (API)» + иконка `Radar` — сделать зависимым от `/api/settings`): режим `api` → «Парсинг (API)» (`Radar`); режимы `drafts`/`drafts_then_api` → «Черновые аккаунты» (`UsersRound`/`Layers`). Вкладку НЕ удалять — пользователь просил оставить, скрывать управление только контентом (см. память `parsing-via-scraper-api` и запрос «оставь вкладку но добавь галочку»).
- **Назначение:** статус/настройка источника парсинга. В API-режиме — статус скрейпера; в draft-режимах — управление черновыми (HELPER) аккаунтами.

**Режим `api` (по умолчанию, уже сделано — `ParsingStatus`):**
- `PageHeader` «Парсинг (API)» → карточка статуса «Скрейпер-API работает / подключён / ошибка / не подключён» (цвет+бейдж), кнопка «Проверить связь» (`/api/scraper-health?test=1`), ссылка hikerapi.com. Ниже — «Как это работает»: что каким триггером закрывается (Подписка/Комментарий/Лайк → API, Сторис → сам основной).
- Управления черновыми НЕТ.

**Режимы `drafts` / `drafts_then_api` (вернуть из истории):**
- Вверху (только `drafts_then_api`) — **баннер статуса API** («используется как запасной, если черновой умрёт»).
- Список карточек ЧЕРНОВЫХ (HELPER): @username, статус, чип прокси, «последний парсинг» (время), «Сбросить снапшот», «Удалить». Клик — детали.
- Кнопки «Добавить» (`AddAccountModal role="HELPER"`, вход браузером) и «Импорт списком» (`ImportCookiesModal lockedRole`).
- **Что делает черновой:** логинится браузером (свой прокси, свой `browserState`), парсит подписчиков/комменты/лайки через DOM (`/parse/*` браузерного воркера, §4.4). Т.е. черновые = «всё через эмуль», как основные, только роль HELPER и назначение — парсинг.

- **Данные:** `GET /api/settings` (режим), `GET /api/scraper-health` (статус API), `GET /api/accounts?role=HELPER` (черновые), те же auth/import endpoints, что у основных.
- **Состояния:** загрузка статуса — спиннер на кнопке; API не подключён — красная карточка + подсказка про `HIKER_API_KEY`; в draft-режимах пусто — «Добавить черновой».
- **Что меняется при переходе на эмуль:** появляется ветвление по `parsingSource`; draft-контур возвращается из коммита `da71ede~1` (=`0fc6134`, файл `poll/route.ts`) и переводится с instagrapi на браузерный `/parse/*`. Меню и контент реагируют на настройку (TopNav подтягивает `/api/settings`).

### 7.4 Прокси (`/proxy`)
- **Маршрут/меню:** `/proxy`, пункт «Прокси» (иконка `Globe`).
- **Назначение:** пул прокси + здоровье каждого + привязка к аккаунтам. Критичная вкладка: качество прокси — топ-фактор входа/бана.

**Структура (сверху вниз):**
1. **`PageHeader`** «Прокси».
2. **Строка-сводка (`StatCard`):** Пуловые · Приватные · Свободно в пуле (с `Hint`-подсказками про цвета).
3. **«Здоровье пула»** — счётчики: ✅ годных · 🔴 датацентр/VPN · 🚫 бан Instagram · ⚪ не отвечают · ❔ не проверены. Кнопка **«Проверить все»** (параллельность 4, живой прогресс `N/M`, сохраняет здоровье в БД).
4. **«Добавить прокси»** (свёрнуто, «+»): переключатель **в пул / приватные**; авто-определение схемы (http → socks5 → socks4). «Проверить список прокси» (пачкой, без сохранения — для отбора перед покупкой): таблица `Прокси · IP · Страна · Провайдер · Вердикт` + «годных из N».
5. **Две колонки: «Пуловые» и «Приватные».** Карточка прокси: адрес, **статус-бейдж** (✅ резидент/мобильный · 🔴 датацентр/VPN · 🔴 не отвечает · 🚫 бан Instagram · не проверен), **«Проверить IP»** (исходящий IP · страна · ISP · вердикт + чип схемы SOCKS если не http), привязанные аккаунты (чипы с «×» — отвязать), «Привязать аккаунт» (пикер) / «Новый аккаунт» (`AddAccountModal` с зафиксированным прокси).
6. **Настройка «аккаунтов на один пуловый прокси»** (свёрнуто внизу; дублирует Настройки).

- **Данные:** `GET /api/proxies` (+поля здоровья), `POST /api/proxies` (`kind`), `PATCH /api/proxies/[id]` (attach/detach), `POST /api/proxies/check` (`{proxyId}` — сохраняет; `{url}` — только проверка). Проверка живого прокси снимает ошибочный `igBlocked` («второй шанс»).
- **Состояния:** загрузка — карточки с плейсхолдером здоровья; «Проверить все» — прогресс-бар `N/M`; проверка одного — спиннер на кнопке.
- **Изменение при переходе на эмуль:** прокси теперь применяется **на контекст браузера** (`newContext({proxy})`), а не только в instagrapi. Добавить: (а) явную подсказку «датацентр и shared-резидентка (напр. CROCKER) IG банит — бери мобильные / чистые резидентные в стране аккаунта»; (б) кнопку **«Удалить все датацентр/выжженные»** (массовая чистка пула — снимает боль «прокси пишутся забаненными»); (в) при браузерном входе IP-бан фиксируется так же (`igBlocked`), плюс можно проверять исходящий IP ИЗНУТРИ браузера (заход на ipapi изнутри контекста) — точнее, чем внешняя проверка.

### 7.5 Статистика (`/stats`)
- **Маршрут/меню:** `/stats`, пункт «Статистика» (иконка `BarChart3`).
- **Назначение:** сводные метрики и журнал событий по всем аккаунтам.

**Структура (сверху вниз):**
1. **`PageHeader`** «Статистика».
2. **Метрики (`StatCard`, с `Hint`):** Аккаунты · Подписчики · Активные кампании · Срабатывания · Выполнено действий (≠ срабатывания) · Ошибки.
3. **Разбивки:** срабатывания по типу кампании (Подписка/Комментарий/Лайк/Сторис), топ аккаунтов по подписчикам.
4. **Журнал последних событий** (`/api/logs`, ~80 записей): уровень (✓/⚠/✕/ℹ), время, текст.

- **Данные:** `GET /api/accounts`, `GET /api/triggers`, `GET /api/logs`, `GET /api/browser-health`, `GET /api/scraper-health`.
- **Состояния:** загрузка — счётчики-плейсхолдеры; пусто — «пока нет данных».
- **Изменение при переходе на эмуль:** добавить блок **«Здоровье автоматизации»**: статус браузерного воркера (`/api/browser-health` → build/playwright/chromium/занятость), статус API (`/api/scraper-health`), сколько аккаунтов с **живой `browserState`**, сколько на паузе/челлендже/бане. Это даёт пользователю одну панель «всё ли работает».

### 7.6 Настройки (`/settings`)
- **Маршрут/меню:** `/settings`, пункт «Настройки» (иконка `Settings`).
- **Назначение:** глобальные настройки владельца + справка.

**Структура (сверху вниз):**
1. **`PageHeader`** «Настройки».
2. **«Работать без прокси»** (тумблер `allowNoProxy`, красная подсказка «высокий риск бана»).
3. **НОВОЕ — «Источник парсинга»** (радио `parsingSource`, 3 варианта = ровно то, что просил пользователь про «галочки»):
   - ○ **«Только API»** (`api`, по умолчанию) — подписчиков/комменты/лайки читает HikerAPI. Черновые НЕ используются, вкладка становится «Парсинг (API)». Это галочка «использовать апишку» во ВКЛ.
   - ○ **«Только черновые»** (`drafts`) — парсят черновые аккаунты (браузером). API не используется. Вкладка = «Черновые аккаунты».
   - ○ **«Черновые + API запасной»** (`drafts_then_api`) — черновые основной источник, НО если у владельца нет живого чернового → автоматически HikerAPI. Это галочка «использовать апишку как запасной вариант». Вкладка = «Черновые аккаунты» + баннер про API-fallback.
   - Под радио — краткое пояснение выбранного режима + предупреждение «черновым нужны свои прокси и они несут риск бана; API — платный за запрос (нужен `HIKER_API_KEY`)».
4. ~~«Движок действий» (радио `actionEngine`)~~ — **УБРАНО 2026-07-09(3)** по запросу пользователя: выбор legacy больше не нужен пользователю (браузерный воркер живой в проде), `resolveEngine` всегда возвращает `'browser'` (кроме случая, когда воркер физически не задеплоен — инфраструктурная страховка, не выбор). Опередили Фазу 5 в этой части.
5. **(Опц., только локальная отладка) «Показывать окно браузера»** (`browserHeadful`) — не для прода.
6. **«Аккаунтов на один пуловый прокси»** (число `accountsPerProxy`).
7. **Справка «Что где находится»** (свёрнуто по умолчанию) — обновить под новые вкладки/режимы (эмуль, источники парсинга).

- **Данные:** `GET/PATCH /api/settings` (расширить полями `parsingSource`, `actionEngine`, `browserHeadful`; `allowNoDrafts`/`likeByDraft`/`storyByDraft` — LEGACY).
- **Изменение при переходе на эмуль:** LEGACY-тумблеры «Работать без черновых» и «Кто что делает» (лайк/сторис черновым) — **скрыть/удалить** (не нужны: разделение действий было костылём Python-эры). Их поля в БД остаются no-op ради обратной совместимости.

### 7.7 Логин/Регистрация приложения (`/login`, `/register`)
- **Маршрут:** `(auth)/login` → `/login`, `(auth)/register` → `/register`. Публичные (в `PUBLIC_PATHS` middleware). Это вход в САМ сервис (ReactiveGram), не в Instagram.
- **Структура:** email + пароль (bcrypt + JWT-кука), демо-байпас (`demo@instaguard.com` / `demo1234`), ссылки логин↔регистрация, кнопка Google (`GoogleButton`).
- **Данные:** `POST /api/auth/login`, `POST /api/auth/register` (+`/verify`, `/resend`), `POST /api/auth/logout`, `/api/auth/google`.
- **Что меняется при переходе на эмуль:** ничего. ⚠️ Регистрация-с-кодом (Resend) + Google-вход — **паузная auth-работа, в рабочем дереве, но НЕ закоммичена, ждёт ключей** (см. память `auth-work-paused`). НЕ трогать и НЕ коммитить в рамках перехода на эмуль. Коммитить только файлы своей задачи.

---

## 8. Фазы внедрения (порядок работ)

### Фаза 1 — Браузерный вход (критично, разблокирует аккаунты) ✅ приоритет
- [x] Каркас `workers/browser/` (Node+Playwright+stealth, Dockerfile, `/health`, секрет). Задеплоен, `/health` живой.
- [x] `/login` + `/login/checkpoint` + `/session/test` (анти-детект §4.3, человековвод, storageState).
- [x] Схема: `browserState`, `loginMethod`, `emailLogin/emailPassword`, миграция.
- [x] `lib/browser/client.ts` (login/checkpoint/test). 2026-07-09(2): фикс — принимает `BROWSER_URL` как алиас `BROWSER_WORKER_URL`.
- [x] `accounts/auth` + `challenge` + `AddAccountModal` → браузерный вход, сохранение `browserState`.
- [x] `app/api/browser-health` + деплой воркера. Переменная на Railway была `BROWSER_URL` (не канонично) — покрыто фолбэком, см. журнал.
- [x] Импорт (логин/пароль и куки) в `accounts/import` engine-aware (`browserLoginByCookies` для куки-режима).
- [x] (Опц., параллельно) Локальный `tools/cookie-gen/` (§2.1): headful-Playwright на ПК → `storageState` для «упрямых» аккаунтов. Не блокирует облачный путь. СДЕЛАНО 2026-07-09(7) — см. журнал (было отмечено в CLAUDE.md, но не здесь; чекбокс расходился с фактом).
- **Тест (осталось выполнить):** реальный аккаунт входит через браузерный воркер с чистым прокси на ПРОДЕ после редеплоя с фиксом переменной — см. журнал 2026-07-09(2).
- **Тест:** реальный аккаунт (напр. `evelinepqzg`) входит через браузерный воркер с чистым прокси; checkpoint по коду проходит; `browserState` сохранён; `/session/test` подтверждает. Плюс: `storageState`, полученный локально, импортируется и `/session/test` его подтверждает.

### Фаза 2 — Браузерные действия (директ в первую очередь)
- [x] `/dm`, `/follow`, `/like`, `/stories`, `/comment`, `/reply-comment` — воркер реализует все шесть. `poll/route.ts` поток комментариев теперь зовёт браузер для browser-only аккаунтов (`postUrl` строится из `media_id` через `lib/instagram/shortcode.ts`, алгоритм instagrapi `pk2code` — HikerAPI не отдаёт готовый URL, но отдаёт media pk, этого достаточно). Реплай браузером = top-level коммент (не тред) — ограничение `workers/browser/lib/actions.js`, задокументировано. Лайк КОНКРЕТНОГО коммента (LIKE_COMMENT) браузером не поддержан (нужен клик по нити) — честная ошибка. Фото-в-директ браузером — всё ещё НЕ сделано.
- [x] `poll/route.ts` + `instrumentation.ts` → поток подписчиков/лайкнувших/комментариев идёт в браузерный воркер, `storageState` сохраняется после каждого действия (guard `engine==='browser' && browserState`, legacy не тронут).
- [x] Дневные лимиты/прогрев/паузы — переиспользованы без изменений (`lib/limits.ts`).
- [ ] (Опц.) `mail-reader` — авточтение кода checkpoint по IMAP из `emailLogin`. НЕ сделано.
- **Тест (осталось выполнить):** кампания «Подписка → Директ» реально отправляет DM браузером новому подписчику; лайк/подписка/сторис/коммент исполняются; закрытая личка → fallback follow+like; бан/челлендж → пауза. Нужен реальный аккаунт на проде.

### Фаза 3 — Микс парсинга (API + черновые) + настройки
- [x] Схема `parsingSource`, `actionEngine`; `api/settings` (GET/PATCH, оба поля).
- [x] Вернуть draft-контур в `poll` под `parsingSource`; черновые парсят через `/parse/*`. СДЕЛАНО (2026-07-09(4)): воркер имеет `/parse/followers|following|comments|likers` (`workers/browser/lib/parse.js`); `lib/browser/client.ts` — `parseFollowersBrowser/…`; `poll/route.ts` — `parseFollowersFor/parseCommentsFor/parseLikersFor` ветвятся по `parsingSource` (`api` → HikerAPI; `drafts` → черновой браузером, пусто если чернового нет; `drafts_then_api` → черновой, при отсутствии → HikerAPI). Подбор чернового — `lib/browser/draftPool.ts` (round-robin по `lastChecked`, живой `browserState`).
- [x] Настройки: радио «Источник парсинга» (`settings/page.tsx`). [x] TopNav реагирует на режим (`Черновые аккаунты`/`Парсинг (API)` по `parsingSource`). [x] `drafts/page.tsx` реагирует (2026-07-09(4)): тянет `/api/settings` → при `drafts`/`drafts_then_api` рендерит `DraftsManager` (список HELPER + «Добавить»/«Импорт списком», управление прокси/снапшотом), иначе `ParsingStatus` (API-статус).
- [x] Стори-события основным (браузер) — СДЕЛАНО 2026-07-09(8): `workers/browser/lib/actions.js readStoryEvents` читает `/api/v1/direct_v2/inbox/`+`/pending_inbox/` изнутри залогиненной страницы (fetch+`x-ig-app-id`, надёжнее DOM); роут `/story-inbox`; `lib/browser/client.ts browserStoryEvents`; `poll/route.ts` ветвится `useBrowserForStories` (browserState) → браузер, иначе legacy `getStoryEvents`. Форма события совпадает с Python. ⚠️ ЭКСПЕРИМЕНТАЛЬНО — не прогнано на живом IG (нужен рабочий прокси).
- **Тест:** переключение режимов; в `drafts_then_api` смерть чернового → переход на API; вкладка «Черновые» появляется/скрывается. Вкладка/меню/ветвление poll реализованы (2026-07-09(5)). Draft-парсинг браузером (`/parse/*`) реализован, но ЭКСПЕРИМЕНТАЛЬНЫЙ — не прогнан на живом Instagram (DOM-селекторы могут требовать правки). Режим `api` — рабочий дефолт.

### Фаза 4 — Полировка и надёжность («до идеала»)
- [x] Селекторы в конфиг с фолбэками (RU/EN, роли, aria-label), СКРИН при сбое в лог/UI. СДЕЛАНО 2026-07-09(5-8): `selectors.js` (расширенные варианты), `login.js captureDiag/fail` → скрин доходит до модалки. Плюс баги входа из аудита (отправка кода, 2FA/challenge, таймауты).
- [x] Стабильный отпечаток на аккаунт (UA/viewport детерминированно по username) — `fingerprint.js`. [x] Гео-локаль ИЗ ПРОКСИ — СДЕЛАНО 2026-07-09(10): `lib/browser/geo.ts` (страна прокси → locale/timezoneId, аналог Python `_locale_for_proxy`), `InstagramAccount.locale/timezoneId` (миграция `20260709010000_account_locale`) хранят отпечаток аккаунта, вычисляется при входе/импорте из страны подобранного прокси (`PoolPick.country`) и передаётся браузерному воркеру при КАЖДОМ вызове (вход/checkpoint/действия/парсинг), а не только при входе — иначе отпечаток «прыгал» бы на дефолт между сессиями.
- [x] Реюз браузера (`getBrowser` singleton), ограничение конкуренции (`runLimited`, `BROWSER_CONCURRENCY`), бэкофф/ретрай на сетевых сбоях (`gotoResilient`). Плюс 2026-07-09(8): мёртвый прокси → быстрый явный отказ `proxy_dead` (~8с, параллельный детект схемы), не зависает до таймаута.
- [x] «Здоровье автоматизации» на Статистике — СДЕЛАНО (`stats/page.tsx`: статус браузерного воркера через `/api/browser-health?test=1` — Chromium/загрузка/build + скрейпер-API через `/api/scraper-health?test=1`). Массовая чистка прокси на вкладке Прокси — СДЕЛАНО (2026-07-07(10), «Проверить все»). Проверка прокси переведена с мёртвого Python на браузерный воркер (2026-07-09(8)).
- [x] Прогрев ленты (`human.js warmupFeed`) + человекоподобные паузы/скролл/мышь (`humanType`, `idleMouse`, `jitter`).
- **Тест (осталось, БЛОКИРОВАН рабочим прокси):** прогон 5–10 аккаунтов сутки без банов; устойчивость к смене вёрстки IG. Реальный прогон невозможен, пока у пользователя мёртвые прокси (TCP_INVALID) — нужен рабочий резидентный прокси.

### Фаза 5 — Вывод legacy
- [x] Убрать `actionEngine='legacy'` — сделано 2026-07-09(3), опережая фазу (пользователь попросил раньше срока): `resolveEngine` больше не читает настройку, UI-переключатель удалён из `settings/page.tsx`. Поле в схеме осталось no-op.
- [ ] Удалить/заархивировать Python instagrapi-воркер, чистка мёртвого кода и LEGACY-полей (`sessionData`, `allowNoDrafts`, `likeByDraft`, `storyByDraft`, `actionEngine` из схемы). НЕ сделано — оставлен как аварийный резерв, пока не подтверждена стабильность браузерного движка на реальных аккаунтах.
- **Тест:** полный цикл только на браузере + API; `tsc`/`build` чисто.

---

## 9. Чек-лист тестирования (сквозной)
- [ ] Вход: успех / неверный пароль / checkpoint-код / 2FA / suspended — все ветки дают понятный результат.
- [ ] Сессия переживает перезапуск воркера (storageState в БД, не только в памяти).
- [ ] Прокси на контекст реально применяется (проверить исходящий IP изнутри браузера).
- [ ] Действия: DM/лайк/подписка/сторис/коммент выполняются и логируются; счётчики `stats` растут.
- [ ] Лимиты/прогрев/паузы соблюдаются; бан/челлендж → аккаунт на паузу, без долбёжки.
- [ ] Парсинг во всех трёх режимах `parsingSource`.
- [ ] RAM/CPU воркера в пределах Railway при `BROWSER_CONCURRENCY`.
- [x] `tsc --noEmit` + `next build` чисто; воркер стартует; `/health` отдаёт свежий build. Подтверждено 2026-07-09(14): публичный `GET https://amiable-laughter-production-2857.up.railway.app/health` → `build:"2026-07-09-browser-15-emailfield"`, `headful:true`, `chromium:"130.0.6723.31"`. Остальные пункты чек-листа — ниже, требуют живого аккаунта.

---

## 10. Риски и как гасим
| Риск | Митигейшн |
|---|---|
| Playwright тяжёлый на Railway (RAM) | `BROWSER_CONCURRENCY=1–2`, headless, закрывать контексты, следить за метриками; при нехватке — апгрейд плана или локальный вход |
| IG меняет вёрстку → падают селекторы | селекторы с фолбэками + скрин в лог + понятная ошибка, не роняющая весь цикл |
| Checkpoint без доступа к почте | сохранять `emailLogin/emailPassword` при покупке → IMAP-автокод; иначе ручной ввод |
| Бан при массовых действиях с одного IP/устройства | стабильный отпечаток, паузы, прогрев по возрасту, дневные лимиты, 1 прокси = немного аккаунтов |
| Стоимость HikerAPI при частом поллинге | настраиваемый интервал/глубина парсинга (Фаза 4) |
| Потеря паузной auth-работы | коммитить точечно, не `git add -A` |

## 11. Переменные окружения (новые)
```
BROWSER_WORKER_URL        # URL браузерного воркера (Railway)
BROWSER_WORKER_SECRET     # секрет; в воркере — тот же
BROWSER_CONCURRENCY=2     # параллельных контекстов
HIKER_API_KEY             # парсинг (уже есть)
# ENCRYPTION_KEY — используется и для emailPassword
```

## 12. Открытые вопросы (решить по ходу)
- IMAP firstmail: подтвердить доступ/хост для авточтения кода (иначе — ручной ввод). Спрошено пользователя 2026-07-09 — ответ «не сейчас, пока вводить код вручную» (ручной ввод и так уже работает через модалку). §337 сознательно НЕ делаем, пока не попросят явно.
- Desktop web vs mobile web (`instagram.com` vs эмуляция мобильного устройства) — начать с desktop web; при слабой доставке DM попробовать mobile web (device descriptor).
- Хранить ли `browserState`/`sessionData` зашифрованными — **уточнение 2026-07-09(13): формулировка «как sessionData» была неверной, `sessionData` НИКОГДА не шифровался** (`ENCRYPTION_KEY` был задан на Railway с самой первой записи про почту, но код шифрования нигде не существовал — проверено grep'ом по всему репо). `emailPassword` теперь шифруется (`lib/crypto.ts`, AES-256-GCM) — это было безопасно сделать точечно, т.к. поле пока нигде не читается обратно (mail-reader §337 не реализован). `browserState`/`sessionData` — сознательно НЕ трогал: требует смены типа колонки Json→String и правки каждой точки чтения на ЖИВОМ пути входа/действий (poll/route.ts, instrumentation.ts, lib/browser/actions.ts, туда-обратно к воркеру) — риск регресса именно сейчас, когда вход только начал стабильно работать, не оправдан. Решение по ним по-прежнему открыто.
- Держать ли постоянный warm-браузер на аккаунт (быстрее, но дороже по RAM) — решить в Фазе 4 по метрикам.

---

## Журнал внедрения (агенты дописывают снизу)
- 2026-07-09 — план создан. Текущее состояние: парсинг через HikerAPI внедрён (Фаза 3 частично, режим `api`); вход/действия ещё на legacy instagrapi (Python-воркер) — Фазы 1–2 не начаты.
- 2026-07-09 — **Воркер задеплоен и подтверждён живым в проде** (Railway-сервис `amiable-laughter`, `/health` → `build:2026-07-09-browser-1`, Chromium 130, concurrency 2). Root Directory сервиса = `workers/browser`, сборка по Dockerfile. Остаётся задать `BROWSER_WORKER_URL` на Next.js-сервисе, чтобы `resolveEngine`='browser'. **Фаза 4 (частично): панель «Здоровье автоматизации» на Статистике** — статус браузерного воркера (`/api/browser-health?test=1`) + скрейпер-API (`/api/scraper-health?test=1`) прямо в приложении. `tsc`+`build` чисто.
- 2026-07-09 — **Фаза 3 (частично): UI переключателей в Настройках.** Добавлены радио «Движок входа и действий» (`actionEngine`: browser/legacy — РАБОЧИЙ, читается `resolveEngine`) и «Источник парсинга» (`parsingSource`: api/drafts/drafts_then_api). Роут `/api/settings` уже принимал эти поля. ⚠️ Draft-режимы парсинга помечены в UI как «дорабатываются»: `poll/route.ts` пока ВСЕГДА парсит через HikerAPI и не читает `parsingSource` — контур парсинга черновыми (возврат из `da71ede~1`, перевод на браузерный `/parse/*`) НЕ сделан (остаток Фазы 3). Также ещё не сделано: динамический пункт меню `TopNav` (Парсинг API ↔ Черновые) по режиму, стори-события основным через браузер. `tsc`+`build` чисто.
- 2026-07-09 — **Фаза 2 (действия), ядро: поток «подписчики/лайкнувшие → директ/подписка/лайк/сторис» переведён на браузерный воркер.** Новый `lib/browser/actions.ts` `runFollowerActionsBrowser(job)` — чистый исполнитель (по username через `/dm|/follow|/like|/stories`), возвращает счётчики + финальный `browserState` + `brk` (стоп при мёртвой сессии). Подключён в ОБЕИХ точках: `poll/route.ts` `runFollowerActionsInline` и dm-очередь `instrumentation.ts` — строго под guard `job.engine==='browser' && job.browserState` (legacy-путь не тронут). После действий `browserState` сохраняется в аккаунт (сессия дозревает). `poll` теперь обрабатывает и browser-only аккаунты (нет `sessionData`, но есть `browserState`): правки `workingMains`/skip-guard; `getAccountInfo`/стори-инбокс/поток комментов идут только при наличии `sessionData` (legacy-чтение). ОТЛОЖЕНО (Фаза 3/4, с явными комментами в коде): (а) поток КОММЕНТОВ через браузер — reply требует `postUrl` из `media_id` (у HikerAPI его нет); (б) чтение стори-инбокса основного через браузер; (в) фото в директе через браузер. Проверено: `tsc --noEmit` + `next build` чисто. ⚠️ Как и Фаза 1 — работает только после ДЕПЛОЯ браузерного воркера (`BROWSER_WORKER_URL`); без него `resolveEngine`='legacy' и всё идёт по-старому.
- 2026-07-09 — **`accounts/import` переведён на engine-aware** (был пробел: одиночный `auth` уже шёл через браузер, а массовый импорт — всё ещё legacy instagrapi, хотя пользователь входит именно импортом). Теперь при `engine==='browser'` импорт логин/пароль зовёт `browserLogin`, куки/сессия — `browserLoginByCookies`, сохранение через `persistInstagramAccount` (browserState + loginMethod). Код (checkpoint/2FA) отдаётся как `needsCode` (дожимается в модалке). Дополнительно: из строки «логин пароль почта почта-пароль» извлекаются `emailLogin/emailPassword` для будущего IMAP-автокода (plan §4.5). Legacy-путь сохранён как fallback (когда браузерный воркер не задеплоен → `resolveEngine`='legacy'). Проверено: `tsc --noEmit` + `next build` чисто. ⚠️ Чтобы браузерный вход РЕАЛЬНО заработал — нужно ЗАДЕПЛОИТЬ браузерный воркер (`workers/browser`) отдельным Railway-сервисом и задать `BROWSER_WORKER_URL`/`BROWSER_WORKER_SECRET`; без URL всё безопасно падает в legacy. Дальше по плану: Фаза 2 — `poll/route.ts` + `instrumentation.ts` слать действия в браузерный воркер (эндпоинты воркера `/dm|/follow|/like|/stories|/comment` уже готовы).
- 2026-07-09 (4) — **Выбор движка убран по запросу пользователя** (опережает Фазу 5): `resolveEngine` всегда `'browser'` (кроме `!browserConfigured()` — страховка на недеплой воркера, не выбор), радио в Настройках удалено. Заодно задан `HIKER_API_KEY` на Railway (`gallant-generosity`) — скрейпер-API парсинга должен заработать после редеплоя. `tsc`+`build` чисто.
- 2026-07-09 (9) — **Проверка прокси снята с мёртвого Python + стори-события браузером + Фаза 4 закрыта.** (1) Пользователь: внешний чекер видит прокси живым, наш — «не отвечает» + Railway-404 «Application not found» → `/api/proxies/check` и `pickPoolProxy` звали НЕсуществующий Python-воркер. Переведено на браузерный воркер: новые `/check-proxy`+`/pick-proxy` (проверяют своим Chromium через ipapi.is), `browserCheckProxy`/`browserPickProxy`. Детект схемы прокси теперь через НЕЙТРАЛЬНЫЙ ipify (не Instagram) → живой прокси не метится мёртвым из-за капризов IG. (2) **Стори-события основного браузером (§344 закрыт)** — `readStoryEvents` через веб-приватный API инбокса. (3) **Фаза 4 полностью [x]:** «Здоровье автоматизации» на Статистике уже было; селекторы+скрин, отпечаток, реюз/бэкофф, прогрев — подтверждены. Билды `browser-8-proxycheck`→`browser-9-storyinbox`. `node --check`+`tsc` чисто, коммиты точечные. **⚠️ Последняя живая зависимость от Python убрана — теперь Python-воркер не вызывается ниоткуда на рабочих путях (только dormant legacy для аккаунтов с sessionData). ОСТАЛОСЬ: §357 (архивация Python + чистка LEGACY-полей) — сознательно НЕ делаем, пока браузерный вход не подтверждён на живом аккаунте (нужен рабочий прокси); опц. §329 cookie-gen, §337 IMAP-автокод, §334 фото-в-директ. Сквозной тест §9 — блокирован рабочим прокси.**
- 2026-07-09 (8) — **Вход починен на уровне инфры + кода (Фаза 4 существенно закрыта).** Цепочка: (1) воркер в проде бежал HEADLESS → Instagram отдавал бот-стену без формы (корень «страница открылась, полей нет») → перевёл на HEADFUL под Xvfb (`browser.js getBrowser` headful по умолчанию, `start.sh` поднимает `Xvfb :99`, Dockerfile ставит `xvfb` через apt, `railway.json startCommand=sh start.sh`). ⚠️ Первый заход через `xvfb-run` уронил воркер в 502 (его нет в образе) — решено прямым Xvfb+guard. `/health` подтверждает `headful:true`,`display:":99"`. (2) СКРИН при неудаче доведён до модалки (`captureDiag/fail`→`server.js diag`→`client.ts`→`accounts/auth screenshot`→`AddAccountModal`). (3) Аудит кода входа нашёл реальные баги — исправлены: `codeSubmit` содержал CSS в текстовом матчере → кнопка кода не жалась (новый `submitCodeForm`: CSS→текст→Enter); challenge по почте/SMS путался с 2FA (2FA теперь строго по URL+authenticator-фразам); бюджет входа мог превысить клиентский таймаут (подрезаны таймауты `findLoginForm`/scheme-детект, клиент 120→180с). (4) Мёртвый прокси → быстрый явный `proxy_dead` (~8с, параллельный `Promise.any` детект схемы) вместо зависания. Билды `browser-4-headful`…`browser-7-fastproxy`, все подтверждены живыми. Проверено: `node --check` + `tsc --noEmit` чисто. Коммиты точечные (auth-работа не тронута). **⚠️ БЛОКЕР входа — прокси пользователя мёртвые (TCP_INVALID в его же сетевом логе), не код.** Нужен рабочий резидентный прокси, чтобы прогнать сквозной тест. **Осталось:** стори-события браузером (Фаза 3 §344); «Здоровье автоматизации» на Статистике (Фаза 4 §351); вывод legacy Python (Фаза 5 §357); опц. cookie-gen/IMAP/фото-в-директ; сквозной тест-чеклист §9 (блокирован рабочим прокси).
- 2026-07-09 (5) — **Фаза 3 ЗАВЕРШЕНА (микс парсинга API + черновые).** Достроен последний крупный кусок: (1) браузерный воркер получил эндпоинты `/parse/followers|following|comments|likers` (`workers/browser/lib/parse.js`, node --check чисто); (2) `lib/browser/client.ts` — обёртки `parseFollowersBrowser/parseFollowingBrowser/parseCommentsBrowser/parseLikersBrowser`; (3) `lib/browser/draftPool.ts` (новый) — подбор живого HELPER-чернового (round-robin по `lastChecked`, только с `browserState`); (4) `poll/route.ts` — `parseFollowersFor/parseCommentsFor/parseLikersFor` ветвятся по `parsingSource` владельца: `api`→HikerAPI, `drafts`→черновой браузером (пусто, если чернового нет), `drafts_then_api`→черновой с фолбэком на HikerAPI; `needsScraper`-гейт больше не блокирует владельцев в режиме `drafts`; (5) `drafts/page.tsx` — `DraftsManager` (список HELPER, добавить/импорт/прокси/сброс снапшота) для draft-режимов, `ParsingStatus` для `api` (выбор по `/api/settings`). Схема/миграция уже были (parsingSource в `20260709000000_browser_engine`) — не трогал. Проверено: `tsc --noEmit` + `next build` чисто; воркер `node --check` чисто. Коммит точечный (без паузной auth-работы: schema.prisma/package.json/middleware/auth-файлы НЕ трогал). ⚠️ Draft-парсинг браузером (`/parse/*` через DOM) НЕ прогонялся на живом Instagram — помечен экспериментальным; режим `api` (HikerAPI) остаётся дефолтным и рабочим. **Осталось (Фаза 4/5):** стори-инбокс основного через браузер; полировка селекторов/анти-детекта; вывод legacy instagrapi; опц. `tools/cookie-gen/` и IMAP-автокод.
- 2026-07-09 (3) — **Фаза 2 доведена: комментарии через браузер.** `lib/instagram/shortcode.ts` (media pk → shortcode, алгоритм instagrapi `pk2code`) разблокировал `postUrl` для `/comment`/`/reply-comment` воркера — раньше это было причиной отката (HikerAPI не отдаёт готовый URL поста, только pk). `poll/route.ts`: поток комментариев теперь работает и для browser-only аккаунтов (guard `engine==='browser' && account.browserState`, аналогично потоку подписчиков), покрыты ответ/приглашение/лайк-постов/подписка/директ-с-fallback/сторис; лайк КОНКРЕТНОГО коммента браузером не поддержан (нет нужного эндпоинта в воркере) — честная ошибка вместо тихого пропуска. **Фаза 3 (частично): TopNav динамический** — пункт `/drafts` читает `parsingSource` из `/api/settings`, показывает «Черновые аккаунты»/`UsersRound` при `drafts`/`drafts_then_api`. `tsc`+`build` чисто. **Осталось по Фазе 3 (главное):** `drafts/page.tsx` всё ещё жёстко показывает API-статус независимо от режима (нужно вернуть список HELPER-аккаунтов + добавление в draft-режимах); браузерный воркер не имеет `/parse/followers|comments|likers` — без них draft-парсинг физически невозможен, это следующий крупный кусок; стори-инбокс основного всё ещё только legacy.
- 2026-07-09 (2) — **фикс окружения:** Railway-переменная на Next.js-сервисе была задана `BROWSER_URL` вместо канонической `BROWSER_WORKER_URL` → `browserConfigured()` возвращал `false`, `resolveEngine` молча оставался `'legacy'` несмотря на живой задеплоенный воркер (`/health` подтверждён: `build:2026-07-09-browser-1`, Chromium 130, playwright:true). Фикс — `lib/browser/client.ts` принимает оба имени. Закоммичено и запушено ТОЧЕЧНО (`git add lib/browser/client.ts`, не `-A`) — паузная auth-работа (Google/email-код) в дереве не тронута. `tsc --noEmit` + `next build` чисто. Заодно сверил чек-листы Фаз 1–2 с фактическим кодом — большинство пунктов уже реализовано в предыдущих сессиях, но не было отмечено `[x]`; проставлено. **Следующий шаг:** после того как Railway доразвернёт Next.js с этим коммитом — проверить `/api/browser-health`, затем реальный вход на аккаунте через UI (тест из чек-листа Фазы 1), дальше по плану — comment/reply-comment через браузер и парсинг черновыми (остаток Фазы 2/3).
- 2026-07-09 — план ДОВЕДЁН и сверен с кодом. Проверено по репозиторию: (а) `autofacebook` = черновой Playwright-прототип (stealth импортирован, но НЕ применён; прокси в коде нет; `page.fill` мгновенно — §4.3/§1 подтверждены); (б) draft-контур для возврата лежит в `da71ede~1` (=`0fc6134`, `app/api/poll/route.ts`); (в) `TopNav.tsx` держит захардкоженный пункт «Парсинг (API)» — сделать динамическим (§7.3); (г) `UserSettings` сейчас = `accountsPerProxy/allowNoProxy/allowNoDrafts/likeByDraft/storyByDraft` (последние три — LEGACY), `InstagramAccount` = `sessionData` без `browserState` — расширения §3 корректны; (д) `drafts/page.tsx` = статус-страница API (`ParsingStatus`). ДОБАВЛЕНО в план: §2.1 (решение облако vs локальный `tools/cookie-gen/`, storageState портируется), детальные §7.1–7.7 (структура/данные/состояния/что меняется по каждой вкладке), точная семантика «Источник парсинга» = 3 радио под запрос пользователя (§7.6), задача локального инструмента в Фазе 1. Фазы 1–2 (браузерный вход и действия) по-прежнему НЕ начаты — следующий агент стартует с каркаса `workers/browser/`.
- 2026-07-09 (7) — **Аудит «интерфейс = реальность» + §329 cookie-gen готов.** (Запись дублирует CLAUDE.md — при прошлой сессии журнал `plan.md` не был обновлён, только CLAUDE.md; чек-бокс §329 расходился с фактом, поправлено выше.) Коммиты `924b79a`/`2592382`/`471e12b`: (1) реальное число подписчиков через `scrapeUserInfo` (HikerAPI) вместо мёртвого Python `/account-info` — метрика и спарклайн больше не всегда пустые; (2) проверка прокси (`proxies/check`) больше не гасит живые прокси на сбое САМОГО воркера (только явный `{ok:false}` от него тушит статус); (3) честные тексты: тур/баннер «нет черновых» больше не врут про обязательность черновых (парсинг через API), шапка `TopNav` показывает реальный статус браузерного воркера вместо захардкоженного «Активно», legacy-бейдж `LIKE_COMMENT` помечен; (4) §329 `tools/cookie-gen/` — локальный headful-логин на ПК, экспорт `storageState` для «Импорта». `tsc`/`build`/`node --check` чисто.
- 2026-07-09 (10) — **Гео-локаль отпечатка из страны прокси (Фаза 4 §349, последний «минорный остаток» закрыт).** `workers/browser/lib/fingerprint.js` уже умел принимать `locale/timezoneId` (override), а `server.js` их принимал на `/login`, `/login/cookies` и во ВСЕХ `actionRoute`-эндпоинтах — но Next.js ничего не вычислял и не передавал, поэтому реально всегда шёл дефолт `en-US/America/New_York` независимо от страны прокси (аккаунты id_ID/pt_BR/en_PH логинились с американским отпечатком на польском/etc IP — несовпадение локали и гео-IP, отдельный антибан-сигнал, см. CLAUDE.md 2026-07-07(9)). Сделано: (1) `lib/browser/geo.ts` (новый) — таблица «страна прокси (как отдаёт ipapi.is) → IETF-локаль/IANA-таймзона»; (2) `InstagramAccount.locale/timezoneId` (миграция `20260709010000_account_locale`) — отпечаток закрепляется на аккаунте ПРИ ВХОДЕ и переиспользуется на КАЖДОМ последующем действии/парсинге (не только на входе — иначе отпечаток «плавал» бы между сессиями, тоже сигнал); (3) `lib/proxyPool.ts` `PoolPick` теперь несёт `country` (селект добавлен, все 4 ветки подбора его возвращают); (4) `accounts/auth`, `accounts/auth/challenge`, `accounts/import` вычисляют `geo = localeForCountry(proxyCountry)` и прокидывают в `browserLogin/browserLoginByCookies`, сохраняют через `persistInstagramAccount` (новые необязательные поля `locale/timezoneId` — не затирают уже сохранённый отпечаток, если этот вход гео не определил); (5) все точки, где строится `Ctx` для действий/парсинга/стори-инбокса (`lib/browser/actions.ts`, `poll/route.ts` — job для follower-действий/`bctx()` комментариев/`browserStoryEvents`/`parseXBrowser` для черновых через `draftPool.ts`, `instrumentation.ts` dm-воркер), прокидывают `account.locale/timezoneId` дальше. Ручной прокси без известной страны / незнакомая страна → `geo=null` → воркер берёт прежний дефолт — регрессии нет. Билд воркера → `browser-11-geolocale`. Проверено: `tsc --noEmit` + `next build` + `node --check` (весь `workers/browser/lib/*.js` + `server.js`) чисто. Коммит точечный — НЕ трогал `app/(auth)/*`, `app/api/auth/*`, `lib/auth.ts`, `middleware.ts`, `package.json` (паузная auth-работа) и `workers/browser/lib/login.js` (см. заметку ниже). **⚠️ Обнаружено при подготовке коммита: `workers/browser/lib/login.js` в рабочем дереве оказался модифицирован (устойчивость поиска формы входа — `waitForm`/`LOGIN_FORM_CORE`/переписанный `findLoginForm`) — правка НЕ моя и не была в дереве в начале этой сессии; похоже на параллельную сессию/агента по этому же репо. Я его НЕ трогал и НЕ коммитил (не моя работа, не хочу конфликтовать) — если это чья-то незавершённая правка, она осталась в рабочем дереве нетронутой.** Не тестировано на живом Instagram (нужен рабочий прокси, тот же блокер §357/§9).
- 2026-07-09 (11) — **Фиксы входа по ЖИВЫМ ошибкам пользователя (браузерный движок; headful в проде подтверждён — `/health` `headful:true`, форма ВИДНА на скрине diag → бот-стены нет).** (1) `login.js findLoginForm`: форму ждём нативным `page.waitForSelector(LOGIN_FORM_CORE,{state:'visible'})` до 25с (возврат СРАЗУ, как поле появилось) вместо жёстких ~9с поллинга; фолбэк «Log in» — по ССЫЛКЕ `getByRole('link')`, а не по submit-кнопке пустой формы. Лечит «unknown: поля логина/пароля не найдены» при РЕАЛЬНО отрисованной форме. (2) `browser.js gotoResilient`: `waitUntil` `domcontentloaded`→`commit` (первый байт, толерантнее к моргающим резидентным прокси; форму/сессию дальше ждём явно), ретраи 2→3 + длиннее паузы; `attemptLogin` первая навигация retries 1→3, backoff [3,6,12]с, timeout 30с. Лечит `net::ERR_HTTP_RESPONSE_CODE_FAILURE` при `page.goto` (транзиент/лимит резидентного прокси — тот же IP минутой раньше грузил форму; парсинг прокси корректен: `splitProxy`→`{server,username,password}`). (3) BUILD воркера → `browser-12-loginfix` (по `/health` видно, что задеплоен воркер с этими фиксами). `node --check` воркера чисто; коммиты точечные (`workers/browser/lib/{login,browser}.js` + `server.js`). ⚠️ Локальный `tsc` падает на `app/api/auth/*` — ОЖИДАЕМО: паузная auth-работа ссылается на поля схемы (`googleId/emailVerified/EmailVerification`), откаченные в `c0f55bf` (ломали билд). Закоммиченный (Railway) стейт ЧИСТ (HEAD-схема без auth-полей, google/verify/resend не в гите). НЕ коммитить auth-файлы, пока с ними в одном коммите не пойдут поля схемы + миграция `20260705000000_auth_email_verify_google`. **«Воркер офлайн» в шапке в момент теста — вероятно транзиент от череды редеплоев (много пушей подряд, воркер перезапускался); `/health` подтверждён живым.**
- 2026-07-09 (12) — **Деплой Next.js падал 2 раза подряд — причина найдена и исправлена; РЕАЛЬНЫЙ баг индикатора «Воркер офлайн»; диагностика провала входа усилена по свежему живому кейсу.**
  1. **Деплой FAILED×2** (`d0b2dc99`, `fa6cad8a`, 14:42–14:46): расследовал `railway deployment list`/`railway logs --latest --build`. Корень — МОЙ же коммит `dd1fbc4` (запись 10): `git add prisma/schema.prisma` захватил ВЕСЬ диф файла, а не только мои `locale/timezoneId` — включая ЧУЖИЕ несохранённые изменения (nullable `User.password`, `googleId`, `EmailVerification`, паузная Google/email-auth работа), для которых нет закоммиченного кода. `tsc` на Railway падал на `app/api/auth/login/route.ts` (`compareSync(password, user.password)` — `string|null` не `string`). Фикс — `c0f55bf`: откатил User/EmailVerification к закоммиченному виду, оставил только `InstagramAccount.locale/timezoneId`. **Впредь: перед пушем схемы/любого часто разделяемого файла — проверять `git diff <file>` перед `git add`, не полагаться на «я редактировал только вот это», и гонять билд в ИЗОЛИРОВАННОМ `git worktree` от HEAD (а не в рабочей директории, где всегда подмешаны чужие несохранённые файлы) — так и подтвердил фикс перед пушем.** Деплой `fad65530` → SUCCESS, миграция `20260709010000_account_locale` применена (17 миграций), контейнер стартовал чисто.
  2. **«Воркер офлайн» в шапке — НЕ транзиент (вопреки записи 11), а реальный баг индикатора.** `lib/browser/client.ts browserHealth()` шёл через `browserFetch()` (всегда POST), а `workers/browser/server.js` регистрировал `/health` только `app.get` → POST давал 404 → индикатор ВСЕГДА показывал «офлайн», хотя воркер жив (GET `/health`, которым его проверяют Railway/curl, всегда отвечал). На вход/действия это НЕ влияло (`resolveEngine` смотрит на `BROWSER_WORKER_URL`, не на индикатор). Фикс (коммит `f951eeb`): `server.js` `app.get`→`app.all('/health',...)` (примет любой метод) + `client.ts browserHealth()` переписан на собственный лёгкий `fetch(GET)` вместо `browserFetch(POST)`. Билд → `browser-13-healthfix`. Изначально это увидел и исправил пользователь/параллельная сессия (правки уже лежали в рабочем дереве при старте этой части сессии) — я проверил (`tsc`+`node --check`+сборка в чистом `git worktree`) и запушил.
  3. **Диагностика провала входа усилена по СВЕЖЕМУ живому кейсу.** Пользователь: реальный вход упал с «поля логина/пароля не найдены», но приложенный скрин чётко показывает НОРМАЛЬНУЮ страницу входа с видимыми полями — предыдущий диагноз (только скрин) больше не объясняет причину. Добавлено (коммит `25a31e2`): `browser.js firstVisibleAnyFrame` — как `firstVisible`, но перебирает ВСЕ фреймы страницы (`page.frames()`), не только главный — на случай, если форма спрятана во фрейме (consent/anti-bot прослойка), которую обычный `page.locator()` не видит, хотя на скрине всё нормально. `findLoginForm` получил шаг 4 (фолбэк по всем фреймам). `domSummary(page)` — реальный DOM-дамп (name/type/aria-label первых 10 инпутов + число форм) **по КАЖДОМУ фрейму** при провале: уходит и в `console.error` воркера (видно в `railway logs`), и прямо в текст ошибки, которую видит пользователь — больше не нужно вручную пересылать скриншот, чтобы отличить «Instagram переименовал атрибуты» / «форма во фрейме» / «поля правда исчезли». Билд → `browser-14-formdiag`. ⚠️ Гипотеза про фрейм/переименованные атрибуты НЕ подтверждена — просто нет данных без нового провала; следующий провал входа сам покажет DOM-дамп в сообщении об ошибке, скриншот присылать уже не обязательно.
  Все три фикса задеплоены и подтверждены (`railway deployment list` → SUCCESS на обоих сервисах). `node --check`/`tsc --noEmit`/`next build` чисты (Next.js — через изолированный `git worktree`, чтобы не давала ложный «зелёный» подмесь паузных auth-файлов в рабочей директории). **Осталось:** дождаться следующей живой попытки входа и прочитать её DOM-дамп в логах/ошибке — тогда будет ясна настоящая причина «поля не найдены»; до этого §357/§9 по-прежнему блокированы.
- 2026-07-09 (13) — **`emailPassword` теперь шифруется at-rest (AES-256-GCM) — закрыт кусок открытого вопроса §12.** Пока ждал живую попытку входа пользователя, проверил grep'ом весь репозиторий на предмет шифрования — **выяснилось, что `sessionData`/`browserState`/`emailPassword` НИКОГДА не шифровались**, несмотря на то что `ENCRYPTION_KEY` (32 байта) был задан на Railway ещё с записи про почту (§4.5), а комментарий в схеме прямо обещал «ШИФРОВАТЬ». Формулировка в §12 «хранить `browserState` зашифрованным, как sessionData» была неверной — sessionData тоже лежал открытым текстом. Новый `lib/crypto.ts` (AES-256-GCM: iv+authTag+ciphertext в base64, префикс `enc1:` — `decrypt()` отличает новый шифротекст от старых plaintext-записей и не падает на них, `encryptionConfigured()` — graceful fallback, если переменная вдруг не задана). Подключено ТОЧЕЧНО к `emailPassword` в `lib/accountPersist.ts` — это поле пока НИГДЕ не читается обратно (IMAP-автокод §337 не реализован), поэтому изменение чисто аддитивное, риска для живого входа/действий нет. `sessionData`/`browserState` сознательно НЕ тронул — шифрование потребовало бы смены типа колонки (Json→String) и правки каждой точки чтения на живом пути (poll/route.ts, instrumentation.ts, lib/browser/actions.ts, воркер туда-обратно) — решение по-прежнему открыто, делать это именно сейчас (когда вход только начал стабильно работать) неоправданно рискованно. Round-trip encrypt/decrypt + plaintext-passthrough проверены отдельным прогоном; `tsc --noEmit` + сборка в изолированном `git worktree` чисты. Коммит точечный (`50c2d4c`), деплой подтверждён SUCCESS.
