# InstaGuard — ГЛОБАЛЬНЫЙ АУДИТ (для следующих агентов)

> # ⚠️ АРХИВ / УСТАРЕЛО (снимок на 2026-07-09). НЕ БРАТЬ В РАБОТУ КАК СПИСОК ЗАДАЧ.
>
> **Сверено с кодом 2026-07-16: все открытые `[ ]`-пункты этого файла УЖЕ исправлены или потеряли смысл.**
> Не переделывайте их — вот куда они делись:
> - **[A1]** (браузерный аккаунт без `browserState` уходил в мёртвый Python) — исправлено: гард в
>   `poll/route.ts` метит `CHALLENGE` + `skipped:'no-browser-session'`; сам Python-путь УДАЛЁН целиком (Фаза V).
> - **[A2]** (директы коммент-потока без пауз) — исправлено: пейсинг между комментами (`randDelay`),
>   плюс меж-потоковый дедуп директа (`dmedThisCycle`) — 1 человек = максимум 1 директ за цикл.
> - **[A3]** (фото-в-директ не отправлялось) — реализовано в воркере (`tryAttachPhoto`/`setInputFiles`),
>   отдельным сообщением после подтверждённого текста.
> - **[A4]/[A5]** (лайк коммента / тред-ответ) — честно помечены в UI; тред-reply вынесен в
>   `PLAN-BEHAVIOR.md §4.2` (единственный реально открытый пункт, нужен живой прогон).
> - **[M1]–[M5]** (стоимость/лимиты HikerAPI и черновые) — потеряли смысл: детект переведён на
>   **self-events** (свои уведомления), черновые/HikerAPI из детекта убраны (`plan4.md` Фазы A–G).
>
> **Актуальные планы:** `PLAN-PRODUCTION.md` (финальный перед рынком) · `PLAN-BEHAVIOR.md` (поведение) ·
> `plan4.md` (self-events) · `CLAUDE.md` (журнал — источник правды по тому, что сделано).
>
> Ниже — исторический текст аудита 2026-07-09 (оставлен как контекст, НЕ как задачи).

---

> Дата: 2026-07-09. Провёл: агент-ассистент (прямое чтение всего критического пути + статические проверки).
> Это **список проблем и подтверждённо-рабочего**, чтобы следующие агенты сразу начали чинить, а не переоткрывали.
> Формат находки: **[ID] Заголовок** — где · в чём суть · чем грозит · что делать. Ранжировано по важности.
> После исправления пункта — отметь `[x]`, впиши коммит и перенеси суть в `CLAUDE.md`. Приоритеты и цель — в `plan.md`.

---

## 0. TL;DR для занятого агента

- **Движок**: `resolveEngine` (lib/browser/engine.ts) всегда возвращает `'browser'`, кроме случая, когда не задан `BROWSER_WORKER_URL`. В проде задан → движок всегда браузерный.
- **Главная мина**: браузерные ветки везде гейтятся `engine==='browser' && account.browserState`. Если `browserState` пуст — код **молча падает в LEGACY-ветку, которая зовёт МЁРТВЫЙ Python-воркер** (404). См. **[A1]**.
- **Ban-risk №1**: директы **потока комментариев** шлются ИНЛАЙН без очереди и без пауз между комментаторами (в отличие от потока подписчиков, который через очередь с паузами 45–115с). См. **[A2]**.
- **Незакрытые функции** (UI честно помечен, но не работает): фото-в-директ **[A3]**, лайк коммента браузером **[A4]**, тред-ответ на коммент **[A5]**.
- **Не трогать — подтверждено рабочим**: см. §5 (лимиты, прогрев, лок, резерв бюджета fallback, предохранитель бан→пауза, «первый проход только база»).

---

## 1. КРИТИЧЕСКОЕ / ВЫСОКОЕ (риск бана или сломанная функция)

### [A1] Браузерный аккаунт без `browserState` → тихо уходит в МЁРТВЫЙ Python-воркер
- **Где**: `app/api/poll/route.ts` — `runFollowerActionsInline` (гейт `job.engine==='browser' && job.browserState`, стр. ~258), коммент-поток (`useBrowserForComments = engine==='browser' && Boolean(account.browserState)`, стр. ~744), стори-поток (стр. ~700/716); `instrumentation.ts` dm-воркер (тот же гейт, стр. ~36). Legacy-ветки зовут `sendDM/followUser/likeLatestMedia/likeComment/getStoryEvents/sendDMPhoto` из `lib/instagram/client.ts` → `PYTHON_WORKER_URL`.
- **Суть**: Python-воркер выведен из эксплуатации (мёртв, отдаёт 404 «Application not found»). Пока у аккаунта есть `browserState` — всё идёт через браузер (ок). Но если `browserState` пуст (аккаунт заведён по старой legacy-сессии `sessionData`; или браузерный вход не сохранил state; или state почистили) — исполнение проваливается в legacy-ветку и бьёт в мёртвый Python.
- **Чем грозит**: действия падают → `errorCount++` → после 5 подряд аккаунт метится `PAUSED` с невнятной ошибкой (таймаут/404). Пользователь видит «аккаунт остановлен», хотя это НЕ бан Instagram, а обращение к мёртвому сервису.
- **Что делать**: в обоих исполнителях + dm-воркере: если `engine==='browser'` и НЕТ `browserState` — **не звать legacy Python вообще**. Вместо этого залогировать понятное «нужен повторный вход браузером (нет браузерной сессии)» и выставить статус `CHALLENGE` (нужен вход), НЕ инкрементируя errorCount как «бан». Идеально — при листинге аккаунтов подсвечивать «нет браузерной сессии» и предлагать повторный вход. (Полное решение — Фаза 5: удалить legacy-ветки целиком, см. plan.md §357.)
- [x] **ИСПРАВЛЕНО 2026-07-09** (Фаза I): poll-гард в `app/api/poll/route.ts` — при `engine==='browser' && !browserState` аккаунт метится `CHALLENGE` + пропускается (`skipped:'no-browser-session'`), БЕЗ вызова Python и БЕЗ инкремента errorCount (лог WARN «переподключите аккаунт»). Плюс страховочные гарды в `runFollowerActionsInline` (poll) и dm-воркере (`instrumentation.ts`): `if (engine==='browser') return` до legacy-ветки. `tsc` чист (только паузные auth-ошибки, ожидаемо).

### [A2] Директы потока КОММЕНТАРИЕВ шлются инлайн без очереди и без пауз между комментаторами
- **Где**: `app/api/poll/route.ts`, коммент-поток стр. ~768–928 (цикл `for (const c of toProcess) { for (const trigger ...) }`). Все действия (директ/коммент/лайк/подписка/сторис) выполняются СИНХРОННО внутри одного запроса poll. Между действиями одного комментатора есть `randDelay(3,8)`, но **между разными комментаторами явной паузы нет**.
- **Суть**: поток ПОДПИСЧИКОВ ставит директы в очередь `dm-send` с разносом 45–115с и `concurrency:1` (безопасно). Поток КОММЕНТАРИЕВ — целиком инлайн. При 12 новых комментах это до 12 директов подряд с интервалом в единицы секунд.
- **Чем грозит**: серия быстрых директов/действий с одного аккаунта = классический сигнал спама → **бан/ограничение основного**. Единственная текущая защита — дневной лимит `dm=30` и `MAX_NEW_PER_POLL=12`.
- **Что делать**: перевести директы коммент-потока в ту же очередь `dm-send` (как follower-поток), ЛИБО добавить паузу ~45–90с между обработкой разных комментаторов + жёсткий потолок «директов за цикл». Приоритет — как у follower-потока.
- [ ] не исправлено

### [A3] Фото-в-директ (§334) не отправляется НИ на одном движке
- **Где**: `workers/browser/lib/actions.js` `sendDM` (шлёт только текст, upload нет); legacy-ветки poll/instrumentation зовут `sendDMPhoto` → мёртвый Python. UI уже честно помечен (прошлый батч: пометка в конструкторе + тултип бейджа).
- **Суть/грозит**: функция не работает; данные картинки сохраняются, но не уходят. НЕ регресс (UI не врёт), но и не «всё работает».
- **Что делать**: реализовать upload в браузерном `/dm` через Playwright `setInputFiles` буфером из data-URL картинки (картинка приходит base64), с graceful-фолбэком на текст; прокинуть `image` в `browserDM`/actions.ts/`/dm`/poll. Либо совсем убрать загрузчик. **Требует живого теста** (сейчас блокирован рабочим прокси).
- [ ] не исправлено

### [A4] Лайк конкретного комментария (LIKE_COMMENT) браузером не реализован
- **Где**: `app/api/poll/route.ts` стр. ~841–850 — браузерная ветка пушит `'лайк коммента: не поддерживается браузерным движком'`; legacy зовёт `likeComment` → мёртвый Python.
- **Суть/грозит**: затрагивает только СТАРЫЕ кампании (новые создают `LIKE_MEDIA`, не `LIKE_COMMENT` — см. triggers/page.tsx). UI уже помечен «(legacy)». Низкий охват, но действие молча не выполняется.
- **Что делать**: реализовать в браузере (найти комментарий в нити и кликнуть «Нравится») ИЛИ окончательно вывести `LIKE_COMMENT` из типов и миграцией схлопнуть старые кампании в `LIKE_MEDIA`.
- [ ] не исправлено

### [A5] «Ответ в комментариях» публикует обычный коммент к посту, а не тред-ответ комментатору
- **Где**: `workers/browser/lib/actions.js` `replyComment` → `commentPost` (top-level коммент); poll стр. ~743 честно это отмечает в комментарии кода. `lib/instagram/shortcode.ts` строит `postUrl` из `media_id`.
- **Суть/грозит**: пользователь ожидает ответ В ВЕТКЕ конкретному комментатору, а бот пишет общий коммент под постом. Коммент появляется (функция «частично работает»), но адресность теряется, и под постом копится «шум».
- **Что делать**: реализовать тред-ответ (клик «Ответить» под нужным комментом, ввод, отправка) — Фаза 4. До этого — честно пометить в UI конструктора: «ответ публикуется как отдельный комментарий под постом, не в ветке».
- [ ] не исправлено

---

## 2. СРЕДНЕЕ

### [M1] Экспериментальный черновой парсинг (`parsingSource='drafts'|'drafts_then_api'`) не проверен на живом Instagram
- **Где**: `workers/browser/lib/parse.js` (помечен ЭКСПЕРИМЕНТАЛЬНО), `lib/browser/client.ts` parseXBrowser, poll `parseFollowersFor/parseCommentsFor/parseLikersFor`.
- **Суть/грозит**: DOM-разбор модалок Instagram (вёрстка часто меняется). При `parsingSource='drafts'` и сбое — поток молча возвращает пусто → триггеры не срабатывают (не ban, но «тихо не работает»). Дефолт `'api'` безопасен; Настройки предупреждают («режимы дорабатываются»).
- **Что делать**: либо протестировать `parse.js` на живом HELPER-аккаунте и снять «эксперимент», либо в UI жёстко пометить draft-режимы как бета и оставить `'api'` единственным боевым.
- [ ] не исправлено

### [M2] Стоимость HikerAPI ничем не ограничена сверху (cost-risk)
- **Где**: `lib/scraper/hiker.ts` + вызовы в poll. За один цикл на аккаунт: followers + `scrapeUserInfo` + comments(3 медиа) + likers(3 медиа) + gate-followers(до 4 стр) + gate-following(до 4 стр). × N аккаунтов × до 48 циклов/сутки.
- **Суть/грозит**: при включённом гейте и многих аккаунтах — заметные деньги. Кулдауны (коммент 60м, лайк 30м) и кеши на процесс снижают, но глобального бюджета/лимита запросов нет. `follower_count` уже гейтится «раз в день» (haveToday) — ок.
- **Что делать**: дневной потолок запросов HikerAPI на владельца + счётчик/метрика расхода в UI; кешировать gate-множества дольше (сейчас пересобираются на цикл).
- [ ] не исправлено

### [M3] Инлайн-путь без Redis блокирует весь запрос poll надолго
- **Где**: poll — когда `dmQueue` null (нет `REDIS_URL`), follower-действия идут через `runFollowerActionsInline` с `randDelay(40,90)` между целями; коммент-поток инлайн всегда.
- **Суть/грозит**: один аккаунт с 12 целями = до ~18 мин в одном HTTP-запросе. Лок 25м, backstop авто-поллинга 20м. Несколько аккаунтов → перехлёст/таймаут/обрыв цикла. В проде Redis есть (очередь), так что это про окружения/периоды без Redis.
- **Что делать**: зафиксировать в доках, что **Redis обязателен для прода**; при отсутствии Redis — снизить число целей/паузы, либо не гонять инлайн длинные цепочки.
- [ ] не исправлено

### [M4] Гейт подписки может ложно решать «не подписан» на аккаунтах с большим числом подписчиков
- **Где**: poll `ensureGateFollowers` — множество = снапшот подписчиков + свежие 200 (`GATE_FOLLOWERS_SCAN`).
- **Суть/грозит**: если реальный подписчик не попал в выборку (200 свежих + накопленный снапшот) — гейт решит «не подписан» → DM пропущен. Это «ошибка в безопасную сторону» (пропуск легитимного DM, НЕ бан), код это признаёт. Но для крупных аккаунтов пропусков может быть много.
- **Что делать**: увеличивать снапшот со временем (уже копится), при желании — поднять `GATE_FOLLOWERS_SCAN` для аккаунтов с активным гейтом, либо отдельный «полный» прогрев множества раз в сутки.
- [ ] не исправлено (низкий приоритет — не ban)

### [M5] Стори-события и часть действий для чисто-legacy аккаунтов (только `sessionData`) → мёртвый Python
- **Где**: poll стр. ~716 `getStoryEvents(session,…)`; legacy-ветки коммент/лайк/подписка. Пересекается с **[A1]** (тот же корень — нет browserState).
- **Что делать**: закрывается вместе с [A1] (не звать legacy Python для browser-движка).
- [ ] см. [A1]

---

## 3. НИЗКОЕ / КОСМЕТИКА

- **[L1]** `UserSettings.actionEngine / allowNoDrafts / likeByDraft / storyByDraft` — LEGACY no-op поля, всё ещё в схеме/API. Не мешают, но путают. Вычистить в Фазе 5 вместе с legacy.
- **[L2]** `lib/instagram/client.ts`, `app/api/worker-health/route.ts`, `docker-compose.yml`, `.env.example` — всё ещё ссылаются на Python-воркер. Пока legacy не удалён (Фаза 5) — оставить, но пометить «LEGACY, не на боевом пути».
- **[L3]** `getStoryEvents` для стори-инбокса читает `/api/v1/direct_v2/...` из страницы (веб-приватный API) — форма события завязана на `item_type` (`reel_share`/`story_share`); при смене формата Instagram молча вернёт пусто. Держать под наблюдением.

---

## 4. ЧТО ПРОВЕРЕНО СТАТИКОЙ (этот аудит)

- `npx tsc --noEmit` — **чисто по всему боевому коду**. Единственные ошибки — в ПАУЗНЫХ auth-файлах (`app/api/auth/google/callback`, `app/api/auth/register/{route,resend,verify}`): они ссылаются на поля схемы (`googleId`, `emailVerification`, `emailVerified`), которых нет в ЗАКОММИЧЕННОЙ `prisma/schema.prisma` (миграция не применена, ждёт ключей — см. [[auth-work-paused]], plan.md §0/§7.7). Это ОЖИДАЕМО и НЕ на боевом пути. ⚠️ Локальный `tsc` даёт ложный результат в обе стороны в зависимости от того, по какой схеме сгенерён Prisma-клиент — надёжная проверка перед пушем: сборка в изолированном `git worktree` от HEAD.
- `node --check` по всем файлам `workers/browser/*.js` — **чисто**.
- Прочитан весь критический путь: `lib/browser/engine.ts`, `lib/limits.ts`, `app/api/poll/route.ts` (целиком), `instrumentation.ts` (dm-воркер + авто-поллинг), `lib/browser/actions.ts`, `workers/browser/lib/actions.js`, `lib/scraper/hiker.ts`.
- **Живой end-to-end НЕ прогонялся** — блокирован рабочим резидентным прокси ([[login-blocker-ip-blacklist]]). Все выводы — из чтения кода.

---

## 5. ПОДТВЕРЖДЕНО РАБОЧИМ — НЕ ТРОГАТЬ БЕЗ ПРИЧИНЫ

Эти механизмы проверены и корректны — не переоткрывать:

1. **Дневные лимиты + прогрев по возрасту** (`lib/limits.ts`): `scaleCaps(warmupFactor(createdAt))`, применяются во ВСЕХ потоках через единый `use(k,n)`. День 0 → 15% лимита, к 14-му → 100%.
2. **Глобальный лок `poll:all`** (poll стр. ~365): и полный авто-цикл, и ручная проверка одного аккаунта берут ОДНУ аренду → гонка счётчиков и задвоение директов закрыты.
3. **Резерв бюджета fallback** (`willDM && !willFollow && use('follow')`, стр. ~563): fallback follow+like при закрытой личке заранее списывает бюджет → не пробивает дневной потолок.
4. **Предохранитель бан/challenge → стоп** (`statusFromError` → `CHALLENGE`/`PAUSED`) в обоих исполнителях + `failed`-хендлер dm-воркера. Сбой ПАРСИНГА (API) при этом НЕ ставит основной на паузу (WARN, не бан) — сбой API ≠ бан.
5. **«Первый проход только фиксирует базу»** (`selectTargets`, `hadBaseline`): на новом аккаунте бот НЕ рассылает разом всем существующим подписчикам/комментаторам → нет массовой рассылки при подключении.
6. **Прокси обязателен по умолчанию** (`proxyRequired`): аккаунт без прокси пропускается (`skipped:'no-proxy'`), отключается тумблером «Работать без прокси».
7. **Браузерный воркер**: ограничение конкуренции (`BROWSER_CONCURRENCY`), человекоподобные паузы (`rd`/`jitter`/`humanType`), стабильный отпечаток + гео-локаль под страну прокси, headful под Xvfb, keep-alive прогрев (`browserWarmup`, ~раз в 3.5ч).
8. **Изоляция мультитенанта**: poll скоупится по `userId` (ручной) / все тенанты (крон по секрету); `/api/accounts`, `/api/logs` и др. фильтруют по владельцу.
9. **Исправлено в прошлых батчах** (не регрессировать): реальное число подписчиков через HikerAPI (`scrapeUserInfo`); проверка прокси не гасит живые прокси на сбое воркара; индикатор воркера в шапке; честные описания (Instagram API → браузер, фото, лайк коммента legacy); единый `AddAccountModal` для всех точек добавления (в т.ч. черновых) с обработкой 202 challenge/2FA.

---

## 6. РЕКОМЕНДУЕМЫЙ ПОРЯДОК ДЛЯ СЛЕДУЮЩИХ АГЕНТОВ

1. **[A1]** — самый коварный (тихий уход в мёртвый Python). Гардить browser-движок от отсутствия `browserState`. Дёшево, снимает ложные «PAUSED».
2. **[A2]** — ban-risk на реальном трафике комментариев. Перевести коммент-директы в очередь / добавить паузы.
3. **[A3]/[A4]/[A5]** — доделать функции (фото/лайк-коммента/тред-ответ) ИЛИ окончательно убрать из UI. Требуют живого прокси для теста.
4. **[M1]–[M4]** — черновые-бета, потолок стоимости HikerAPI, Redis-обязателен, гейт на крупных аккаунтах.
5. **Фаза 5** (plan.md §357) — удалить legacy Python целиком (закрывает [A1], [M5], [L1], [L2]) — **только после** сквозного теста браузерного входа на живом аккаунте.

> ⚠️ Общий блокер сквозного теста — рабочий резидентный прокси. Пока его нет, [A3]/[A4]/[A5] и Фазу 5 не закрывать вслепую.

---

# ЧАСТЬ 2 — ГЛУБОКИЙ АУДИТ: АНТИДЕТЕКТ · «ЖИВОСТЬ» · ЧЕРНОВЫЕ · API (2026-07-09, доп. проход)

> По запросу: максимально точно проверить антидетект (ни одного бот-фактора), безопасность черновых, эффективность API, «живость» поведения (прогревы, листание ленты, естественные паузы), слаженность триггер→действие. Записан КАЖДЫЙ мелкий недочёт, даже потенциальный.
> Методология: (а) **живой запуск реального браузерного кода воркара** (`getBrowser`+`newAccountContext` = stealth+fingerprint+init-скрипты) с дампом всех детект-векторов — см. §7.1; (б) прямое чтение антидетект-кода (`browser.js`, `human.js`, `fingerprint.js`, `login.js`, `server.js`), draft-пула, HikerAPI. Instagram-уровневый тест по-прежнему блокирован прокси.

## 7.1 Живой запуск браузера — дамп отпечатка (эмпирика)

> Скрипт `workers/browser/_fptest.mjs` (временный, удалить после): реальный `newAccountContext` под отпечаток `pl-PL / Europe/Warsaw`, дамп `navigator`/UA-CH/WebGL/permissions на `about:blank`.
> **РЕЗУЛЬТАТ ПРОГОНА — заполняется** (шёл параллельно с написанием отчёта; ключевые проверки: `navigator.webdriver`, UA-vs-platform, UA-CH `platform` под Linux/Xvfb, WebGL renderer=SwiftShader из-за `--disable-gpu`, timezone/locale-совпадение). Локальный прогон на Windows не 1:1 воспроизводит прод (Linux/Xvfb + `--disable-gpu` → SwiftShader), поэтому [D1]/[D2] помечены как подтверждаемые именно в проде.

## 7.2 Антидетект / отпечаток (D)

- **[D1] `--disable-gpu` в `LAUNCH_ARGS` → WebGL renderer = SwiftShader (софт-рендер) — сильная headless/VM-утечка.** `workers/browser/lib/browser.js:16`. Реальные юзеры отдают `ANGLE (NVIDIA/Intel/AMD…)`; SwiftShader/llvmpipe — маркер серверного/headless окружения, который читают FingerprintJS и антибот Instagram через `WEBGL_debug_renderer_info`. **HIGH.** Fix: убрать `--disable-gpu` (в headful под Xvfb рендер и так софтовый — но лучше спуфить `UNMASKED_RENDERER_WEBGL`/`UNMASKED_VENDOR_WEBGL` через `addInitScript` под правдоподобный GPU, консистентный с ОС отпечатка). — **[x] СДЕЛАНО 2026-07-09:** `--disable-gpu` убран; `fingerprint.js` даёт per-account GPU (Intel/NVIDIA/AMD по ОС), `browser.js` спуфит WebGL getParameter(37445/37446). Детектор-приёмка — §11.2.
- **[D2] Desktop-UA (Windows/Mac), а воркер бежит на Linux (Xvfb) → рассинхрон `navigator.platform` и UA-CH `Sec-CH-UA-Platform`.** `fingerprint.js` заявляет `Windows NT 10.0`, реальная ОС — Linux. Если stealth не патчит **UA Client Hints**, то `navigator.userAgentData.platform === "Linux"` при UA=Windows — это железобетонный бот-сигнал (и в JS, и в заголовках запросов `sec-ch-ua-platform`). **КРИТИЧНО** — проверить дампом §7.1 и на уровне сетевых заголовков. Fix: принудительно выставлять `sec-ch-ua-platform` и `userAgentData` под заявленную ОС (route-интерцептор заголовков + initScript). — **[x] СДЕЛАНО 2026-07-09 (код):** `browser.js` — `sec-ch-ua-platform` в extraHTTPHeaders + initScript спуфит `navigator.platform`/`userAgentData.platform`/`getHighEntropyValues` под ОС отпечатка. ⚠️ ПОДТВЕРДИТЬ замером через прокси (§11.2) — локально chromium не докачался.
- **[D3] UA прибит к `Chrome/130` хардкодом** (`fingerprint.js:18-22`). Дрейфует от реальной версии Chromium в образе Playwright (обновляется — коммент это признаёт). Реальная версия видна по JS-фичам и UA-CH `uaFullVersion` → расхождение «UA говорит 130, движок 1XX». **MED.** Fix: вычислять мажор из реального `chromium.version()`, не хардкодить.
- **[D4] `--lang=en-US` захардкожен в `LAUNCH_ARGS`, а `context.locale` = страна прокси** (pl-PL и т.д.). Рассинхрон языка на уровне браузера (Accept-Language/`navigator.language` базовый) vs контекста. **MED.** Fix: убрать `--lang` из args (контекст сам ставит locale) либо синхронизировать с гео.
- **[D5] Таймзона берётся из СТРАНЫ прокси, а не из реального гео IP.** `lib/browser/geo.ts localeForCountry(country)` → одна tz на страну. США → всегда `America/New_York`, даже если exit-IP в Калифорнии → расхождение tz на 3ч с geoIP (антибот сверяет browser-tz с geoIP). **MED.** Fix: брать tz из ответа `ipapi.is` по конкретному IP (он отдаёт `location.timezone`), а не по стране.
- **[D6] `deviceScaleFactor=1` для ВСЕХ профилей, включая Mac (1440×900/1680×1050).** Реальные Mac — Retina, DPR=2. Mac-UA + DPR=1 + отсутствие touch — лёгкий, но реальный mismatch. **LOW.** Fix: Mac-профилям DPR=2.
- **[D7] `hardwareConcurrency`/`deviceMemory` не спуфятся** — отдадут значения контейнера Railway (напр. 1-2 ядра, memory часто `undefined`). `deviceMemory=undefined` при десктоп-Chrome нетипичен. **LOW.** Проверить дампом. Fix: спуфить под правдоподобные (8 ядер / 8 ГБ).
- **[D8] Всего 5 desktop-профилей** (`fingerprint.js`). Стабильность на аккаунт — правильно, но у двух аккаунтов с одинаковым хэш-профилем отпечатки неотличимы. **LOW.** Fix: расширить пул.
- **[D9] Canvas/AudioContext/шрифты-fingerprint не рандомизируются per-account.** Stealth добавляет общий шум, но canvas-хэш у всех контекстов на ОДНОЙ машине может совпадать → «ферма на одном железе» (много аккаунтов = один canvas-хэш). **MED.** Fix: per-account seed для canvas/audio-noise.
- **[D10] `navigator.webdriver` глушится дважды** (stealth + `addInitScript` в `browser.js:75`) — безвредно, но избыточно; можно убрать ручной override (stealth уже покрывает).

## 7.3 Поведение / «живость» (B)

- **[B1] Каждое действие = СВЕЖИЙ контекст (open → одно действие → close).** `server.js actionRoute` создаёт `newAccountContext` на КАЖДЫЙ `/dm|/follow|/like|/stories`, делает ОДНО действие, закрывает (`finally`). Нет непрерывной сессии, нет браузинга — «одноцелевые» заходы (залогинился → сразу на профиль незнакомца → действие → вышел) = очень робот. **HIGH для живости.** Fix: держать сессию дольше, батчить действия одной цели, добавлять «побочную» активность.
- **[B2] Прогрев ленты (`warmupFeed`) вызывается ТОЛЬКО в keep-alive (`warmupSession`, ~раз в 3.5ч), НЕ перед действиями.** Действия идут через `openProfile` сразу на цель. **MED-HIGH.** Fix: лёгкий feed-браузинг (1-2 скролла/пары кликов) в начале каждой action-сессии.
- **[B3] Прогрев минимален: `warmupFeed` = 2 прокрутки колеса + `idleMouse`.** Нет лайков, просмотра сторис, переходов по профилям, «чтения» — слабо похоже на живого пользователя. **MED.** Fix: обогатить прогрев (случайно посмотреть 1-2 сторис, задержки чтения, иногда лайк в ленте).
- **[B4] Мышь не движется К кнопке человеческой траекторией перед кликом.** `.click({delay})` телепортирует курсор в центр элемента; `idleMouse` двигает случайно, но не к цели. **MED.** Fix: двигать курсор к элементу кривой (Безье) перед кликом.
- **[B5] Нет опечаток/backspace при вводе** — `humanType` печатает идеально по буквам (задержки есть, ошибок нет). Реальные люди иногда стирают/правят. **LOW.**
- **[B6] Фиксированный порядок действий** (DM → follow → like → story во всех исполнителях). Всегда одна последовательность = паттерн. **LOW-MED.** Fix: рандомизировать порядок действий на цель.
- **[B7] Нет суточного ритма / «сна».** Авто-поллинг каждые 30 мин 24/7 → аккаунт активен в 4 утра по СВОЕЙ таймзоне = не по-человечески. **MED.** Fix: гейт активности по локальному времени аккаунта (ночью тише/пауза). — **[x] СДЕЛАНО 2026-07-09:** `lib/activity.ts activityWindow` + гейт в poll (окно 7–9…22–24 по tz аккаунта + «выходные»; вне окна — пропуск, парсинг тоже). Лимиты заодно снижены (§1.8: dm10/follow12/like30/comment8/story40).
- **[B8] Ввод логина/пароля/кода — через `humanType` ✓** (login.js:214/216/249/339) — это ХОРОШО, не трогать.
- **[B9] `idleMouse` только при `openProfile`, но не во время «чтения» страницы действия** — мало движения между наведением и кликом. **LOW.**

## 7.4 Черновые аккаунты (H)

- **[H1] Черновые НЕ получают keep-alive/прогрев.** Поллинг греет только ОСНОВНЫЕ (`warmupSession` в главном цикле; HELPER исключены из sender-loop `role:{in:['RESPONDER','BOTH']}`). Сессии черновых стынут → чаще требуют повторного входа → выше риск бана черновых (а именно их пользователь просит «не банить»). **HIGH.** Fix: отдельный проход прогрева по активным HELPER (тот же `/session/warmup`). — **[x] СДЕЛАНО 2026-07-09:** проход прогрева HELPER в `poll/route.ts` (гейт по кулдауну+суточному окну+прокси; метит `CHALLENGE` при мёртвой сессии — заодно [H2] частично). tsc чист.
- **[H2] `pickDraft` берёт черновой с `browserState`, но НЕ проверяет, жива ли сессия.** `lib/browser/draftPool.ts` — только наличие `browserState`. Мёртвая сессия → парсинг падает → пусто (режим `drafts`) или API-фолбэк. Нет детекта/пометки мёртвого чернового. **MED.** Fix: `testSession` перед использованием либо метить `BLOCKED`/`CHALLENGE` при неудаче парсинга N раз.
- **[H3] Черновые делят тот же браузерный воркер и `BROWSER_CONCURRENCY` (2) с основными.** Парсинг черновым конкурирует за слоты с действиями основных → взаимная сериализация/замедление при нагрузке. **LOW-MED.** Fix: отдельный лимит/очередь для парсинга.
- **[H4] Draft-парсинг (`workers/browser/lib/parse.js`) ЭКСПЕРИМЕНТАЛЕН, не проверен на живом IG** (= [M1]). DOM-разбор модалок Instagram, вёрстка которых часто меняется → тихий сбой. **HIGH-risk тихого нуля.**
- **[H5] Один черновой обслуживает ВСЕХ основных владельца** (round-robin `lastChecked`, `take:5`). При многих основных — узкое горло + подозрительно много активности парсинга с одного чернового IP. **MED.** Fix: масштабировать/троттлить нагрузку на чернового.
- **[H6] Черновой парсит публичные данные ЧУЖИХ (основных) аккаунтов через свою сессию** — навигация на `/{main}/followers` модалку. Частые заходы «смотреть чужих подписчиков» с одного аккаунта — тоже поведенческий сигнал. **LOW-MED.** Держать умеренным.

## 7.5 API / HikerAPI (P)

- **[P1] Нет верхнего потолка расходов** (= [M2]). Оценка: ~15-20 платных запросов/аккаунт/цикл С гейтом (followers + userInfo + comments×3медиа + likers×3медиа + gate-followers до 4 стр + gate-following до 4 стр); ~8-10 без гейта. × N аккаунтов × до 48 циклов/сутки (с учётом кулдаунов комменты 24/сут, лайки 48/сут). **MED-HIGH (деньги).** Fix: дневной лимит запросов на владельца + метрика расхода в UI.
- **[P2] Кеши (`_uidCache`, `_mediaCache`) per-process, теряются на редеплое/рестарте.** Частые деплои → холодный кеш → лишние платные запросы. **LOW.** Fix: вынести кеш в Redis/БД с TTL.
- **[P3] `scrapeUserInfo` (follower_count) НЕ читает `_uidCache` — всегда отдельный запрос к `/v1/user/by/username`.** Если `scrapeUserId` уже дёрнул тот же эндпоинт в этом цикле — дубль-запрос. Гейтится «раз в день» (`haveToday`), поэтому мелочь, но лишняя. **LOW.** Fix: `scrapeUserInfo` класть полный объект в кеш, `scrapeUserId` брать pk оттуда.
- **[P4] Гейт-множества (`ensureGateFollowers/Following`) пересобираются КАЖДЫЙ цикл** (замыкание на аккаунт-цикл, не переживает между циклами) — до 4+4 платных страниц каждый поллинг у аккаунтов с активным гейтом. **MED (стоимость).** Fix: кешировать gate-множества между циклами (followers частично уже в снапшоте — переиспользовать снапшот активнее, добирать реже).
- **[P5] Нет retry/backoff на `429`/`5xx` HikerAPI.** `req()` бросает сразу; один транзиентный сбой = поток пропущен на весь цикл. **LOW-MED.** Fix: 1-2 ретрая с backoff на 429/503.
- **[P6] При недоступности API `scrape()` логирует WARN на КАЖДЫЙ поток отдельно** (followers/comments/likers) — спам одинаковых WARN в логах. **LOW.** Fix: агрегировать/троттлить.
- **[P7] `paginate maxPages=4` фиксирован** — для гейта (200) это до 4 стр; но лимиты потоков (`FOLLOWERS_FETCH_LIMIT=50`) → 1 стр. Ок, но стоит вынести в конфиг и снизить для дорогих эндпоинтов. **LOW.**

## 7.6 Триггер→действие: слаженность (T)

- **[T1]** browserState отсутствует → мёртвый Python — см. **[A1]** (критично).
- **[T2]** коммент-директы инлайн без пауз/очереди — см. **[A2]** (ban-risk).
- **[T3]** фото / лайк-коммента / тред-ответ не работают — см. **[A3]/[A4]/[A5]**.
- **[T4] `mergeStats` делает отдельный `SELECT triggerRule` на КАЖДОЕ действие** (poll + instrumentation) — N+1 запросов к БД при многих целях. **LOW (перф, не ban).** Fix: батчить/кешировать stats на цикл.
- **[T5] Гонка `browserState` при перезаписи из разных потоков.** Follower-поток (dm-воркер), коммент-поток (inline), стори, keep-alive — каждый сохраняет `browserState` в аккаунт независимо; при близких по времени операциях последний перезаписывает предыдущий (теряется «дозревание»). Лок `poll:all` защищает poll, но dm-воркер (очередь) и warmup идут отдельно. **LOW.** Fix: сериализовать запись browserState на аккаунт / версионирование.
- **[T6] Резерв бюджета fallback может «съедать» дневной лимит вхолостую** при массово закрытых личках (follow+like резервируются, но при успешном DM не тратятся — reserved сгорает). Осознанный размен (см. §5.3), но при холодном трафике занижает реальную пропускную способность. **LOW.**

## 7.7 Итог доп. аудита — приоритеты

**Максимально «убивают живость / палят бота» (чинить первыми):**
1. **[D2]** UA-CH platform под Linux при desktop-UA (если подтвердится дампом) — самый явный технический бот-сигнал.
2. **[D1]** WebGL SwiftShader из-за `--disable-gpu`.
3. **[B1]+[B2]** одноцелевые сессии без браузинга + прогрев не перед действиями.
4. **[H1]** черновые без прогрева (их сессии мрут → баны черновых).

**Затем:** [D5] tz по IP, [D3] UA-версия, [D9] canvas per-account, [B7] суточный ритм, [B4] траектория мыши, [P1]/[P4] потолок и кеш стоимости HikerAPI, [H2] health черновых.

> ⚠️ Всё это — по чтению кода + локальному запуску браузера. Финальную проверку «палимся или нет» даст только прогон через рабочий резидентный прокси на живом IG (детектор-страница creepjs/fingerprint.com ЧЕРЕЗ прокси + реальная сессия). Блокер прежний.

## 7.8 Покрытие известных защит Instagram (что обходим, что нет)

Сверка нашей системы с типовым списком триггеров Meta. Легенда: ✅ закрыто · ⚠️ частично · ❌ дыра.

| Защита Instagram | Статус | Как у нас / что не так |
|---|---|---|
| Вход с нового устройства/IP | ✅ | Прокси на аккаунт (резидент/мобильный), гео-локаль под страну; при новом устройстве IG шлёт код — обрабатываем challenge/2FA. |
| Датацентр-IP / VPN | ⚠️ | Детектим (`ipapi.is is_datacenter/is_vpn`), метим `flagged`, предпочитаем чистые — но НЕ блокируем жёстко; качество зависит от прокси пользователя. |
| Частая смена локации входа | ⚠️ | Прокси закреплён на аккаунт. Но пуловые прокси обслуживают несколько аккаунтов; tz берётся по СТРАНЕ, не по IP ([D5]). |
| Приватный API / сторонние сервисы (Buffer/Later, instagrapi) | ✅ (с оговоркой) | Главный смысл перехода: действия основного идут через **реальный браузер**, НЕ через API. Приватный API (instagrapi) больше не на боевом пути (кроме мёртвой legacy-ветки [A1]). Парсинг — HikerAPI, но он читает ПУБЛИЧНЫЕ данные по username, НЕ от лица аккаунта пользователя → API-сигнал на сам аккаунт не вешается. |
| Резкий всплеск активности | ⚠️ | Дневные лимиты + прогрев по возрасту + паузы 45–115с (поток подписчиков). НО поток комментариев шлёт инлайн без пауз ([A2]). |
| Нестандартный браузер / headless | ⚠️❌ | Реальный Chromium + stealth + официальный web.instagram.com — хорошо. НО: WebGL=SwiftShader из `--disable-gpu` ([D1]); возможен рассинхрон UA-CH `platform`=Linux при desktop-UA ([D2]); UA прибит к 130 ([D3]). Это технические бот-факторы. |
| Одно устройство/браузер на несколько аккаунтов | ⚠️❌ | Куки/UA/viewport/прокси — РАЗНЫЕ на аккаунт (контексты изолированы). НО canvas/audio/WebGL/железо-отпечаток ОБЩИЙ (все контексты на одной машине воркара) → на уровне hardware-fingerprint это «одно устройство → много аккаунтов» ([D9]). |
| Поведение как у бота (нет ритма, одноцелевые сессии) | ❌ | Каждое действие — свежий контекст, одно действие, без браузинга ([B1]); прогрев не перед действиями ([B2]); нет суточного «сна» ([B7]); мышь не идёт к кнопке ([B4]); фикс. порядок действий ([B6]). Здесь самый большой отрыв от «живого». |
| Молодой аккаунт / прошлые жалобы | ⚠️ | Прогрев лимитов по возрасту (14 дней). Историю купленного аккаунта не контролируем. |
| Смена пароля / вкл-выкл 2FA | ✅ | Не делаем ни того, ни другого. |
| Эмулятор Android на ПК | ✅ н/п | У нас реальный Chromium (desktop web), не эмулятор — фактор неприменим. |
| Черновые (парсеры) не банятся | ⚠️❌ | Черновые логинятся браузером (как основные), НО не прогреваются ([H1]) → сессии мрут; их health не проверяется ([H2]); DOM-парсинг экспериментален ([H4]). |

**Вывод:** технически-сетевые и «не-API» факторы закрыты хорошо (это и была цель перехода на браузер). **Реально недобираем в трёх местах:** (1) поведение слишком «роботное» ([B1/B2/B7/B4]), (2) hardware-отпечаток общий на все аккаунты одной машины ([D9] + [D1/D2]), (3) черновые без прогрева мрут ([H1]). Их закрытие = следующий приоритет «до идеала».
